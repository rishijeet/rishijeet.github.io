<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

    <title><![CDATA[Category: technology | Rishijeet Mishra | Technologist | Tech Trends & Development Blog]]></title>
    <link href="https://rishijeet.github.io/blog/categories/technology/atom.xml" rel="self"/>
    <link href="https://rishijeet.github.io/"/>
    <updated>2026-06-12T09:52:22+05:30</updated>
    <id>https://rishijeet.github.io/</id>
    <author>
        <name><![CDATA[Rishijeet Mishra]]></name>
        <email><![CDATA[rishijeet@gmail.com]]></email>
      </author>
    <generator uri="http://octopress.org/">Octopress</generator>

    
    <entry>
        <title type="html"><![CDATA[How CashKaro Works]]></title>
        <link href="https://rishijeet.github.io/blog/how-cashkaro-works/"/>
        <updated>2026-06-12T09:43:24+05:30</updated>
        <id>https://rishijeet.github.io/blog/how-cashkaro-works</id>
        <content type="html"><![CDATA[<blockquote><p><em>If you have ever used a cashback platform and wondered how the money actually flows from a merchant back to your wallet, you are not alone. Most users think of it as a simple discount. You click a link, buy something, and money appears in your account a few weeks later. But beneath that simple experience is a surprisingly intricate distributed system that touches affiliate networks, fraud detection, financial ledgers, event pipelines, and real-time attribution engines.</em></p></blockquote>

<p><img src="/images/system_design/cashkaro-banner.jpg" height="300" width="900" alt="Alt text" /></p>

<p>CashKaro is one of India&rsquo;s largest cashback and rewards platforms. It operates as a bridge between users who want to save money and merchants who want to acquire customers. Every time a user makes a purchase through CashKaro, a chain of events fires across multiple systems, involving third-party affiliate networks, internal computation engines, wallet services, and eventually a payment processor. Building and scaling that chain reliably is a serious engineering challenge.</p>

<p>This blog walks through the complete system design of a platform like CashKaro. We will go from first principles all the way to distributed systems concerns, and we will stop at every layer to explain not just what the system does but why it is built that way.</p>

<a name="Why.Cashback.Platforms.Are.More.Complex.Than.They.Look"></a>
<h2>Why Cashback Platforms Are More Complex Than They Look</h2>

<p>Cashback is fundamentally different from a discount. A discount reduces the price at the point of sale. Cashback is a post-purchase reward paid from the merchant&rsquo;s commission budget, routed through an affiliate network, validated against a return window, and then credited to a user&rsquo;s wallet after a delay of days or even weeks.</p>

<!--more-->


<p>This delay exists for a real business reason. If CashKaro credited cashback the moment someone clicked and bought something, a fraudulent user could make a purchase, collect the cashback, and then return the product. The merchant would refund the buyer while CashKaro would have already paid out. To protect against this, cashback is held in a pending state until the merchant&rsquo;s return window closes, typically fourteen to sixty days depending on the category.</p>

<p>This creates a temporal gap between the user&rsquo;s action and the reward, and that gap introduces a whole class of engineering problems around state management, event durability, reconciliation, and fraud prevention. None of these problems exist in a simple discount system.</p>

<p>There is also the affiliate ecosystem to consider. CashKaro does not have a direct financial relationship with every merchant on its platform. Instead, most merchants participate through affiliate networks like Commission Junction, Admitad, or Rakuten. These networks act as intermediaries that track sales attributed to CashKaro and report commission earnings. This means CashKaro is dependent on third-party systems for attribution data, and those systems have their own reporting delays, data formats, and reliability characteristics.</p>

<p>The result is that building a cashback platform means simultaneously building a marketplace, a fintech product, a real-time tracking system, and a batch reconciliation engine. Each of these has different latency, consistency, and reliability requirements, and they all have to work together coherently.</p>

<a name="Core.Features.of.CashKaro"></a>
<h2>Core Features of CashKaro</h2>

<p>Before diving into architecture, it helps to enumerate what the platform actually does from a product perspective.</p>

<p>Users browse a catalog of merchant offers with associated cashback rates. They click through to a merchant&rsquo;s website using a tracked affiliate link. They make a purchase on the merchant&rsquo;s platform. CashKaro receives a notification from the affiliate network that a sale has occurred and attributes it to the user who clicked. After a validation period, the cashback moves from pending to confirmed and becomes available for withdrawal. Users can redeem their confirmed cashback via bank transfer or as gift cards.</p>

<p>In addition to this core loop, the platform supports a referral program where existing users earn rewards for bringing in new users, promotional cashback boosts tied to specific campaigns, offer discovery features like search and category browsing, and a notification system that keeps users informed about their reward status.</p>

<p>Each of these features has non-trivial engineering requirements. The referral program alone has abuse prevention challenges, and the notification system has to operate at scale with proper prioritization and delivery guarantees.</p>

<a name="High-Level.Architecture"></a>
<h2>High-Level Architecture</h2>

<p>At the top level, CashKaro&rsquo;s architecture can be broken into a client layer, a set of backend microservices, a data layer, and an event streaming layer. Let us look at each.</p>

<div class="mermaid">
flowchart TD
    subgraph Clients
        A[Mobile App]
        B[Web App]
    end

    subgraph Gateway
        C[API Gateway]
    end

    subgraph CoreServices
        D[Offer Service]
        E[Affiliate Tracking Service]
        F[Attribution Engine]
        G[Cashback Engine]
        H[Wallet Service]
        I[Redemption Service]
        J[Fraud Detection Service]
        K[Notification Service]
        L[Referral Service]
        M[Merchant Catalog Service]
    end

    subgraph EventLayer
        N[Event Bus - Kafka]
    end

    subgraph Storage
        O[PostgreSQL]
        P[Redis Cache]
        Q[Elasticsearch]
        R[Object Store]
    end

    A &#8211;> C
    B &#8211;> C
    C &#8211;> D
    C &#8211;> E
    C &#8211;> H
    C &#8211;> I
    D &#8211;> M
    E &#8211;> F
    F &#8211;> G
    G &#8211;> H
    H &#8211;> I
    F &#8211;> N
    G &#8211;> N
    H &#8211;> N
    N &#8211;> J
    N &#8211;> K
    N &#8211;> L
    D &#8211;> P
    M &#8211;> P
    H &#8211;> O
    F &#8211;> O
    G &#8211;> O
    J &#8211;> Q

    classDef client fill:#4A90D9,color:#fff
    classDef gateway fill:#E67E22,color:#fff
    classDef service fill:#27AE60,color:#fff
    classDef event fill:#8E44AD,color:#fff
    classDef storage fill:#2C3E50,color:#fff

    class A,B client
    class C gateway
    class D,E,F,G,H,I,J,K,L,M service
    class N event
    class O,P,Q,R storage
</div>


<p>The API Gateway is the single entry point for all client requests. It handles authentication, rate limiting, and request routing. Behind it, a collection of microservices each own a specific domain. The Offer Service manages the merchant catalog and cashback rates. The Affiliate Tracking Service handles click events and link generation. The Attribution Engine matches purchases to users. The Cashback Engine computes reward amounts. The Wallet Service maintains user balances. The Redemption Service manages payouts. The Fraud Detection Service operates as a cross-cutting concern that observes events across all these services.</p>

<p>Event streaming, typically via a system like Apache Kafka, connects everything asynchronously. When a click happens, it becomes an event. When a purchase is attributed, that becomes an event. When cashback is computed, credited, or redeemed, those are all events. This event-driven design is not just an architectural preference; it is a requirement imposed by the nature of the domain, as we will see in detail later.</p>

<a name="Merchant.Integration.Architecture"></a>
<h2>Merchant Integration Architecture</h2>

<p>One of the most underappreciated complexities in a cashback platform is merchant integration. CashKaro works with hundreds of merchants simultaneously, and no two integrations are exactly alike.</p>

<p>There are two broad categories of merchant integration. The first and most common is through affiliate networks. Networks like Admitad aggregate thousands of merchants and provide a standardized API for generating tracking links, receiving postback notifications when sales occur, and querying commission reports. CashKaro integrates with multiple such networks, each with its own authentication protocol, data schema, and reporting schedule.</p>

<p>The second category is direct merchant integration. Larger merchants like Flipkart or MakeMyTrip may have their own affiliate programs with custom APIs. These are often more reliable and provide richer data, but they require custom engineering for each merchant.</p>

<div class="mermaid">
flowchart LR
    subgraph CashKaro
        A[Merchant Sync Service]
        B[Tracking Link Generator]
        C[Postback Handler]
        D[Reconciliation Engine]
    end

    subgraph Networks
        E[Admitad]
        F[Commission Junction]
        G[Rakuten]
    end

    subgraph DirectMerchants
        H[Flipkart API]
        I[MakeMyTrip API]
    end

    A &#8211;>|Pull offer data| E
    A &#8211;>|Pull offer data| F
    A &#8211;>|Pull offer data| G
    A &#8211;>|Pull offer data| H
    A &#8211;>|Pull offer data| I
    E &#8211;>|Sale postback| C
    F &#8211;>|Sale postback| C
    G &#8211;>|Sale postback| C
    H &#8211;>|Order event| C
    I &#8211;>|Order event| C
    B &#8211;> E
    B &#8211;> F
    C &#8211;> D

    classDef internal fill:#27AE60,color:#fff
    classDef network fill:#E67E22,color:#fff
    classDef direct fill:#4A90D9,color:#fff

    class A,B,C,D internal
    class E,F,G network
    class H,I direct
</div>


<p>The Merchant Sync Service runs periodic jobs to pull updated offer data from affiliate networks. Merchant cashback rates change frequently, offers expire, and new promotions are added. This sync process has to be idempotent, meaning running it twice should not create duplicate offers, and it has to handle partial failures gracefully, since an affiliate network API might return data for some merchants but fail on others.</p>

<p>Tracking link generation is worth examining closely. When CashKaro creates a link for a merchant offer, it does not just return the merchant&rsquo;s URL. It constructs a URL through the relevant affiliate network that encodes CashKaro&rsquo;s affiliate ID, a campaign identifier, and a sub-ID that it will use to identify the specific user later. The format varies by network, but the principle is the same across all of them.</p>

<p>Postback handling is the flip side. When a sale occurs, the affiliate network sends an HTTP callback to CashKaro&rsquo;s postback URL, carrying the order ID, sale amount, commission amount, and the sub-ID that was included in the original tracking link. This is the moment when CashKaro learns that a purchase has happened and can begin the attribution and cashback computation process.</p>

<p>The reconciliation challenge is significant. Affiliate networks batch their reporting, often with a delay of twenty-four to forty-eight hours. So CashKaro might receive a postback in near-real-time for some networks, but for others it has to poll a reporting API daily to discover sales. These two modes of learning about purchases require different processing pipelines but ultimately feed into the same attribution engine.</p>

<a name="Affiliate.Tracking.System"></a>
<h2>Affiliate Tracking System</h2>

<p>The affiliate tracking system is the most technically nuanced part of the platform. Its job is to answer a deceptively simple question: when a user makes a purchase on Amazon or Myntra, how do we know they came from CashKaro?</p>

<p>The answer involves tracking URLs, affiliate identifiers, cookies, and postback parameters working together.</p>

<p>When a user clicks on an offer in the CashKaro app, the client sends a request to the CashKaro tracking endpoint rather than going directly to the merchant. The tracking endpoint performs several operations atomically: it logs the click event with a timestamp and user ID, it generates a tracking URL for the appropriate affiliate network, and it returns a redirect to that tracking URL.</p>

<p>The affiliate network&rsquo;s tracking URL contains CashKaro&rsquo;s affiliate ID and a sub-ID. The sub-ID is a string that CashKaro controls and uses to carry user context. A typical sub-ID might encode the user&rsquo;s internal ID, the offer ID, and the click&rsquo;s timestamp. The affiliate network stores this sub-ID and associates it with the session.</p>

<p>When the user completes a purchase on the merchant&rsquo;s site, the merchant fires a tracking pixel or makes a server-to-server call to the affiliate network, reporting the order details. The affiliate network then sends a postback to CashKaro&rsquo;s postback handler URL, including the sub-ID it stored at click time. This is how the purchase gets attributed back to the original user.</p>

<div class="mermaid">
flowchart TD
    A[User clicks offer in app]
    B[CashKaro Tracking Endpoint]
    C[Log click event]
    D[Generate affiliate tracking URL with sub-ID]
    E[Redirect user to affiliate network URL]
    F[Affiliate network sets cookie and redirects to merchant]
    G[User browses and purchases on merchant site]
    H[Merchant fires conversion pixel to affiliate network]
    I[Affiliate network sends postback to CashKaro]
    J[CashKaro Postback Handler receives order data plus sub-ID]
    K[Attribution Engine resolves user from sub-ID]
    L[Cashback Computation Engine calculates reward]
    M[Wallet Service credits pending cashback]

    A &#8211;> B
    B &#8211;> C
    B &#8211;> D
    D &#8211;> E
    E &#8211;> F
    F &#8211;> G
    G &#8211;> H
    H &#8211;> I
    I &#8211;> J
    J &#8211;> K
    K &#8211;> L
    L &#8211;> M

    classDef user fill:#4A90D9,color:#fff
    classDef cashkaro fill:#27AE60,color:#fff
    classDef external fill:#E67E22,color:#fff
    classDef wallet fill:#8E44AD,color:#fff

    class A user
    class B,C,D,J,K,L cashkaro
    class E,F,G,H,I external
    class M wallet
</div>


<p>There are important failure modes in this flow. If the user clears their browser cookies between clicking and purchasing, attribution can break for web flows. If the user switches devices, the attribution may fail entirely unless the affiliate network supports fingerprinting or the user is logged into both the merchant and CashKaro. Mobile app tracking is generally more reliable because it uses device-level identifiers rather than browser cookies, but it comes with its own challenges around in-app browser sessions.</p>

<p>The sub-ID approach is also the source of a subtle timing risk. CashKaro uses the click&rsquo;s timestamp in the sub-ID to enforce attribution windows. If a postback arrives more than thirty days after the original click, the attribution engine will reject it as outside the window. This is a deliberate business decision: it prevents situations where a user clicked an offer months ago, forgot about it, and then happened to buy from the same merchant through a different path. Without attribution windows, the system would overcredit and the fraud surface area would expand significantly.</p>

<a name="Click.Redirection.Workflow.in.Detail"></a>
<h2>Click Redirection Workflow in Detail</h2>

<p>The click redirection flow looks simple from the outside but has meaningful latency and reliability implications at scale.</p>

<p>When a user taps an offer, the app sends a request to the CashKaro API. The tracking endpoint receives this request and needs to do several things quickly: authenticate the user, validate the offer is still active, log the click, generate the affiliate URL with the correct sub-ID encoding, and return the redirect URL. All of this should complete in under two hundred milliseconds to avoid noticeable user experience degradation.</p>

<p>The click logging step is where the first engineering tradeoff appears. A synchronous write to the main database on every click is not viable at scale. During a Diwali flash sale, CashKaro might see hundreds of thousands of clicks per hour. Writing each one synchronously to PostgreSQL would create a bottleneck that slows down the entire redirect flow.</p>

<p>The standard solution is to write click events asynchronously. The tracking endpoint publishes the click event to Kafka and immediately generates the redirect URL without waiting for the database write to complete. A consumer service picks up the event from Kafka and persists it to the database. This decouples the user-facing latency from the storage operation.</p>

<p>The tradeoff is that in the window between the click and the Kafka consumer writing to the database, the click is not yet durable. If the Kafka message is lost (which should not happen with proper replication settings but is theoretically possible), the attribution context for that click is gone. This is an acceptable risk for most click events because the attribution window is long and a small number of lost clicks does not significantly affect revenue. The more critical events, like confirmed purchases, get stricter durability guarantees.</p>

<p>Affiliate URL generation also needs to be fast. If the offer catalog data for URL generation is in a hot cache in Redis, this operation is microseconds. If it requires a database round-trip to fetch the merchant&rsquo;s affiliate program configuration, it is milliseconds. At high scale, even a few milliseconds per request adds up, so merchant configurations are aggressively cached.</p>

<a name="Transaction.Attribution.Engine"></a>
<h2>Transaction Attribution Engine</h2>

<p>Once a postback arrives from an affiliate network, the attribution engine has to answer several questions. Which user does this purchase belong to? Is this purchase new or have we seen it before? Is it within the attribution window? Is the purchase amount valid?</p>

<p>The sub-ID decoding is the first step. The attribution engine parses the sub-ID from the postback, extracts the encoded user ID, offer ID, and click timestamp, and queries the click log to verify that a matching click event exists. This cross-referencing is important because CashKaro&rsquo;s sub-IDs could theoretically be forged by a sophisticated attacker trying to steal cashback. Verifying against the click log ensures the attribution chain is intact.</p>

<p>Duplicate detection is the next concern. Affiliate networks sometimes send the same postback multiple times due to their own retry logic or network issues. The postback handler must be idempotent: receiving the same order ID twice must not result in double cashback crediting. This is implemented with a unique constraint on the order ID and network combination in the transactions table, combined with an upsert operation rather than a blind insert.</p>

<p>Cancellations and returns are handled through a separate update flow. When a user returns a product, the merchant notifies the affiliate network, which sends an updated postback with a negative or zero commission amount. The attribution engine processes this update by marking the original transaction as cancelled and reversing the pending cashback credit from the wallet. If the cashback had already moved from pending to confirmed, the reversal is more complicated and may involve a debit from the user&rsquo;s confirmed balance.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Transaction State</th>
        <th>Trigger</th>
        <th>Cashback State</th>
        <th>Action Required</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Order Placed</td>
        <td>Postback received from affiliate network</td>
        <td>Pending</td>
        <td>Credit pending balance, schedule validation</td>
      </tr>
      <tr>
        <td>Return Window Active</td>
        <td>Time elapsed since order</td>
        <td>Pending</td>
        <td>No action, monitor for cancellation signal</td>
      </tr>
      <tr>
        <td>Return Window Closed</td>
        <td>Merchant confirmation or timer expiry</td>
        <td>Confirmed</td>
        <td>Move balance from pending to confirmed</td>
      </tr>
      <tr>
        <td>Order Cancelled</td>
        <td>Cancellation postback received</td>
        <td>Reversed</td>
        <td>Remove pending credit, log reversal</td>
      </tr>
      <tr>
        <td>Order Returned</td>
        <td>Return postback after confirmation</td>
        <td>Clawed Back</td>
        <td>Debit confirmed balance, trigger audit</td>
      </tr>
      <tr>
        <td>Partial Refund</td>
        <td>Partial return postback</td>
        <td>Adjusted</td>
        <td>Recalculate cashback on adjusted order value</td>
      </tr>
    </tbody>
  </table>
</div>


<a name="Cashback.Computation.Engine"></a>
<h2>Cashback Computation Engine</h2>

<p>Once a transaction is attributed, the cashback computation engine calculates how much cashback the user earns. This is more nuanced than it first appears.</p>

<p>Cashback rates are not always a simple percentage of the order total. They vary by category, by product, by time, and by promotional campaign. A merchant might offer five percent cashback on electronics but two percent on grocery. A campaign might boost cashback to ten percent for the first twenty-four hours of a flash sale. Certain products might be excluded entirely from cashback eligibility, which is common for products with thin margins like smartphones or prepaid recharges.</p>

<p>The computation engine therefore needs access to the offer configuration at the time of the purchase, not just the current offer configuration. This is a subtle but important distinction. If the cashback rate was boosted to ten percent during a flash sale and a user bought during that window, they are entitled to the ten percent rate even if the rate has since reverted to five percent. This means the system cannot just look up the current offer when computing cashback; it needs to look up the offer as it existed at the time of the click or the purchase.</p>

<p>One way to handle this is to snapshot the relevant offer data at click time and store it as part of the click event record. When the postback arrives, the computation engine retrieves the click record and uses the snapshotted rate rather than the current one. This is a straightforward solution that avoids complex temporal queries.</p>

<p>The commission structure from the affiliate network also affects the cashback amount. CashKaro earns a commission from the merchant, and it passes a portion of that commission back to the user as cashback. The split between CashKaro&rsquo;s margin and the user&rsquo;s cashback is part of the offer configuration and varies by merchant relationship. The computation engine needs to know both the gross commission and the user&rsquo;s share.</p>

<p>Category-based exclusions are implemented as a rule set evaluated against the product category codes reported in the postback. If the merchant reports that an order contained multiple items across different categories, the engine may apply different rates to different line items, or it may apply the lowest applicable rate to the entire order, depending on the merchant&rsquo;s contract terms.</p>

<a name="Pending.vs.Confirmed.Cashback"></a>
<h2>Pending vs Confirmed Cashback</h2>

<p>The lifecycle of a cashback credit is central to how the platform manages financial risk.</p>

<p>When a postback first arrives, the cashback is created in a pending state. The user can see it in their wallet and track it, but they cannot redeem it. The pending state represents a conditional reward: we have seen a purchase, but we are not yet certain it will stick.</p>

<p>The transition from pending to confirmed happens through one of two mechanisms. The first is explicit merchant confirmation, where the affiliate network sends a confirmation signal indicating that the order is past the return window and the commission has been approved. The second is a timer-based confirmation, where CashKaro&rsquo;s internal confirmation scheduler moves cashback to confirmed after a fixed period based on the merchant&rsquo;s return policy, in the absence of a cancellation signal.</p>

<p>The timer-based approach is less accurate but more practical. Most affiliate networks do not send reliable confirmation signals; they send postbacks for new sales and cancellations but do not explicitly confirm non-cancelled orders. So CashKaro has to infer confirmation by waiting long enough that cancellations are unlikely.</p>

<p>This creates an interesting systems design problem. CashKaro needs to schedule a future state transition for every pending cashback record. The naive approach of polling the database for pending records older than the confirmation threshold and updating them is functional but does not scale well. A better approach is to schedule a delayed event at cashback creation time, using a system like a delayed message queue or a scheduled job service. When the event fires, it triggers the confirmation check. This is more efficient because it targets specific records rather than scanning the entire pending set.</p>

<a name="Wallet.Architecture"></a>
<h2>Wallet Architecture</h2>

<p>The user wallet is the financial core of the platform. Getting its design right is critical because mistakes here directly impact users&#8217; money.</p>

<p>A wallet has multiple balance buckets that represent different states of money:</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Balance Type</th>
        <th>Description</th>
        <th>User Action Allowed</th>
        <th>Example Source</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Pending Balance</td>
        <td>Cashback earned but not yet confirmed</td>
        <td>View only</td>
        <td>Recent purchase postback</td>
      </tr>
      <tr>
        <td>Confirmed Balance</td>
        <td>Validated cashback available for redemption</td>
        <td>Redeem or withdraw</td>
        <td>Pending balance after confirmation window</td>
      </tr>
      <tr>
        <td>Redeemed Balance</td>
        <td>Amount submitted for payout</td>
        <td>Track payout status</td>
        <td>User-initiated withdrawal request</td>
      </tr>
      <tr>
        <td>Referral Bonus</td>
        <td>Rewards from referrals, may have separate rules</td>
        <td>Redeem after conditions met</td>
        <td>Referred user&#8217;s first purchase</td>
      </tr>
    </tbody>
  </table>
</div>


<p>The wallet is implemented as a ledger, not as a single balance field. This is the fundamental design decision that makes wallet systems reliable. Instead of updating a balance number directly, every financial event is recorded as a ledger entry: credit or debit, amount, source transaction ID, timestamp, and resulting balance. The current balance is derived from summing all ledger entries, or more practically, from a cached sum that is updated incrementally.</p>

<p>This ledger approach provides an immutable audit trail. If a user disputes their balance, the system can replay every credit and debit and arrive at the exact current state. It also prevents a class of consistency bugs that would arise from concurrent balance updates, because the system can use optimistic locking on the balance version and retry on conflict rather than risking a lost update.</p>

<div class="mermaid">
flowchart TD
    subgraph Wallet
        A[Pending Balance]
        B[Confirmed Balance]
        C[Redeemed Balance]
        D[Ledger Store]
    end

    E[Attribution Engine] &#8211;>|Credit pending| A
    A &#8211;>|Confirmation event| B
    A &#8211;>|Cancellation event| F[Reversal - remove pending]
    B &#8211;>|Redemption request| C
    B &#8211;>|Clawback event| G[Debit confirmed]
    E &#8211;> D
    A &#8211;> D
    B &#8211;> D
    C &#8211;> D

    classDef balance fill:#27AE60,color:#fff
    classDef event fill:#4A90D9,color:#fff
    classDef action fill:#E67E22,color:#fff
    classDef storage fill:#2C3E50,color:#fff

    class A,B,C balance
    class E event
    class F,G action
    class D storage
</div>


<p>One important design consideration is that the wallet service should be a separate microservice with its own database, rather than tables in a shared database accessed by multiple services. This isolation ensures that only the wallet service can write to wallet data, which simplifies reasoning about consistency and makes auditing straightforward. Other services request wallet operations via API rather than writing directly.</p>

<a name="Cashback.Redemption.System"></a>
<h2>Cashback Redemption System</h2>

<p>When a user decides to redeem their confirmed cashback, the redemption service takes over. Redemption can go to a bank account via NEFT or IMPS, or it can be converted to gift cards from partner retailers.</p>

<p>The redemption flow has several validation gates. The minimum redemption threshold must be met, usually somewhere between fifty and one hundred rupees. The user&rsquo;s KYC status must be valid for bank transfers above a regulatory threshold. The bank account details must be on file and verified.</p>

<p>Once these checks pass, the redemption service creates a payout record, debits the confirmed balance in the wallet (via the ledger), and submits the payout request to the payment processor. The payment processor is typically a banking API partner or a payout aggregator that handles the actual transfer.</p>

<p>Payout processing is asynchronous. The payment processor accepts the request and returns a transaction reference, then processes the actual transfer in the background. The redemption service uses a webhook from the payment processor to update the payout status from initiated to processing to completed or failed.</p>

<p>Failures require careful handling. If the payout fails due to an incorrect bank account number, the system needs to credit the amount back to the user&rsquo;s confirmed balance, notify the user, and give them an opportunity to correct the bank details. If the payout fails due to a payment processor issue, the system should retry automatically after a backoff interval rather than immediately crediting back and leaving the user confused.</p>

<p>Reconciliation is a background process that runs daily to verify that the sum of all initiated payouts matches the amounts recorded by the payment processor. Discrepancies trigger alerts and manual review. This is not just good practice; for a platform handling real money, reconciliation is a regulatory requirement in many jurisdictions.</p>

<a name="Fraud.Detection.Systems"></a>
<h2>Fraud Detection Systems</h2>

<p>Fraud in a cashback platform takes several forms, and each requires a different detection strategy.</p>

<p>Self-referral fraud is one of the most common patterns. A user creates multiple accounts, uses one to refer the other, and then makes a purchase through both to collect referral bonuses and cashback multiple times. Detection relies on matching signals like device fingerprints, IP addresses, payment instrument hashes, and behavioral patterns. If two accounts that refer each other share the same device fingerprint, the referral is flagged for manual review.</p>

<p>Transaction fraud involves making purchases with the intent to return after collecting cashback. The attribution system&rsquo;s return window handling is the primary defense here, but sophisticated fraudsters may attempt to game this by making non-returnable purchases like digital goods or gift cards, which is why many cashback platforms explicitly exclude these categories.</p>

<p>Affiliate fraud is a more sophisticated attack where someone manipulates tracking parameters to steal attribution for purchases that would have happened anyway, either through cookie stuffing or by injecting CashKaro&rsquo;s affiliate IDs into links shared outside the platform. Cookie stuffing specifically involves loading a hidden iframe that fires the tracking pixel without the user taking any intentional action, which inflates click counts and may attribute organic purchases to CashKaro.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Fraud Type</th>
        <th>Detection Signal</th>
        <th>Mitigation Strategy</th>
        <th>Risk Level</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Self-referral</td>
        <td>Device fingerprint overlap, same IP, shared payment method</td>
        <td>Device graph matching, multi-account detection</td>
        <td>High</td>
      </tr>
      <tr>
        <td>Return abuse</td>
        <td>High return rate per user, short hold periods</td>
        <td>Extended pending windows, return rate scoring</td>
        <td>High</td>
      </tr>
      <tr>
        <td>Cookie stuffing</td>
        <td>Very high click-to-purchase ratio, no session depth</td>
        <td>Click quality scoring, affiliate audit</td>
        <td>Medium</td>
      </tr>
      <tr>
        <td>Fake transactions</td>
        <td>Mismatched order amounts, unusual category patterns</td>
        <td>Merchant cross-referencing, order validation API</td>
        <td>Medium</td>
      </tr>
      <tr>
        <td>Account takeover</td>
        <td>Unusual login location, rapid redemption after login</td>
        <td>Device change alerts, redemption cooling-off period</td>
        <td>High</td>
      </tr>
      <tr>
        <td>Automated click bots</td>
        <td>Inhuman click timing, non-standard user agents</td>
        <td>Rate limiting, CAPTCHA on suspicious sessions</td>
        <td>Medium</td>
      </tr>
    </tbody>
  </table>
</div>


<p>The fraud detection service consumes events from Kafka and applies both rule-based checks and machine learning models. Rule-based checks handle known patterns, like flagging any account that makes more than five referrals in a single day. ML models handle emergent patterns by scoring users and transactions against a baseline of normal behavior. High-risk scores trigger holds on payouts and alerts for human review, rather than automatic account suspension, because false positives are expensive in terms of user trust.</p>

<a name="Referral.Program.Architecture"></a>
<h2>Referral Program Architecture</h2>

<p>The referral program is conceptually simple but operationally complex. A user shares a referral code. A new user signs up using that code. The new user makes their first qualifying purchase. Both the referrer and the referee receive a reward.</p>

<p>The challenge is the &ldquo;qualifying purchase&rdquo; condition, which typically means the purchase has to be real and confirmed. So the referral reward cannot be credited immediately on signup; it has to wait for the new user&rsquo;s first purchase to be attributed and confirmed. This means the referral service has to maintain state across two separate events that may be separated by days or weeks.</p>

<p>This is implemented using a state machine. When a user signs up with a referral code, the referral service creates a referral record in a pending state, linked to both the referrer and the referee. When the referee&rsquo;s first qualifying cashback is confirmed, an event is published to Kafka. The referral service consumes this event, transitions the referral record to fulfilled, and credits the referral rewards to both parties.</p>

<p>Abuse prevention here overlaps with the general fraud detection system. The same device fingerprinting and multi-account detection logic that catches self-referral fraud also protects the referral program. Additional protections include a maximum number of referral bonuses per user per month and a minimum account age before a user can redeem referral rewards.</p>

<a name="Offer.Management.System"></a>
<h2>Offer Management System</h2>

<p>The offer catalog is the user-facing surface that drives engagement. Managing it at scale involves a combination of automated synchronization and manual curation.</p>

<p>Merchant sync jobs run on schedules appropriate to each affiliate network&rsquo;s update frequency. Some networks provide real-time feeds; others batch updates daily. The sync service pulls new offers, updates changed cashback rates, and marks expired offers as inactive. Offers that are no longer available on the affiliate network are deactivated rather than deleted, to preserve attribution history.</p>

<p>Promotional campaigns layer on top of the base offer catalog. A campaign might boost cashback rates for a specific merchant, apply to a category, or be targeted to a user segment based on purchase history. Campaigns have scheduling rules: start time, end time, and optionally a budget cap that deactivates the campaign when a certain total cashback has been awarded.</p>

<p>Offer prioritization is an interesting product and engineering problem. When a user searches for deals on electronics, the platform wants to surface the offers most likely to result in a purchase and earn CashKaro a commission. This combines several signals: cashback rate attractiveness, merchant brand strength, recent conversion rates, and personalization based on the user&rsquo;s browsing history. The ranking logic runs server-side at query time, using precomputed signals stored in Redis and Elasticsearch.</p>

<a name="Notification.Infrastructure"></a>
<h2>Notification Infrastructure</h2>

<p>Notifications are how CashKaro keeps users engaged and informed. There are several categories with different urgency and personalization requirements.</p>

<p>Transactional notifications are high-priority: cashback earned, cashback confirmed, payout initiated, payout completed. These are triggered by specific events and should reach the user promptly. They are published as events on Kafka and consumed by the notification service, which fans out to push notification, email, and SMS channels based on the user&rsquo;s preferences.</p>

<p>Promotional notifications are lower-priority and higher-volume: flash sale alerts, personalized offer recommendations, cashback boost announcements. These are scheduled in batches and need to respect quiet hours and per-user notification frequency caps to avoid annoying users. Sending too many promotional notifications is a fast path to app uninstalls.</p>

<p>The notification service uses a priority queue to separate transactional from promotional messages. Transactional messages get immediate processing. Promotional messages are batched and sent during appropriate windows based on the user&rsquo;s timezone and engagement patterns.</p>

<p>Delivery tracking matters for transactional notifications. If a user does not open a push notification about a cashback credit within a few hours, the system may follow up with an email. This escalation logic is state-based and requires the notification service to track delivery and open events per user.</p>

<a name="Event-Driven.Architecture.Deep.Dive"></a>
<h2>Event-Driven Architecture Deep Dive</h2>

<p>By now it is clear that events are the connective tissue of the entire platform. Let us be explicit about why this architecture was chosen and what it implies.</p>

<p>The key insight is that the cashback lifecycle spans multiple time scales. A click happens in milliseconds. Attribution happens in hours. Cashback confirmation happens in weeks. Payout processing happens in days. No single synchronous request can span these time scales. Event-driven architecture is not just a stylistic preference; it is the correct solution to the temporal structure of the domain.</p>

<div class="mermaid">
flowchart LR
    subgraph Producers
        A[Tracking Service]
        B[Attribution Engine]
        C[Cashback Engine]
        D[Wallet Service]
        E[Redemption Service]
    end

    subgraph Kafka
        F[click-events]
        G[attribution-events]
        H[cashback-events]
        I[wallet-events]
        J[payout-events]
    end

    subgraph Consumers
        K[Analytics Service]
        L[Fraud Detection]
        M[Notification Service]
        N[Referral Service]
        O[Reconciliation Service]
    end

    A &#8211;> F
    B &#8211;> G
    C &#8211;> H
    D &#8211;> I
    E &#8211;> J
    F &#8211;> K
    F &#8211;> L
    G &#8211;> K
    G &#8211;> L
    G &#8211;> N
    H &#8211;> M
    H &#8211;> K
    I &#8211;> M
    I &#8211;> O
    J &#8211;> M
    J &#8211;> O

    classDef producer fill:#27AE60,color:#fff
    classDef topic fill:#8E44AD,color:#fff
    classDef consumer fill:#4A90D9,color:#fff

    class A,B,C,D,E producer
    class F,G,H,I,J topic
    class K,L,M,N,O consumer
</div>


<p>Kafka is the standard choice for this event bus role. It provides durable, ordered, replayable event streams. The durability is critical: if the fraud detection service is temporarily down, it does not lose events. It resumes consuming from where it left off when it comes back online. This is the exactly-once or at-least-once delivery guarantee that makes the system reliable.</p>

<p>Consumer groups allow multiple services to independently consume the same events. The analytics service and the fraud detection service both consume click events but do entirely different things with them. They maintain separate offsets in Kafka and neither is aware of the other.</p>

<p>Event schema evolution is a practical concern in long-running systems. As the platform evolves, event payloads may need new fields. Using a schema registry like Confluent Schema Registry with Avro or Protobuf allows producers to add new fields without breaking consumers, as long as backward compatibility rules are followed.</p>

<p>Retries and dead letter queues are essential. If the notification service fails to send a push notification due to a transient error, the message should be retried with exponential backoff. After a configured number of retries, the message goes to a dead letter queue where it can be inspected and reprocessed manually. Without this, failed messages are silently dropped, which is unacceptable for transactional notifications.</p>

<a name="Database.Design"></a>
<h2>Database Design</h2>

<p>The data model underpins everything. Here are the key entities and their relationships.</p>

<p>The users table stores authentication credentials, profile data, device information, and KYC status. It is indexed on email, phone number, and referral code.</p>

<p>The merchants table stores merchant metadata, affiliate program details, cashback rate configurations, and integration type. It is read-heavy and aggressively cached.</p>

<p>The offers table stores specific cashback offers from merchants, including rate, category rules, exclusions, start and end dates, and campaign associations. Partitioned by status (active or expired) to keep query performance high as the table grows.</p>

<p>The clicks table stores every click event: user ID, offer ID, sub-ID generated, affiliate network, timestamp, and the tracking URL generated. Partitioned by timestamp because queries against this table are almost always time-bounded. Old partitions can be archived to cheap object storage.</p>

<p>The transactions table stores attribution records: order ID, network, user ID, offer ID at click time, order amount, commission amount, cashback amount, status, and timestamps for each state transition. The composite unique index on (order_id, network) enforces idempotent postback processing.</p>

<p>The wallet_ledger table is the most important financial table. Every credit and debit is a row: user ID, transaction type, amount, source type (cashback, referral, promotional), source transaction ID, balance after, and timestamp. This table is append-only. Balances are never updated; they are computed from the ledger.</p>

<p>The redemptions table tracks payout requests: user ID, amount, destination type (bank or gift card), destination details (encrypted), payout processor reference, status, and timestamps.</p>

<p>The referrals table tracks the referral graph: referrer user ID, referee user ID, status, referral code used, and the qualifying transaction ID that triggered fulfillment.</p>

<p>Indexing strategy is important at scale. The wallet_ledger table benefits from an index on (user_id, created_at DESC) for balance lookups. The transactions table needs an index on (user_id, status, created_at) for user-facing transaction history queries. The clicks table needs an index on (sub_id) for the attribution resolution lookup during postback processing.</p>

<a name="Caching.Systems"></a>
<h2>Caching Systems</h2>

<p>Caching is not optional in a platform at this scale; it is a core architectural component.</p>

<p>The offer catalog is the highest-traffic data. A user browsing the app might trigger dozens of offer lookups in a single session. Fetching each from PostgreSQL would be impossibly slow. The offer catalog is therefore maintained in Redis as a hash keyed by offer ID, with TTL-based expiration and explicit invalidation when a sync job updates offer data.</p>

<p>Merchant configurations are cached similarly. The affiliate URL generation code needs the merchant&rsquo;s network configuration on every click. This data changes infrequently (when CashKaro renegotiates terms with a merchant) but is read millions of times per day. An LRU cache in Redis with a generous TTL is the right tradeoff.</p>

<p>User wallet balances are another hot read path. Users check their balance frequently, especially during promotional events. Rather than aggregating the ledger on every read, a materialized balance is maintained in Redis and updated transactionally with every ledger write. The ledger is the source of truth; the Redis balance is a precomputed view. Periodic consistency checks verify that the materialized balances match the ledger aggregates.</p>

<p>Session data for the click tracking system is also cached. When a postback arrives and the attribution engine needs to look up the click record matching a sub-ID, this lookup should be fast. Sub-IDs that have not yet received a postback are kept in a fast lookup structure in Redis, and removed after the attribution window expires.</p>

<p>Cache invalidation is the hardest part. When a merchant&rsquo;s cashback rate changes, all cached offer data for that merchant needs to be invalidated. The sync service knows which offers changed and can issue targeted invalidations rather than flushing the entire cache. This targeted approach is more complex to implement but avoids the thundering herd problem, where flushing a large cache causes a sudden spike in database load as every request misses and rebuilds.</p>

<a name="Scalability.Deep.Dive"></a>
<h2>Scalability Deep Dive</h2>

<p>Cashback platforms have sharp traffic spikes during major shopping events. Diwali, Big Billion Day, Great Indian Festival, and End of Season sales produce traffic volumes that can be ten to twenty times normal levels. The system must handle these spikes without degrading the user experience or losing attribution events.</p>

<p>Click tracking is the first bottleneck. The tracking endpoint is hit on every single click, so it needs to scale horizontally. Stateless service design enables this: because the tracking endpoint only reads from cache and writes to Kafka (not to the database synchronously), any number of instances can run in parallel behind a load balancer without coordination. Kubernetes autoscaling based on CPU and request rate can spin up additional instances within minutes in response to a traffic spike.</p>

<p>Attribution processing is a batch concern during spikes. Affiliate networks do not usually send real-time postbacks for every single sale during a mega-sale event; they batch and delay. So the attribution engine may face a surge in postbacks hours after the sale peak, not during it. This is actually helpful because it smooths out the load. The Kafka consumer group for the postback handler can be scaled independently to process a large postback backlog quickly.</p>

<p>Wallet writes are the most consistency-sensitive bottleneck. Every cashback credit requires a ledger write that must be durable and correctly ordered. PostgreSQL with row-level locking handles concurrent writes to different user wallets without contention, because no two users share ledger rows. The main risk is write throughput. For very high volumes, the wallet service can use connection pooling (PgBouncer) to avoid connection exhaustion, and can batch ledger writes for the same user within a short window to reduce I/O.</p>

<p>Database read scaling uses read replicas. The transaction history, offer catalog, and user profile reads that dominate normal traffic can be served from replicas, leaving the primary database for writes. Read replicas introduce a small amount of replication lag, typically a few hundred milliseconds, which is acceptable for non-financial reads but not for balance checks before a redemption. Those must go to the primary.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Bottleneck</th>
        <th>Scaling Approach</th>
        <th>Tradeoff</th>
        <th>Priority</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Click tracking throughput</td>
        <td>Stateless horizontal scaling, async Kafka writes</td>
        <td>Potential for small click event loss on broker failure</td>
        <td>High</td>
      </tr>
      <tr>
        <td>Offer catalog reads</td>
        <td>Redis cache with targeted invalidation</td>
        <td>Cache staleness during high update frequency</td>
        <td>High</td>
      </tr>
      <tr>
        <td>Wallet ledger writes</td>
        <td>Connection pooling, user-sharded tables at extreme scale</td>
        <td>Sharding adds query complexity</td>
        <td>Medium</td>
      </tr>
      <tr>
        <td>Attribution postback processing</td>
        <td>Kafka consumer group scaling, idempotent processing</td>
        <td>Increased lag during postback surges</td>
        <td>Medium</td>
      </tr>
      <tr>
        <td>Fraud detection</td>
        <td>Async Kafka consumer, async rule evaluation</td>
        <td>Detection happens after the fact, not inline</td>
        <td>Medium</td>
      </tr>
      <tr>
        <td>Notification fanout</td>
        <td>Separate queues per channel, priority separation</td>
        <td>Eventual delivery, not real-time guarantee</td>
        <td>Low</td>
      </tr>
    </tbody>
  </table>
</div>


<a name="Reliability.and.Availability"></a>
<h2>Reliability and Availability</h2>

<p>A platform that handles users&#8217; money has a higher bar for reliability than a typical web application. A bug that results in incorrect cashback amounts or a service outage during a major sale directly impacts user trust and company revenue.</p>

<p>Each microservice should be designed with graceful degradation in mind. If the fraud detection service is down, clicks and purchases should still be processed, but payouts should be held for manual review rather than released automatically. The system should fail safe rather than fail hard.</p>

<p>Circuit breakers around affiliate network API calls are important. If an affiliate network&rsquo;s API is timing out, continuing to send requests will only make things worse. A circuit breaker detects the failure pattern and stops sending requests for a configurable period, returning cached data or a graceful error instead.</p>

<p>Merchant outages are handled at the offer level. If a direct merchant integration goes down, their offers can be temporarily hidden from the catalog rather than showing users links that will fail. The sync service monitors integration health and toggles offer visibility accordingly.</p>

<p>Monitoring and alerting should be organized around business metrics, not just technical metrics. CPU and memory usage are important, but what really matters to the business is click-to-attribution conversion rate, cashback confirmation rate, payout success rate, and error rates on each API endpoint. Alerts based on these business metrics catch problems that might not manifest as infrastructure alerts.</p>

<a name="Security.Considerations"></a>
<h2>Security Considerations</h2>

<p>The wallet is the most security-sensitive component. Unauthorized access to a user&rsquo;s account followed by a redemption to an attacker-controlled bank account is a real threat vector.</p>

<p>Account takeover prevention relies on anomaly detection around login patterns. A login from a new device or a new geographic location, followed by a redemption request, should trigger a step-up authentication challenge. New bank account details should require a cooling-off period before they can receive payouts, giving the legitimate account holder time to notice and respond.</p>

<p>All payout destination data (bank account numbers) must be stored encrypted, not in plaintext. Decryption keys should be stored in a secrets management system, not hardcoded in application code.</p>

<p>API endpoints that trigger financial operations should have additional authorization checks beyond session authentication. For example, a redemption endpoint might require re-entry of a PIN or OTP even for an authenticated user, as a second factor specific to financial transactions.</p>

<p>The postback handler endpoint needs to validate that postbacks are genuinely from the affiliate networks it is integrated with. Networks typically sign postbacks with a shared secret, and the handler should verify this signature before processing. An unsigned or incorrectly signed postback should be rejected, not processed.</p>

<a name="Engineering.Tradeoffs"></a>
<h2>Engineering Tradeoffs</h2>

<p>Real system design always involves making choices between competing concerns. These are the ones that come up most often in cashback platforms.</p>

<p>Attribution accuracy versus redirect latency is the central tension. More data captured at click time improves attribution reliability. But capturing more data adds latency to the redirect. Most platforms settle on capturing user ID, offer ID, and timestamp at click time, and defer anything more expensive to async processing.</p>

<p>Fraud prevention versus user experience is a constant negotiation. Aggressive fraud rules catch more fraud but also generate false positives that block legitimate users from receiving their cashback. The right balance depends on the fraud rate in the user base and the cost of a false positive in terms of user support load and churn. Starting with conservative rules and tightening them based on observed fraud patterns is better than starting strict and loosening after complaints.</p>

<p>Ledger consistency versus write throughput is a database design tradeoff. A strict serializable isolation level guarantees no concurrency anomalies in the ledger but reduces throughput. For a wallet ledger where correctness is paramount, accepting lower throughput in exchange for strong consistency is the right call. Throughput concerns can be addressed at the infrastructure level (faster hardware, read replicas, connection pooling) without compromising the consistency model.</p>

<p>Caching freshness versus cache simplicity is an ongoing operational concern. Serving slightly stale offer data to users is usually acceptable, especially when the TTL is short. But serving a stale cashback rate at click time, which then gets used for computation when the postback arrives, can result in paying out the wrong cashback amount. Snapshotting the rate at click time eliminates this risk at the cost of additional storage.</p>

<a name="Real-World.Technology.Stack"></a>
<h2>Real-World Technology Stack</h2>

<p>The technology choices in a platform like CashKaro are driven by the specific characteristics of each component.</p>

<p>Java and Spring Boot are common choices for core transactional services like the wallet service and redemption service. Java&rsquo;s mature ecosystem for database connection pooling, transaction management, and JVM-level performance tuning makes it well-suited for financial workloads. Spring Boot&rsquo;s opinionated structure makes it easy to build consistent, well-tested services.</p>

<p>Go is increasingly used for high-throughput, latency-sensitive services like the click tracking endpoint. Go&rsquo;s lightweight goroutine model allows handling a large number of concurrent HTTP requests with low memory overhead. The click tracking service needs to handle a burst of requests with minimal latency, and Go is excellent at this.</p>

<p>Python is used for data pipelines, reconciliation jobs, and machine learning model serving. The data science ecosystem in Python (pandas, scikit-learn, TensorFlow) makes it the natural choice for fraud detection model training and the analytics components that feed offer personalization.</p>

<p>Redis serves multiple roles: as a session cache, an offer catalog cache, a materialized wallet balance store, and a distributed lock manager for preventing duplicate processing. Its in-memory nature and atomic operations make it versatile for these different use cases.</p>

<p>Kafka is the event streaming backbone, as described throughout. Its durability, consumer group model, and partition-based parallelism make it the right tool for connecting services across different time scales.</p>

<p>PostgreSQL is the primary relational database for the wallet ledger, transactions, and user data. Its strong consistency guarantees, mature replication, and excellent support for serializable transactions make it appropriate for financial data.</p>

<p>Elasticsearch provides full-text search and analytics capabilities for the offer catalog. When a user searches for &ldquo;laptop cashback offers,&rdquo; this is served from Elasticsearch rather than a SQL database, because the ranking and relevance features that Elasticsearch provides are difficult to replicate in SQL.</p>

<p>Kubernetes orchestrates all of these services. Container-based deployment with autoscaling policies keyed to traffic metrics allows the platform to scale individual services independently, so the click tracking service can scale to fifty instances during a flash sale while the redemption service stays at three instances because payout volume is not spiking.</p>

<a name="System.Design.Interview.Perspective"></a>
<h2>System Design Interview Perspective</h2>

<p>When an interviewer asks you to design a cashback platform, they are testing several things simultaneously: your understanding of distributed systems, your ability to handle financial consistency requirements, your instinct for where the hard problems are, and your awareness of non-functional requirements like fraud prevention and scalability.</p>

<p>Weak answers start with the database schema and spend most of the time on CRUD operations. Strong answers start with the business domain and identify the core engineering challenges before diving into implementation.</p>

<p>The core challenges worth leading with are: click attribution across third-party systems and devices, the temporal gap between click and confirmation and how the system manages state across that gap, wallet consistency under concurrent operations, and fraud prevention without degrading legitimate user experience.</p>

<p>A common mistake is to model the cashback system as a simple e-commerce platform with a loyalty points add-on. The affiliate network dependency, the external attribution system, and the post-purchase validation period make it fundamentally different from a self-contained loyalty program. Recognizing and articulating these differences signals real domain understanding.</p>

<p>Another common mistake is to ignore the failure modes. An interviewer will probe: what happens if the affiliate network is down? What if the postback never arrives? What if the user returns the product after cashback is confirmed? Strong candidates have thought through these scenarios and can describe the system&rsquo;s behavior at each failure point.</p>

<p>When discussing the wallet, bring up the ledger model explicitly. Saying &ldquo;I would maintain a balance field and update it on every credit and debit&rdquo; signals a lack of experience with financial systems. Explaining the append-only ledger, the materialized balance view, and the consistency guarantees it provides signals that you have thought carefully about money at scale.</p>

<p>On scalability, avoid generic answers like &ldquo;add more servers.&rdquo; Instead, identify the specific bottlenecks (click tracking throughput, wallet write contention, postback processing lag) and explain the specific solutions for each. This demonstrates that you understand the system well enough to know where the load concentrates.</p>

<p>The best interviews on this topic become conversations about tradeoffs rather than recitations of architecture. Be prepared to defend your choices and acknowledge what you are trading away. Choosing eventual consistency for the offer catalog is defensible because the cost of serving a slightly stale cashback rate is low. Choosing eventual consistency for the wallet ledger is not defensible because the cost of incorrect balances is high. Articulating this distinction is what separates a strong system design answer from a mediocre one.</p>

<a name="Closing.Thoughts"></a>
<h2>Closing Thoughts</h2>

<p>Cashback platforms are a rich design space that touches almost every area of distributed systems: event streaming, financial ledgers, affiliate tracking, fraud detection, caching, and scalability. Spending time understanding how all of these fit together is time well spent for any engineer who wants to work in marketplace, fintech, or platform engineering.</p>
]]></content>
    </entry>
    
    <entry>
        <title type="html"><![CDATA[How MakeMyTrip Travel Planner Works?]]></title>
        <link href="https://rishijeet.github.io/blog/how-makemytrip-travel-planner-works/"/>
        <updated>2026-05-31T18:53:45+05:30</updated>
        <id>https://rishijeet.github.io/blog/how-makemytrip-travel-planner-works</id>
        <content type="html"><![CDATA[<blockquote><p><em>Most engineers, when they think about travel technology, imagine a flight search engine. You pick a source, a destination, a date, and the system returns a list of flights sorted by price. That is a solved problem. The hard part is what comes after.</em></p></blockquote>

<p><img src="/images/system_design/makemytrip-banner.avif" height="300" width="900" alt="Alt text" /></p>

<p>A vacation planning platform is an entirely different beast. When a user opens MakeMyTrip and says &ldquo;plan me a 7-day trip to Rajasthan,&rdquo; the system has to figure out which cities to include, which attractions to prioritize, how to sequence the days so a traveler is not criss-crossing the map inefficiently, when flights are cheapest, which hotels have availability near the right areas, what local tours are available on which days, how to get from the airport to the hotel, how to price all of this as a coherent bundle, and then orchestrate bookings across a dozen different vendors without any single failure unraveling the whole thing.</p>

<p>That is not a search problem. That is a large-scale optimization problem sitting on top of a distributed marketplace, layered with personalization, real-time inventory, and multi-vendor payment reconciliation.</p>

<p>The evolution from &ldquo;book a flight&rdquo; to &ldquo;plan my vacation&rdquo; happened because travelers stopped wanting to assemble trips themselves. Researching destinations takes hours. Cross-referencing hotel availability with flight times is tedious. Figuring out which Jaisalmer fort tours run on which days requires visiting five different websites. A modern travel planner absorbs all of that complexity and surfaces a coherent, personalized plan.</p>

<!--more-->


<p>This blog is a deep technical walkthrough of how that works, starting from how destinations get discovered and ranked, through how itineraries are algorithmically generated, all the way to how a payment across three different vendors gets reconciled without leaving a user in a half-booked state.</p>

<a name="Core.Features.of.the.Platform"></a>
<h2>Core Features of the Platform</h2>

<p>Before diving into architecture, it helps to be precise about what the platform actually does. These are not abstract features; each one translates into a distinct engineering subsystem.</p>

<p>Destination discovery is the entry point. A user might know they want a beach vacation or a hill station or a heritage city. The platform has to map vague intent to specific, rankable destinations and surface the right one for that traveler&rsquo;s history, budget, and time of year.</p>

<p>Vacation package creation is bundling. Flight plus hotel plus transfers plus activities into a single priced unit. This sounds simple but involves solving a multi-dimensional optimization problem across live inventory.</p>

<p>AI itinerary generation is the core differentiator. Given a destination, a duration, a budget, and a set of preferences, generate a day-by-day plan that is geographically sensible, temporally realistic, and personally relevant.</p>

<p>Flight and hotel aggregation pull from dozens of suppliers and present unified search results. The hard part is latency, consistency, and handling the inevitable discrepancy between what an API says is available and what is actually bookable.</p>

<p>Activities and local transportation are the long tail. Thousands of individual tour operators and cab vendors need to be connected to a single platform with unified inventory and real-time availability.</p>

<p>Budget optimization threads pricing constraints through every layer of the system, from destination ranking to hotel tier selection to activity choices.</p>

<p>Trip collaboration lets multiple travelers contribute to and agree on a shared itinerary, which introduces concurrency and conflict resolution challenges.</p>

<a name="High-Level.Architecture"></a>
<h2>High-Level Architecture</h2>

<p>The platform is built around a set of specialized services, each owning a discrete domain. These services communicate synchronously for user-facing flows and asynchronously via an event bus for downstream processing.</p>

<div class="mermaid">
flowchart LR

%% ==================================================
%% CLIENTS
%% ==================================================

subgraph Clients[&#8220;CLIENT APPLICATIONS&#8221;]
    MOB[&#8220;Mobile App&#8221;]
    WEB[&#8220;Web Application&#8221;]
end

%% ==================================================
%% ACCESS LAYER
%% ==================================================

subgraph Access[&#8220;ACCESS & SECURITY&#8221;]
    GW[&#8220;API Gateway&#8221;]
    AUTH[&#8220;Authentication Service&#8221;]
end

%% ==================================================
%% DISCOVERY & PLANNING
%% ==================================================

subgraph Planning[&#8220;DISCOVERY & TRIP PLANNING&#8221;]
    DEST[&#8220;Destination Service&#8221;]
    ITIN[&#8220;Itinerary Engine&#8221;]
    REC[&#8220;Recommendation Engine&#8221;]
    PKG[&#8220;Package Builder&#8221;]
end

%% ==================================================
%% INVENTORY NETWORK
%% ==================================================

subgraph Inventory[&#8220;INVENTORY NETWORK&#8221;]
    FLIGHT[&#8220;Flight Aggregator&#8221;]
    HOTEL[&#8220;Hotel Aggregator&#8221;]
    ACT[&#8220;Activity Service&#8221;]
    TRANS[&#8220;Transportation Service&#8221;]
end

%% ==================================================
%% BOOKING PLATFORM
%% ==================================================

subgraph Booking[&#8220;BOOKING PLATFORM&#8221;]
    BOOK[&#8220;Booking Orchestrator&#8221;]
    PRICE[&#8220;Dynamic Pricing&#8221;]
    PAY[&#8220;Payment Service&#8221;]
end

%% ==================================================
%% EVENT ECOSYSTEM
%% ==================================================

subgraph Events[&#8220;EVENT ECOSYSTEM&#8221;]
    KAFKA[&#8220;Kafka Event Bus&#8221;]
    NOTIF[&#8220;Notification Service&#8221;]
    ANALYTICS[&#8220;Analytics Platform&#8221;]
end

%% ==================================================
%% DATA PLATFORM
%% ==================================================

subgraph Data[&#8220;DATA PLATFORM&#8221;]
    REDIS[&#8220;Redis Cache&#8221;]
    ES[&#8220;Elasticsearch&#8221;]
    PG[&#8220;PostgreSQL&#8221;]
    S3[&#8220;Object Storage&#8221;]
end

%% ==================================================
%% USER JOURNEY
%% ==================================================

MOB &#8211;> GW
WEB &#8211;> GW

GW &#8211;> AUTH
GW &#8211;> DEST
GW &#8211;> ITIN
GW &#8211;> REC
GW &#8211;> BOOK

%% Planning Flow
ITIN &#8211;> DEST
ITIN &#8211;> REC

DEST &#8211;> ES
REC &#8211;> REDIS

%% Package Assembly
PKG &#8211;> FLIGHT
PKG &#8211;> HOTEL
PKG &#8211;> ACT
PKG &#8211;> TRANS

%% Booking Journey
BOOK &#8211;> PRICE
BOOK &#8211;> PAY
BOOK &#8211;> PG

%% Events
BOOK &#8211;>|&#8221;Booking Events&#8221;| KAFKA

KAFKA &#8211;> NOTIF
KAFKA &#8211;> ANALYTICS

%% ==================================================
%% NODE STYLING
%% ==================================================

%% Clients
style MOB fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff
style WEB fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff

%% Access Layer
style GW fill:#0891b2,stroke:#0e7490,stroke-width:5px,color:#ffffff
style AUTH fill:#0891b2,stroke:#0e7490,stroke-width:4px,color:#ffffff

%% Planning Layer
style DEST fill:#16a34a,stroke:#166534,stroke-width:4px,color:#ffffff
style ITIN fill:#22c55e,stroke:#15803d,stroke-width:5px,color:#ffffff
style REC fill:#84cc16,stroke:#4d7c0f,stroke-width:5px,color:#ffffff
style PKG fill:#65a30d,stroke:#3f6212,stroke-width:4px,color:#ffffff

%% Inventory
style FLIGHT fill:#ec4899,stroke:#be185d,stroke-width:4px,color:#ffffff
style HOTEL fill:#ec4899,stroke:#be185d,stroke-width:4px,color:#ffffff
style ACT fill:#ec4899,stroke:#be185d,stroke-width:4px,color:#ffffff
style TRANS fill:#ec4899,stroke:#be185d,stroke-width:4px,color:#ffffff

%% Booking
style BOOK fill:#f97316,stroke:#c2410c,stroke-width:6px,color:#ffffff
style PRICE fill:#fb923c,stroke:#c2410c,stroke-width:4px,color:#ffffff
style PAY fill:#fb923c,stroke:#c2410c,stroke-width:4px,color:#ffffff

%% Events
style KAFKA fill:#dc2626,stroke:#991b1b,stroke-width:6px,color:#ffffff
style NOTIF fill:#ef4444,stroke:#b91c1c,stroke-width:4px,color:#ffffff
style ANALYTICS fill:#ef4444,stroke:#b91c1c,stroke-width:4px,color:#ffffff

%% Data
style REDIS fill:#9333ea,stroke:#6b21a8,stroke-width:4px,color:#ffffff
style ES fill:#8b5cf6,stroke:#6d28d9,stroke-width:4px,color:#ffffff
style PG fill:#7c3aed,stroke:#5b21b6,stroke-width:5px,color:#ffffff
style S3 fill:#a855f7,stroke:#7e22ce,stroke-width:4px,color:#ffffff

%% ==================================================
%% LINK STYLING
%% ==================================================

linkStyle 0 stroke:#2563eb,stroke-width:3px
linkStyle 1 stroke:#2563eb,stroke-width:3px

linkStyle 2 stroke:#0891b2,stroke-width:4px
linkStyle 3 stroke:#16a34a,stroke-width:4px
linkStyle 4 stroke:#16a34a,stroke-width:4px
linkStyle 5 stroke:#16a34a,stroke-width:4px
linkStyle 6 stroke:#f97316,stroke-width:5px

linkStyle 7 stroke:#22c55e,stroke-width:4px
linkStyle 8 stroke:#22c55e,stroke-width:4px

linkStyle 9 stroke:#9333ea,stroke-width:3px
linkStyle 10 stroke:#9333ea,stroke-width:3px

linkStyle 11 stroke:#ec4899,stroke-width:3px
linkStyle 12 stroke:#ec4899,stroke-width:3px
linkStyle 13 stroke:#ec4899,stroke-width:3px
linkStyle 14 stroke:#ec4899,stroke-width:3px

linkStyle 15 stroke:#f97316,stroke-width:5px
linkStyle 16 stroke:#f97316,stroke-width:5px
linkStyle 17 stroke:#f97316,stroke-width:5px

linkStyle 18 stroke:#dc2626,stroke-width:6px
linkStyle 19 stroke:#ef4444,stroke-width:4px
linkStyle 20 stroke:#ef4444,stroke-width:4px

</div>


<p>The API gateway handles authentication, rate limiting, and routing. Every inbound request is authenticated before reaching a downstream service. Rate limiting happens at the gateway level so no single user can saturate an expensive external API call.</p>

<p>The planning layer is the intelligence of the system. These services do not own bookings; they produce recommendations, itineraries, and packages. The booking layer is where money changes hands, and it is intentionally separate to keep the transactional surface area isolated.</p>

<p>The event bus connects everything that does not need to happen synchronously. When a booking is confirmed, an event propagates to the notification service, the analytics platform, the loyalty system, and the supplier confirmation handlers. None of these need to block the user-facing booking response.</p>

<a name="Destination.Discovery.Engine"></a>
<h2>Destination Discovery Engine</h2>

<p>A destination catalog is deceptively complex. At the surface it looks like a database of cities with descriptions and photos. In practice it is a multi-dimensional index of places annotated with travel metadata, seasonality curves, activity categories, average price tiers, travel time from major origin cities, visa requirements, weather patterns, and popularity signals.</p>

<p>The catalog is stored in Elasticsearch because destination search needs full-text matching combined with faceted filtering and geographic proximity queries. A user searching &ldquo;beach destination under 30,000 for 5 days from Mumbai in December&rdquo; is issuing a query that combines free text, budget constraints, duration constraints, geographic proximity, and seasonal availability simultaneously. A relational database cannot serve this efficiently at scale.</p>

<p>Each destination has a popularity score that is computed offline and refreshed periodically. The score is a weighted composite of booking volume over the trailing 90 days, search volume, social signal proxies, and editorial curation. The weighting shifts by season. Goa&rsquo;s score in December is significantly higher than in July. Manali&rsquo;s score in January is high for winter travelers but low for beach seekers.</p>

<p>Destination ranking for a specific query combines the base popularity score with a personalization layer. A user who has previously booked heritage city trips will see Jaipur ranked higher than someone whose history shows beach destinations. The personalization multiplier is computed by the recommendation engine and applied at query time against the Elasticsearch base ranking.</p>

<p>The separation between base ranking and personalization multiplier is an intentional architectural choice. Base ranking is computed in batch and cached. Personalization multipliers are computed per-user but can also be cached based on user segment. This means a destination search can return results in under 200 milliseconds even with personalization applied.</p>

<p>Seasonality modeling deserves its own discussion. Each destination has a seasonality curve modeled as a time series of demand indices across the calendar year. This curve is used in two places: to surface appropriate destinations in discovery (you do not want to recommend Spiti Valley in January to a casual traveler) and to adjust pricing in the dynamic pricing engine. The curve is learned from historical booking data and manually adjusted by destination managers for edge cases like major events.</p>

<a name="AI-Powered.Recommendation.System"></a>
<h2>AI-Powered Recommendation System</h2>

<p>Recommendation is the system that makes the platform feel personalized. The challenge is that travel recommendations are harder than movie recommendations for a fundamental reason: frequency. A user might book travel twice a year. This means you have very few direct signals about their preferences, and those signals are separated by months.</p>

<p>The recommendation engine layers multiple signal sources to compensate for sparse direct data.</p>

<p>Collaborative filtering identifies users with similar travel profiles and recommends destinations those users enjoyed. If users who booked Rishikesh also frequently book Mcleod Ganj and Bir Billing, the system learns that this cluster of adventure and spiritual destinations belongs together. A new user who books Rishikesh gets recommendations for the others.</p>

<p>Content-based filtering operates on destination attributes. If a user has shown interest in hill stations with trekking and local markets, the system scores unvisited destinations by how closely they match those attribute vectors.</p>

<p>Behavioral signals are richer than explicit bookings. Search queries, destination page dwell time, itinerary views, saved trips, and price alert creation all carry signal. A user who spent 15 minutes reading about Ladakh but did not book is a strong signal of intent that the system should capture.</p>

<p>The cold start problem is real and common. A new user has no history. The platform handles this through onboarding preference collection (asking about trip style, budget range, travel companions, interests) and by defaulting to demographically segmented recommendations based on origin city, age range, and device type. A new user from Bengaluru in their late twenties gets different defaults than one from Kolkata in their fifties.</p>

<div class="mermaid">
flowchart LR

%% =========================
%% USER SIGNALS
%% =========================
subgraph Signals[&#8220;USER SIGNALS&#8221;]
    BS[&#8220;Booking History&#8221;]
    SS[&#8220;Search History&#8221;]
    BH[&#8220;Browse Behavior&#8221;]
    OP[&#8220;Onboarding Preferences&#8221;]
end

%% =========================
%% RECOMMENDATION MODELS
%% =========================
subgraph Models[&#8220;RECOMMENDATION MODELS&#8221;]
    CF[&#8220;Collaborative Filtering&#8221;]
    CB[&#8220;Content-Based Filtering&#8221;]
    SEQ[&#8220;Sequential Behavior Model&#8221;]
    POP[&#8220;Popularity Baseline&#8221;]
end

%% =========================
%% RANKING PIPELINE
%% =========================
subgraph Ranking[&#8220;RANKING PIPELINE&#8221;]
    MR[&#8220;Model Ranker&#8221;]
    PR[&#8220;Personalization Re-Ranker&#8221;]
    BR[&#8220;Business Rules Filter&#8221;]
end

%% =========================
%% RESULTS
%% =========================
subgraph Results[&#8220;RECOMMENDATION OUTPUT&#8221;]
    DR[&#8220;Destination Results&#8221;]
    HR[&#8220;Hotel Results&#8221;]
    AR[&#8220;Activity Results&#8221;]
end

%% =========================
%% SIGNAL FLOW
%% =========================
BS &#8211;> CF
SS &#8211;> SEQ
BH &#8211;> CB
OP &#8211;> POP

%% =========================
%% MODEL ENSEMBLE
%% =========================
CF &#8211;> MR
CB &#8211;> MR
SEQ &#8211;> MR
POP &#8211;> MR

%% =========================
%% RANKING FLOW
%% =========================
MR &#8211;> PR
PR &#8211;> BR

%% =========================
%% OUTPUTS
%% =========================
BR &#8211;> DR
BR &#8211;> HR
BR &#8211;> AR

%% =========================
%% STYLING
%% =========================

%% Signals
style BS fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff
style SS fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff
style BH fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff
style OP fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff

%% Models
style CF fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff
style CB fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff
style SEQ fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff
style POP fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff

%% Ranking
style MR fill:#ec4899,stroke:#be185d,stroke-width:5px,color:#ffffff
style PR fill:#ec4899,stroke:#be185d,stroke-width:5px,color:#ffffff
style BR fill:#ec4899,stroke:#be185d,stroke-width:5px,color:#ffffff

%% Output
style DR fill:#16a34a,stroke:#166534,stroke-width:4px,color:#ffffff
style HR fill:#16a34a,stroke:#166534,stroke-width:4px,color:#ffffff
style AR fill:#16a34a,stroke:#166534,stroke-width:4px,color:#ffffff

%% =========================
%% LINK STYLING
%% =========================

linkStyle 0 stroke:#2563eb,stroke-width:3px
linkStyle 1 stroke:#2563eb,stroke-width:3px
linkStyle 2 stroke:#2563eb,stroke-width:3px
linkStyle 3 stroke:#2563eb,stroke-width:3px

linkStyle 4 stroke:#7c3aed,stroke-width:4px
linkStyle 5 stroke:#7c3aed,stroke-width:4px
linkStyle 6 stroke:#7c3aed,stroke-width:4px
linkStyle 7 stroke:#7c3aed,stroke-width:4px

linkStyle 8 stroke:#ec4899,stroke-width:5px
linkStyle 9 stroke:#ec4899,stroke-width:5px

linkStyle 10 stroke:#16a34a,stroke-width:4px
linkStyle 11 stroke:#16a34a,stroke-width:4px
linkStyle 12 stroke:#16a34a,stroke-width:4px

</div>


<p>The ranking layer sits above the model outputs. Raw model scores are combined using a learned ensemble weight and then passed through a re-ranker that applies business rules. Business rules include things like &ldquo;do not recommend a destination for which the user has already completed a trip in the last 12 months&rdquo; or &ldquo;boost destinations with promotional pricing active.&rdquo; These rules are kept separate from the model to allow rapid iteration without retraining.</p>

<p>Recommendation quality is measured through a combination of offline metrics (precision, recall, NDCG on held-out booking data) and online A/B metrics (click-through rate on recommended destinations, conversion to booked trips, and post-trip satisfaction scores). The online metrics lag by weeks or months because the conversion cycle is slow. This makes recommendation system iteration in travel harder than in e-commerce.</p>

<a name="Itinerary.Generation.Engine"></a>
<h2>Itinerary Generation Engine</h2>

<p>This is where the hardest engineering happens. Generating a coherent, realistic, enjoyable itinerary for an unfamiliar city is a planning problem, not just a retrieval problem.</p>

<p>The input is a destination (or set of destinations), a duration, a set of traveler preferences, and a budget constraint. The output is a day-by-day schedule with specific attractions, meals, transit suggestions, and approximate timings.</p>

<p>The first step is attraction selection. The platform maintains a catalog of points of interest for every supported destination, annotated with category (heritage, adventure, nature, food, shopping), average visit duration, opening hours, best time of day to visit, price, distance from common hotel zones, and quality scores derived from reviews.</p>

<p>Given a 5-day Rajasthan trip covering Jaipur and Jodhpur, the system needs to select from hundreds of candidate attractions and decide which ones to include. This is a variant of the orienteering problem in combinatorial optimization. You have a set of locations with values and time costs, a time budget, and you want to maximize total value while respecting the budget. Solving this exactly for hundreds of candidates is computationally intractable. The platform uses a greedy construction heuristic followed by a local search refinement.</p>

<p>The greedy phase clusters attractions by geographic proximity. Jaipur&rsquo;s Amber Fort, Jaigarh Fort, and Nahargarh Fort are in the same northern cluster. City Palace, Hawa Mahal, and Jantar Mantar are in the central cluster. The system assigns clusters to days to minimize travel time between attractions on the same day.</p>

<p>The local search phase improves the initial solution by trying swaps: replace one attraction with an unselected one and keep the swap if it improves the overall score. The score function incorporates attraction quality, traveler preference match, variety (not three temples in a row), and schedule feasibility (accounting for opening hours and realistic transit times between attractions).</p>

<p>Transit time calculation deserves explicit attention. Many itinerary systems use straight-line distance as a proxy for travel time, which produces wildly unrealistic schedules in dense cities. A production system calls a routing API at itinerary generation time to get real road travel estimates between consecutive attractions, incorporating typical traffic conditions for the time of day.</p>

<div class="mermaid">
graph TD
    INPUT[User Inputs: Destination, Duration, Budget, Preferences]
    AC[Attraction Catalog Query]
    GC[Geographic Clustering]
    GP[Greedy Plan Construction]
    TT[Transit Time Calculation]
    LS[Local Search Optimization]
    OB[Opening Hours Validation]
    PREF[Preference Score Adjustment]
    OUT[Final Itinerary]

    INPUT &#8211;> AC
    AC &#8211;> GC
    GC &#8211;> GP
    GP &#8211;> TT
    TT &#8211;> LS
    LS &#8211;> OB
    OB &#8211;> PREF
    PREF &#8211;> OUT

    classDef input fill:#e94560,stroke:#fff,color:#fff
    classDef process fill:#0f3460,stroke:#e94560,color:#fff
    classDef output fill:#533483,stroke:#e94560,color:#fff

    class INPUT input
    class AC,GC,GP,TT,LS,OB,PREF process
    class OUT output
</div>


<p>A practical example helps. A user wants 5 days in Kerala. The system identifies candidate destinations: Kochi, Munnar, Alleppey, Thekkady. It calculates road distances between them and realizes that Munnar and Thekkady are a 3-hour drive apart, while Kochi to Alleppey is 90 minutes. It constructs a routing sequence: Kochi on arrival day, Alleppey for two nights (houseboat), Thekkady for a day, Munnar for two nights, departure from Kochi. Attractions within each stop are then clustered by area and assigned to half-days.</p>

<p>The system does not just stack attractions. It models realistic tourist energy. A heritage walk in the morning can be followed by a lighter museum visit or a food tour in the afternoon. Physically demanding treks are not placed back-to-back unless the user profile indicates high activity preference. Rest time is built into the schedule.</p>

<p>Itinerary generation runs in under two seconds for a five-day trip. The constraint solving is done in memory using pre-fetched attraction data cached in Redis. The routing API calls are the latency bottleneck; the system batches them and runs them in parallel.</p>

<a name="Vacation.Package.Builder"></a>
<h2>Vacation Package Builder</h2>

<p>Dynamic packaging is the process of assembling flight plus hotel plus activities into a priced bundle at the time of a user&rsquo;s request, rather than pre-defining fixed packages. This gives users flexibility while still allowing the platform to offer bundled pricing that is cheaper than booking each component separately.</p>

<p>The package builder takes the output of the itinerary engine (a list of nights at each destination, activities, and transit legs) and queries the flight aggregator, hotel aggregator, and activity service simultaneously to find available inventory for each component.</p>

<p>The pricing challenge is that each component has its own pricing model. Flights are priced dynamically and change by the hour. Hotels have base rates that change by night with minimum stay rules. Activities have fixed prices but limited availability. The bundle price has to remain coherent while reflecting real inventory costs.</p>

<p>The platform computes a bundle discount by negotiating preferred rates with suppliers for bundled bookings. An airline might offer a 3% reduction on fares when sold as part of a hotel plus flight package because it guarantees seat fill. This preferred rate is encoded as a pricing rule applied at bundle construction time.</p>

<p>Bundle profitability is tracked per package type. Some combinations are consistently high-margin (flight plus hotel for popular routes where the platform has preferred hotel rates). Others are low-margin but high in conversion (budget packages to high-demand destinations). The pricing engine uses this data to set minimum bundle margins dynamically.</p>

<a name="Flight.Aggregation.System"></a>
<h2>Flight Aggregation System</h2>

<p>Flight search is a high-latency, high-volume operation. A single user search might fan out to a dozen airline APIs and three or four GDS (Global Distribution System) connections simultaneously.</p>

<p>The aggregation service sends parallel requests to each supplier with a timeout. The timeout is typically set at 4-5 seconds. Suppliers that respond within the timeout contribute their results to the merged set. Suppliers that time out are excluded from that search. The user sees a &ldquo;searching more results&rdquo; indicator while the parallel fetches complete.</p>

<p>Fare caching is critical. Airline APIs are rate-limited and charge per search in some configurations. Caching search results for short periods (typically 10-15 minutes) dramatically reduces API call volume. The cache key is a combination of origin, destination, date, passenger count, and cabin class. Cache hits serve results instantly; misses trigger a live search.</p>

<p>The staleness risk is real. A fare cached 12 minutes ago might no longer be bookable. The platform handles this through a pre-booking availability check. Before showing the user a &ldquo;confirm booking&rdquo; screen, the system re-checks availability for the selected fare in real time. If the fare has changed or disappeared, the user sees the updated options.</p>

<p>Price watch is a downstream feature built on top of the aggregation layer. When a user sets a fare alert, the system schedules periodic re-queries for that route and date combination, caching results and diffing against the previous query to detect price changes. These scheduled queries are distributed across time to avoid thundering herd effects on supplier APIs.</p>

<a name="Hotel.Aggregation.System"></a>
<h2>Hotel Aggregation System</h2>

<p>Hotel inventory is even more complex than flights because the supply side is fragmented across property management systems, channel managers, and OTA connections.</p>

<p>The hotel aggregator pulls inventory from OTA partners (who aggregate properties themselves), direct hotel API connections, and wholesale supplier feeds. Each source has different data models, availability refresh rates, and pricing structures.</p>

<p>Inventory synchronization is the chronic challenge. A hotel might sell the last available room directly through its front desk at 3pm. The channel manager updates the OTA within minutes, but the OTA feed to MakeMyTrip might only refresh every 15 minutes. During that window, the platform might show availability that no longer exists.</p>

<p>The platform handles this through a two-stage commitment model. When a user selects a hotel, the system holds an inventory reservation with the supplier for a short period (typically 15-30 minutes). The hold does not confirm the booking; it reserves the room while the user completes payment. If payment succeeds, the hold converts to a confirmed booking. If the hold cannot be established because inventory is gone, the user is informed before reaching the payment screen.</p>

<p>Rate parity is a contractual and technical challenge. Hotels contract with different OTAs and GDSs at different rates, often with rate parity clauses requiring the same price across channels. Enforcing this requires the aggregator to compare rates across sources and flag anomalies. The platform does this comparison periodically in a batch job and surfaces rate parity violations for supplier management review.</p>

<a name="Activities.and.Experiences.Platform"></a>
<h2>Activities and Experiences Platform</h2>

<p>The activity marketplace is the most fragmented part of the inventory stack. Activities are provided by thousands of small tour operators, individual guides, and local experience providers. These operators have wildly different technical sophistication, from API-connected operators with real-time availability to operators who still take bookings by phone.</p>

<p>The platform tiered its supplier integrations accordingly. Tier-one suppliers (large tour operators, museum ticketing systems, theme parks) have direct API integrations with real-time availability. Tier-two suppliers use a booking platform that MakeMyTrip integrates with. Tier-three suppliers are managed through a manual allocation model where MakeMyTrip pre-purchases blocks of capacity.</p>

<p>The pre-purchase model for tier-three suppliers introduces inventory risk. The platform pays for a block of slots upfront and needs to sell them before the activity date. Dynamic pricing on these pre-purchased blocks allows the platform to increase margins when demand is strong and discount aggressively as the date approaches to clear remaining inventory.</p>

<p>Activity availability is time-bound differently from hotels. A hotel room is available until midnight. A morning jeep safari has a specific departure time and a fixed group size. The data model for activities includes time slots, group size limits, language of delivery, physical requirements, and cancellation windows. All of this needs to be reflected accurately in the user-facing search and booking flow.</p>

<a name="Local.Transportation.Planning"></a>
<h2>Local Transportation Planning</h2>

<p>Transportation planning connects the dots between bookings. A user lands at Cochin International Airport at 6pm. Their hotel is in Fort Kochi. The itinerary engine schedules a houseboat experience in Alleppey the next day. There are three legs here that need to be surfaced: airport to hotel, hotel to Alleppey jetty, and return to Kochi for the final day.</p>

<p>The transportation service maintains a catalog of standard routes between airports, major hotels, and popular destinations, with associated transfer options (prepaid cabs, shared shuttles, app-based cabs, trains, buses). For each route, the service stores estimated duration, cost range, booking lead time requirements, and reliability score.</p>

<p>Transfer booking is integrated into the itinerary at the time of package construction. The system pre-fills suggested transfers based on the hotel and activity locations. Users can accept the defaults or modify them. For routes where the platform has vendor relationships, the transfer is bookable directly. For others, the platform surfaces deep links to cab apps or train booking systems.</p>

<p>Route optimization within a destination uses a simplified TSP (traveling salesman problem) approach. Given a set of attractions and a hotel, the system finds a visiting sequence that minimizes total travel time. For small sets (under 10 locations), exact solutions are feasible. For larger sets, a 2-opt heuristic produces near-optimal results quickly.</p>

<a name="Dynamic.Pricing.Engine"></a>
<h2>Dynamic Pricing Engine</h2>

<p>Pricing in a travel platform is not just displaying supplier rates. The platform computes a final package price that covers component costs, adds a margin, applies any applicable discounts, and presents a coherent total.</p>

<p>Component pricing starts with supplier rates. For flights, the fare class determines the base price. For hotels, the room rate is per night. For activities, it is per person. These are the floor costs.</p>

<p>The platform&rsquo;s margin is applied as a percentage over the cost, calibrated to competitive rates in the market. The pricing engine checks real-time competitor pricing for similar packages and adjusts the margin to stay competitive. This requires the system to be continuously monitoring competitor prices, which is done through a combination of public scrapers and data partnerships.</p>

<p>Demand forecasting drives seasonal pricing adjustments. The pricing engine trains a demand model on historical booking data, incorporating day of week, weeks before travel, origin-destination pair, and event calendar. A package for Goa in the week before Christmas will be priced significantly higher than the same package for late January because the demand model predicts higher conversion even at elevated prices.</p>

<p>Promotional pricing introduces complexity. A discount applied to a bundle requires correctly computing the post-discount price while ensuring the platform still covers its costs. The pricing engine maintains a margin floor below which a promotion cannot push the price. If a user applies a coupon code that would push the package below the margin floor, the coupon is accepted but the applied discount is capped.</p>

<p>Total trip price calculation is the final aggregation. The pricing engine sums flight costs for all passengers, hotel costs for all nights, activity costs per person, transfer costs, and platform service fees, then applies any bundling discounts and promotional codes. The output is a fully itemized price breakdown that the user sees before confirming.</p>

<a name="Booking.Orchestration.Engine"></a>
<h2>Booking Orchestration Engine</h2>

<p>This is the most consequential engineering in the entire platform. A vacation package booking is a distributed transaction across multiple external systems, each of which can fail independently.</p>

<div class="mermaid">
graph TD
    START[User Confirms Package]
    IV[Inventory Validation]
    PR[Price Recalculation]
    LK[Inventory Lock]
    PI[Payment Initiation]
    PC[Payment Confirmation]
    FB[Flight Booking API]
    HB[Hotel Booking API]
    AB[Activity Booking API]
    BK[Booking Record Created]
    CN[Confirmation Sent]
    RL[Rollback if Failure]

    START &#8211;> IV
    IV &#8211;> PR
    PR &#8211;> LK
    LK &#8211;> PI
    PI &#8211;> PC
    PC &#8211;> FB
    PC &#8211;> HB
    PC &#8211;> AB
    FB &#8211;> BK
    HB &#8211;> BK
    AB &#8211;> BK
    BK &#8211;> CN
    IV &#8211;>|Failed| RL
    LK &#8211;>|Failed| RL
    PC &#8211;>|Failed| RL
    FB &#8211;>|Failed| RL
    HB &#8211;>|Failed| RL

    classDef start fill:#e94560,stroke:#fff,color:#fff
    classDef process fill:#0f3460,stroke:#e94560,color:#fff
    classDef booking fill:#533483,stroke:#e94560,color:#fff
    classDef failure fill:#2b2d42,stroke:#ef233c,color:#fff

    class START start
    class IV,PR,LK,PI,PC process
    class FB,HB,AB,BK,CN booking
    class RL failure
</div>


<p>The orchestrator implements a saga pattern. Each booking step is recorded as an event. If a step fails, compensating actions are triggered to undo completed steps.</p>

<p>Inventory validation runs first. The system re-checks availability for all selected components. If any component is no longer available, the user is redirected to select alternatives. This step is the last chance to catch stale inventory before money changes hands.</p>

<p>Price recalculation runs immediately before payment. Flight fares can change in the time between initial search and booking. If the total price has changed, the user is shown the updated price and asked to confirm again.</p>

<p>Inventory locking attempts to hold all components simultaneously. Holds with supplier systems reserve inventory but do not confirm. This is time-limited, typically 15-20 minutes, enough to complete payment.</p>

<p>Payment processing is attempted next. If payment fails (card declined, bank timeout), the holds are released and the booking fails cleanly. The user sees the payment failure and can retry.</p>

<p>If payment succeeds, the orchestrator fans out confirmation requests to each supplier: the flight booking API, the hotel booking API, and the activity booking API. These can be sent in parallel. Each confirmation generates a supplier booking reference (PNR for flights, booking ID for hotels).</p>

<p>The partial confirmation failure case is the hardest. What if flight booking succeeds but hotel booking fails? The platform has collected payment for a complete package. The correct behavior is to attempt to book an alternative hotel first. If no alternative is available, the platform cancels the flight (triggering a refund request to the airline) and initiates a full refund to the user. All of this is handled by the orchestrator&rsquo;s rollback logic, and it must be idempotent: if the rollback itself crashes halfway through, re-running it must not double-cancel or double-refund.</p>

<a name="Payment.Infrastructure"></a>
<h2>Payment Infrastructure</h2>

<p>Payment in a multi-vendor travel booking involves more complexity than a single-vendor e-commerce purchase.</p>

<p>The user pays a single amount to MakeMyTrip. The platform then disburses payments to each supplier: the airline, the hotel, and the activity operator. Each disbursement happens on a different schedule. Airlines typically require payment at booking time. Hotels might allow payment at check-in. Activity operators might have a payment-at-service model.</p>

<p>The platform maintains a virtual wallet or escrow account that holds the collected payment until disbursement timings are met. A reconciliation engine runs daily (and sometimes hourly for high-volume periods) to match collected payments against disbursed amounts and flag discrepancies.</p>

<p>Payment failures are retried with exponential backoff. Idempotency keys are sent with every payment API call so that a retried call does not create a duplicate charge. These keys are generated from the booking ID and the component being paid, ensuring uniqueness even across retries.</p>

<p>Multi-vendor refunds are the ugliest case. A cancelled trip requires refunding the user while also recovering disbursed payments from each vendor. Each vendor has its own refund API and cancellation policy. The orchestrator tracks the state of each refund request and retries failed refunds asynchronously. The user receives a single consolidated refund regardless of how many partial vendor refunds were required to fund it.</p>

<a name="Travel.Notification.Infrastructure"></a>
<h2>Travel Notification Infrastructure</h2>

<p>Notifications are multi-channel (push, SMS, email, in-app) and time-sensitive in ways that most notification systems are not. A check-in reminder sent 30 minutes after check-in time is not just useless; it actively damages trust.</p>

<p>The notification service maintains a schedule for each booking. At booking confirmation time, a series of scheduled notifications are created: a confirmation summary immediately, a trip preparation reminder 7 days before departure, a check-in reminder 24 hours before hotel check-in, a flight status check at T-3 hours, and a local tips notification at destination arrival.</p>

<p>The scheduling system is built on a delayed job queue. Each notification is placed in the queue with a scheduled delivery time. Workers pick up jobs when their time arrives and dispatch them through the appropriate channel.</p>

<p>Travel alerts are triggered reactively by external events rather than scheduled times. Flight delay data comes from airline status feeds. Weather alerts come from meteorological data feeds. These are consumed by the notification service as event streams. When a monitored flight shows a delay exceeding 30 minutes, the service generates and dispatches a delay notification to the affected users immediately.</p>

<p>Personalization applies even to notifications. Users who have opted for WhatsApp delivery get messages formatted for that channel. Users who previously marked push notifications as unhelpful get email instead. The notification service maintains channel preferences per user and routes accordingly.</p>

<a name="Event-Driven.Architecture"></a>
<h2>Event-Driven Architecture</h2>

<p>Every significant state change in the platform emits an event. Booking created, payment completed, hotel confirmed, activity cancelled, itinerary modified — all of these produce events that are published to a distributed event bus.</p>

<p>The event bus (implemented using a system like Apache Kafka) decouples producers from consumers. When a booking is confirmed, the booking service publishes an event. It does not know or care who consumes that event. Multiple consumers can independently react: the notification service sends a confirmation, the loyalty service awards points, the analytics platform records the conversion, the supplier systems receive confirmation requests.</p>

<div class="mermaid">
flowchart LR

%% ==================================================
%% EVENT PRODUCERS
%% ==================================================

subgraph Producers[&#8220;EVENT PRODUCERS&#8221;]
    BOOK[&#8220;Booking Service&#8221;]
    PAY[&#8220;Payment Service&#8221;]
    ITIN[&#8220;Itinerary Engine&#8221;]
    SUPP[&#8220;Supplier APIs&#8221;]
end

%% ==================================================
%% EVENT STREAMING PLATFORM
%% ==================================================

subgraph EventBus[&#8220;EVENT STREAMING PLATFORM&#8221;]
    KB[&#8220;Kafka Event Bus&#8221;]
end

%% ==================================================
%% EVENT CONSUMERS
%% ==================================================

subgraph Consumers[&#8220;EVENT CONSUMERS&#8221;]
    NOTIF[&#8220;Notification Service&#8221;]
    LOYAL[&#8220;Loyalty Service&#8221;]
    ANAL[&#8220;Analytics Platform&#8221;]
    AUDIT[&#8220;Audit Log&#8221;]
    REC[&#8220;Recommendation Engine&#8221;]
end

%% ==================================================
%% EVENT FLOW
%% ==================================================

BOOK &#8211;>|&#8221;booking.created&#8221;| KB

PAY &#8211;>|&#8221;payment.completed&#8221;| KB

ITIN &#8211;>|&#8221;trip.generated&#8221;| KB

SUPP &#8211;>|&#8221;inventory.updated&#8221;| KB

KB &#8211;>|&#8221;Notify Users&#8221;| NOTIF

KB &#8211;>|&#8221;Reward Points&#8221;| LOYAL

KB &#8211;>|&#8221;Business Metrics&#8221;| ANAL

KB &#8211;>|&#8221;Compliance Records&#8221;| AUDIT

KB &#8211;>|&#8221;Personalization Signals&#8221;| REC

%% ==================================================
%% STYLING
%% ==================================================

%% Producers
style BOOK fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff
style PAY fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff
style ITIN fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff
style SUPP fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff

%% Kafka
style KB fill:#dc2626,stroke:#991b1b,stroke-width:7px,color:#ffffff

%% Consumers
style NOTIF fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff
style LOYAL fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff
style ANAL fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff
style AUDIT fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff
style REC fill:#7c3aed,stroke:#5b21b6,stroke-width:4px,color:#ffffff

%% ==================================================
%% LINK STYLING
%% ==================================================

%% Producer -> Kafka
linkStyle 0 stroke:#2563eb,stroke-width:4px
linkStyle 1 stroke:#2563eb,stroke-width:4px
linkStyle 2 stroke:#2563eb,stroke-width:4px
linkStyle 3 stroke:#2563eb,stroke-width:4px

%% Kafka -> Consumers
linkStyle 4 stroke:#dc2626,stroke-width:5px
linkStyle 5 stroke:#dc2626,stroke-width:5px
linkStyle 6 stroke:#dc2626,stroke-width:5px
linkStyle 7 stroke:#dc2626,stroke-width:5px
linkStyle 8 stroke:#dc2626,stroke-width:5px

</div>


<p>Event durability is non-negotiable. If the Kafka broker is unavailable for a few minutes and a booking occurs during that window, the event must not be lost. Producers write to a local outbox table in the same database transaction as the booking record, using the transactional outbox pattern. A relay process reads the outbox and publishes to Kafka, retrying until successful. This ensures that every booking event eventually reaches all consumers, even if there are transient broker outages.</p>

<p>Consumers maintain their own offset tracking. If the notification service crashes while processing a batch of events, it restarts from the last committed offset and reprocesses. Consumers must therefore be idempotent: processing the same event twice should not send the user two confirmation messages.</p>

<a name="Database.Design"></a>
<h2>Database Design</h2>

<p>The core entity relationships are relatively straightforward. Complexity enters in the sheer volume of data and the read/write patterns.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Entity</th>
        <th>Primary Storage</th>
        <th>Key Columns</th>
        <th>Access Pattern</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Users</td>
        <td>PostgreSQL</td>
        <td>user_id, email, preferences, tier</td>
        <td>Point lookup by user_id or email</td>
      </tr>
      <tr>
        <td>Destinations</td>
        <td>PostgreSQL + Elasticsearch</td>
        <td>destination_id, name, country, lat, lng, popularity_score, seasonality</td>
        <td>Full-text + geo search via ES; detail by ID via PG</td>
      </tr>
      <tr>
        <td>Trips</td>
        <td>PostgreSQL</td>
        <td>trip_id, user_id, status, start_date, end_date, budget</td>
        <td>Lookup by user_id; status-based queries</td>
      </tr>
      <tr>
        <td>Itineraries</td>
        <td>PostgreSQL + S3 (JSON)</td>
        <td>itinerary_id, trip_id, version, day_plans (JSONB)</td>
        <td>Point lookup; versioned history in S3</td>
      </tr>
      <tr>
        <td>Flights</td>
        <td>Redis (cache) + PostgreSQL (confirmed)</td>
        <td>search results cached; confirmed bookings in PG</td>
        <td>High-read search from cache; write-once confirmed records</td>
      </tr>
      <tr>
        <td>Hotels</td>
        <td>Elasticsearch + PostgreSQL</td>
        <td>hotel_id, location, amenities, price_tier, ratings</td>
        <td>Geo + facet search; confirmation records in PG</td>
      </tr>
      <tr>
        <td>Bookings</td>
        <td>PostgreSQL</td>
        <td>booking_id, trip_id, component_type, supplier_ref, status, amount</td>
        <td>Lookup by booking_id, trip_id; status updates</td>
      </tr>
      <tr>
        <td>Payments</td>
        <td>PostgreSQL</td>
        <td>payment_id, booking_id, amount, status, idempotency_key, provider_ref</td>
        <td>Lookup by payment_id, idempotency_key; append-mostly</td>
      </tr>
    </tbody>
  </table>
</div>


<p>Partitioning decisions matter at scale. The bookings table is partitioned by created_at month. This keeps hot partition sizes manageable and allows cold partitions to be archived to cheaper storage. The payments table uses the same monthly partitioning strategy.</p>

<p>The itinerary day_plans are stored as JSONB in PostgreSQL. This allows schema flexibility as the itinerary data model evolves without requiring migrations. The full itinerary history is snapshotted to S3 for audit purposes.</p>

<p>Indexes are added deliberately. The bookings table has indexes on user_id, trip_id, and status. The trips table has a compound index on (user_id, status) for the &ldquo;active trips&rdquo; query. Over-indexing slows writes; the platform audits index usage quarterly and drops unused indexes.</p>

<a name="Caching.Systems"></a>
<h2>Caching Systems</h2>

<p>Caching in a travel platform is aggressive because the underlying data is expensive to fetch and changes on different schedules.</p>

<p>Destination data is the most stable. A destination&rsquo;s name, description, photos, and attraction catalog change rarely. This data is cached in Redis with a TTL of several hours. The cache is warmed at startup by pre-loading all destination records. Cache invalidation happens on manual edits by content managers, which triggers a targeted cache key deletion.</p>

<p>Hotel search results are cached with a shorter TTL (10-15 minutes) because availability and pricing change frequently. The cache key encodes all search parameters. Cache hits serve results instantly; misses trigger live supplier calls.</p>

<p>Flight search results are cached even more aggressively because airline API calls are expensive in both latency and (sometimes) money. Results are cached for 10 minutes. The moment a user moves to booking, the cached results are discarded and a live check is made.</p>

<p>Recommendation results are cached per user segment, not per user, to avoid combinatorial explosion. A user is assigned to a set of segments based on their travel history, origin, and preferences. The recommendations for that segment combination are cached and served to all users in that segment. Individual personalization multipliers are applied at serve time by the re-ranker.</p>

<p>The hotspot problem is real during peak periods. When Goa packages are on sale, thousands of users simultaneously search Goa. Without caching, every search would hit the hotel and flight aggregators. With a shared cache keyed on destination plus dates, the first request fetches live data and all subsequent requests within the TTL serve from cache. This is a dramatic reduction in supplier API load.</p>

<a name="Scalability.Deep.Dive"></a>
<h2>Scalability Deep Dive</h2>

<p>Travel platforms face two scaling patterns that are different in character. The first is gradual growth: more users, more destinations, more suppliers over time. The second is extreme spikes: holiday announcements, flash sales, festival season, and school vacation peaks drive 10x or 20x normal traffic in hours.</p>

<p>The platform is horizontally scaled at every layer. API gateway instances behind a load balancer. Stateless service instances that scale on CPU metrics. Database read replicas for high-read services like destination search and hotel catalog.</p>

<p>The recommendation engine is the most compute-intensive service. Model inference for a single user recommendation takes significant CPU. At scale, inference is offloaded to dedicated GPU-backed inference servers. Model updates happen offline in batch training jobs; the resulting model artifacts are deployed to inference servers without taking down the recommendation service.</p>

<p>The itinerary engine is CPU-intensive for the optimization computation. It scales horizontally with each instance maintaining in-memory attraction data cached from Redis. The attraction catalog is small enough to fit entirely in instance memory for fast access during optimization.</p>

<p>Booking orchestration does not scale well with simple horizontal replication because each booking is a stateful saga. The platform assigns each booking saga to a dedicated orchestrator thread with state persisted in the database. If an orchestrator instance fails, another instance can pick up the saga from its last recorded state and continue. This gives horizontal scale without losing saga state.</p>

<p>Holiday traffic spikes are handled through pre-scaling. The platform uses historical traffic data to predict when spikes will occur (e.g., Independence Day travel announcements, school closing dates) and pre-provisions additional capacity a day before. Auto-scaling handles unexpected spikes up to a cap; beyond the cap, graceful degradation kicks in.</p>

<p>Graceful degradation under extreme load means accepting some features may become unavailable while core booking flows remain functional. The first things to degrade are: recommendation engine (fall back to popularity-based results), real-time availability checks on activities (show approximate availability), and personalization on destination search (fall back to globally ranked results). Flights and hotel booking remain fully functional because those are revenue-critical.</p>

<a name="Reliability.and.Availability"></a>
<h2>Reliability and Availability</h2>

<p>The platform targets 99.99% availability for booking flows. This requires redundancy at every layer and well-tested failover procedures.</p>

<p>Each service runs in multiple availability zones. Database primaries have synchronous replicas in a second zone. If the primary zone has an outage, failover to the replica happens within seconds with minimal data loss.</p>

<p>Supplier API outages are the most common reliability challenge. If the Amadeus GDS is down, flight search from that source degrades but results from other connected airlines continue. The aggregator has a circuit breaker per supplier. When a supplier crosses an error rate threshold, its circuit opens and requests to that supplier are fast-failed rather than waiting for a timeout. The circuit closes again after a recovery period.</p>

<p>Booking failures are tracked at every step of the saga. Operations teams have a dashboard showing in-flight saga states, failed steps, and pending rollbacks. An on-call engineer can see exactly which bookings are in a partial failure state and intervene if automated rollback has not resolved the issue.</p>

<p>Disaster recovery means defining recovery time objectives (RTO) and recovery point objectives (RPO) per service tier. Core booking data has an RPO of near-zero (synchronous replication) and an RTO of under a minute (automatic failover). Recommendation model artifacts have an RPO of a few hours (last training run) and an RTO of minutes (model redeployment is fast).</p>

<a name="Security.Considerations"></a>
<h2>Security Considerations</h2>

<p>Travel platforms are high-value targets. A breached account can be used to rebook flights under different names, drain stored travel credits, or access personal identification documents attached to booking records.</p>

<p>Authentication uses OAuth 2.0 with JWT tokens. Tokens have short expiry windows (15-60 minutes). Refresh tokens are long-lived but rotated on use. Social login is offered but accounts linked to social providers are treated with the same security posture as email-password accounts.</p>

<p>Payment card data never touches MakeMyTrip servers. All card processing is handled through PCI-compliant payment gateways. Tokenized card references are stored, not raw card numbers.</p>

<p>Fraud detection runs on every booking attempt. Features include device fingerprint, velocity checks (how many bookings from this account in the last hour), IP reputation, card BIN checks, and booking pattern analysis. Suspicious bookings are flagged for manual review or step-up verification before confirmation.</p>

<p>Bot detection on search APIs is critical because automated fare scrapers can easily saturate supplier API quotas and disrupt legitimate users. CAPTCHA is triggered when search velocity from a single session exceeds normal human behavior. Progressive throttling and IP-based rate limiting are the first defense layers.</p>

<a name="Engineering.Tradeoffs"></a>
<h2>Engineering Tradeoffs</h2>

<p>These are the real decisions that architects make, and the honest answer is almost always &ldquo;it depends.&rdquo;</p>

<p>Recommendation quality versus latency is a genuine tension. A more complex model produces better recommendations but takes longer to compute. The platform resolves this by pre-computing segment-level recommendations offline and personalizing lightly at serve time. The tradeoff is that recommendations are somewhat generalized within segments rather than fully individualized, but latency stays under 100ms.</p>

<p>Personalization versus privacy is increasingly consequential. Deep personalization requires storing detailed user behavior. The platform allows users to opt into or out of behavioral tracking. Users who opt out get segment-level recommendations rather than individual ones. This is architecturally cleaner than trying to personalize without data.</p>

<p>Caching versus freshness is the most common tradeoff in travel systems. A hotel cache with a 15-minute TTL will occasionally show slightly stale pricing. The platform accepts this tradeoff because the alternative (live fetch on every search) would produce unacceptably high latency. The pre-booking availability check catches actual staleness before money changes hands.</p>

<p>Package flexibility versus optimization creates a genuine product tension. A fully optimized itinerary might not match what a user actually wants. The platform generates an optimized base itinerary and then makes every element editable. Users can swap attractions, change hotel selections, and modify timing. The optimization gives a good starting point; editability respects user agency.</p>

<p>Consistency versus availability in booking systems is where CAP theorem becomes concrete. During a supplier outage, the platform can choose to show stale availability (preferring availability) or show no results from that supplier (preferring consistency). The platform chooses to show stale data with a clear recency indicator, letting users know results may not be fully current.</p>

<a name="AI.and.the.Future.of.Travel.Planning"></a>
<h2>AI and the Future of Travel Planning</h2>

<p>The current AI integration in travel planning is sophisticated but still fundamentally rule-based in its core planning logic. The next evolution is genuinely AI-native planning.</p>

<p>Conversational trip planning means a user can describe what they want in natural language and the system assembles a plan through dialogue. &ldquo;I want a relaxed trip with good food and some nature, not too much walking, about 8 days, budget around 80,000&rdquo; is a hard query for a form-based UI but a tractable input for a language model that understands preferences and can map them to itinerary parameters.</p>

<p>Autonomous travel agents take this further. An agent could proactively monitor flight prices after a user expresses interest in a destination, alert them when a good deal appears, and with one confirmation begin assembling a full package. This requires trust, and trust requires the agent to have a provable track record of judgment.</p>

<p>Predictive recommendations flip the current model. Rather than responding to user intent, the system predicts when a user is likely to plan a trip and proactively surfaces destination ideas before they start searching. This relies on longitudinal behavioral modeling: detecting patterns in when a user typically travels, what triggers their trip planning, and what they tend to book.</p>

<p>The architecture implications of an AI-native planning system are significant. Language model inference at scale is expensive. Prompt design becomes a core engineering discipline. Evaluation infrastructure for open-ended AI outputs is harder to build than metrics for ranked lists. The platform needs to maintain correctness over AI-generated itinerary content while allowing the flexibility that makes AI planning valuable.</p>

<p>Multi-modal inputs will change how users interact with planning. A user uploading photos from a travel magazine and saying &ldquo;I want somewhere that looks like this&rdquo; is a legitimate and useful query. Processing that intent requires vision models combined with destination matching on visual similarity. This kind of interaction is not far away.</p>

<a name="Real-World.Technology.Stack"></a>
<h2>Real-World Technology Stack</h2>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Technology</th>
        <th>Used For</th>
        <th>Why This Choice</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Java + Spring Boot</td>
        <td>Core booking and payment services</td>
        <td>Strong ecosystem for transactional systems, mature connection pooling, excellent observability tooling</td>
      </tr>
      <tr>
        <td>Go</td>
        <td>Flight and hotel aggregation, high-throughput API services</td>
        <td>Low memory footprint, excellent concurrency via goroutines, fast startup time for scaled-out instances</td>
      </tr>
      <tr>
        <td>Python</td>
        <td>Recommendation models, ML training, data pipelines</td>
        <td>Dominant ML ecosystem (PyTorch, scikit-learn), fast prototyping, excellent data tooling</td>
      </tr>
      <tr>
        <td>Redis</td>
        <td>Session cache, search cache, recommendation cache, rate limiting</td>
        <td>Sub-millisecond reads, native data structures for sorted sets and pub-sub, wide operational familiarity</td>
      </tr>
      <tr>
        <td>Apache Kafka</td>
        <td>Event bus for booking events, payment events, notification triggers</td>
        <td>Durable message log, consumer group model, high throughput, strong ecosystem</td>
      </tr>
      <tr>
        <td>PostgreSQL</td>
        <td>User data, bookings, payments, itinerary records</td>
        <td>ACID transactions for financial records, JSONB for flexible schema, mature partitioning</td>
      </tr>
      <tr>
        <td>Elasticsearch</td>
        <td>Destination search, hotel search, activity discovery</td>
        <td>Full-text + geo search in one system, horizontal scaling, rich aggregation API</td>
      </tr>
      <tr>
        <td>Kubernetes</td>
        <td>Service orchestration, auto-scaling, rolling deployments</td>
        <td>Industry standard for microservice deployment, declarative scaling, strong observability integrations</td>
      </tr>
    </tbody>
  </table>
</div>


<p>The polyglot stack is an intentional choice, not a mistake. Different services have fundamentally different performance profiles. An aggregation service making thousands of concurrent outbound HTTP calls is a different beast from a booking service managing ACID transactions. Forcing both into the same technology stack would mean accepting suboptimal performance in one of them. The operational cost of polyglot is real (different monitoring setups, different deployment configs, different debugging skills required) but the performance benefits justify it at scale.</p>

<a name="System.Design.Interview.Perspective"></a>
<h2>System Design Interview Perspective</h2>

<p>Travel planner system design is a common senior-level interview question because it touches so many interesting engineering domains simultaneously.</p>

<p>Interviewers expect candidates to identify the key complexities upfront. A weak answer treats travel planner as &ldquo;flight booking plus hotel booking.&rdquo; A strong answer immediately flags the multi-vendor inventory problem, the itinerary generation optimization problem, the distributed transaction challenge in booking, and the recommendation system cold start problem.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Discussion Area</th>
        <th>Weak Answer</th>
        <th>Strong Answer</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Recommendation System</td>
        <td>&#8220;Use collaborative filtering based on user bookings&#8221;</td>
        <td>Identify cold start problem, combine collaborative + content-based + behavioral signals, discuss segment-level caching, explain how to handle sparse travel booking frequency</td>
      </tr>
      <tr>
        <td>Itinerary Generation</td>
        <td>&#8220;Sort attractions by rating and add them to the schedule&#8221;</td>
        <td>Frame as orienteering problem, explain geographic clustering, discuss transit time calculation, describe local search optimization, mention opening hours and energy modeling</td>
      </tr>
      <tr>
        <td>Booking Orchestration</td>
        <td>&#8220;Book flight, hotel, and activities in sequence&#8221;</td>
        <td>Identify distributed transaction problem, propose saga pattern, explain compensating transactions, discuss idempotency, walk through partial failure scenarios</td>
      </tr>
      <tr>
        <td>Scalability</td>
        <td>&#8220;Use horizontal scaling and load balancers&#8221;</td>
        <td>Distinguish gradual growth from spike patterns, explain pre-scaling for predictable spikes, describe graceful degradation, discuss caching strategy for flight and hotel search</td>
      </tr>
      <tr>
        <td>Inventory Staleness</td>
        <td>&#8220;Refresh data frequently&#8221;</td>
        <td>Explain two-stage commitment model (hold then confirm), discuss TTL-based caching with pre-booking live checks, describe circuit breakers per supplier</td>
      </tr>
    </tbody>
  </table>
</div>


<p>Common mistakes candidates make: spending too much time on authentication and API gateway (these are important but not the interesting parts for this problem), ignoring the recommendation system entirely, proposing a single synchronous booking flow without considering partial failures, and treating inventory as always accurate without discussing staleness.</p>

<p>The questions that make strong candidates stand out are: &ldquo;How do you handle the case where flight booking succeeds but hotel booking fails after payment?&rdquo; and &ldquo;How do you keep hotel availability from becoming stale?&rdquo; These show the candidate is thinking about failure cases in a distributed system, not just the happy path.</p>

<p>Interviewer follow-ups often probe into the itinerary generation engine because it is genuinely complex and candidates who claim to understand it often cannot explain the optimization approach coherently. Being able to say &ldquo;this is a variant of the orienteering problem, and we solve it with geographic clustering followed by a greedy construction heuristic and local search improvement&rdquo; is a strong differentiator.</p>

<a name="Closing.Thoughts"></a>
<h2>Closing Thoughts</h2>

<p>The meta-lesson in designing systems like this is that complexity lives at the boundaries. The boundaries between recommendation and booking, between inventory and pricing, between distributed transaction and consistency, between personalization and privacy. The best engineers can articulate what is hard at each boundary and why specific architectural choices were made to manage it.</p>

<p>Travel planning is a product domain where getting the engineering right has direct user impact. A poorly generated itinerary leads to a bad vacation. A failed booking transaction leads to a user stuck at an airport without a hotel. The stakes of the system design are real, and that is what makes it a rich problem worth understanding deeply.</p>
]]></content>
    </entry>
    
    <entry>
        <title type="html"><![CDATA[How BookMyShow Works?]]></title>
        <link href="https://rishijeet.github.io/blog/how-bookmyshow-works/"/>
        <updated>2026-05-31T13:40:29+05:30</updated>
        <id>https://rishijeet.github.io/blog/how-bookmyshow-works</id>
        <content type="html"><![CDATA[<blockquote><p><em>BookMyShow looks simple from the outside. You open the app, pick a movie, choose your seats, pay, and get a ticket. The whole thing takes about two minutes. But underneath that simple experience is one of the most technically demanding systems on the internet — a platform that needs to handle millions of concurrent users, prevent duplicate seat bookings, coordinate with thousands of theatres, process payments reliably, and generate tickets instantly, all without ever letting two people land on the same seat.</em></p></blockquote>

<p><img src="/images/system_design/bookmyshow-banner.jpg" height="300" width="900" alt="Alt text" /></p>

<p>This is not a generic booking tutorial. This is an engineering walkthrough of how a production-scale ticketing platform actually works.</p>

<a name="Why.Ticket.Booking.Is.Harder.Than.It.Looks"></a>
<h2>Why Ticket Booking Is Harder Than It Looks</h2>

<p>Most engineers underestimate ticket booking systems. At first glance it seems like a basic CRUD application — read seats, mark one as taken, take payment. But that mental model breaks down immediately under real-world conditions.</p>

<p>Think about what happens when a blockbuster like a new Avengers film drops its tickets at midnight, or when IPL finals seats go live, or when a Taylor Swift concert in Mumbai opens for booking. In those moments, hundreds of thousands of users simultaneously hit the same endpoint, trying to grab the same finite set of seats. You are not dealing with a typical web workload anymore. You are dealing with a flash sale, and flash sales expose every weakness in your architecture.</p>

<!--more-->


<p>The core problem is that seats are a finite, non-fungible inventory. Unlike a product on an e-commerce site where you can restock, seats are fixed. Row C, Seat 12 in Screen 2 of PVR Juhu on Saturday night at 9 PM is a unique entity. If two users both manage to book it, you have a real-world problem that cannot be resolved with a refund alone — someone is physically going to show up and find someone else in their seat.</p>

<p>This makes the system inherently constrained by strong consistency requirements in a distributed environment. And strong consistency in a distributed environment is expensive. That tension — between consistency, performance, and availability — is what makes this problem genuinely interesting from an engineering perspective.</p>

<a name="Core.Features.of.the.Platform"></a>
<h2>Core Features of the Platform</h2>

<p>Before diving into implementation, it helps to understand the full feature surface. BookMyShow is not just a booking tool.</p>

<p>It handles movie and event discovery — surfacing what is playing near you, with which shows, at which times. It manages a live inventory of seats across thousands of theatres. It coordinates real-time seat selection with a temporary locking mechanism to prevent conflicts during checkout. It orchestrates payments across UPI, cards, wallets, and net banking. It generates tickets with unique QR codes tied to a specific booking. It sends confirmation messages and reminders via SMS, email, and push. It handles cancellations and refunds. It runs offers and coupon validation. And it does all of this while also powering a fairly sophisticated recommendation and personalization layer.</p>

<p>Each of these features is a separate engineering problem, and they all interact with each other in ways that make the system genuinely complex.</p>

<a name="High-Level.Architecture"></a>
<h2>High-Level Architecture</h2>

<p>The platform is built as a collection of services, each owning a distinct domain. This is not microservices for the sake of microservices — the domains are genuinely different in their scaling needs, data models, and consistency requirements.</p>

<div class="mermaid">
graph TD
    subgraph ClientLayer[&#8220;Client Layer&#8221;]
        MA[Mobile App]
        WA[Web App]
    end

    subgraph Gateway[&#8220;API Layer&#8221;]
        AG[API Gateway]
        AL[Auth Layer]
    end

    subgraph CoreServices[&#8220;Core Services&#8221;]
        CS[Movie Catalog Service]
        ES[Event Catalog Service]
        SS[Search Service]
        IS[Inventory Service]
        SL[Seat Lock Service]
        BS[Booking Service]
        PS[Payment Service]
        TS[Ticket Service]
        NS[Notification Service]
    end

    subgraph Storage[&#8220;Storage Layer&#8221;]
        PG[(PostgreSQL)]
        RD[(Redis)]
        ES2[(Elasticsearch)]
        S3[(Object Storage)]
    end

    subgraph Messaging[&#8220;Messaging Layer&#8221;]
        KF[Kafka]
    end

    MA &#8211;> AG
    WA &#8211;> AG
    AG &#8211;> AL
    AL &#8211;> CS
    AL &#8211;> ES
    AL &#8211;> SS
    AL &#8211;> IS
    AL &#8211;> SL
    AL &#8211;> BS
    AL &#8211;> PS
    CS &#8211;> PG
    ES &#8211;> PG
    SS &#8211;> ES2
    IS &#8211;> PG
    IS &#8211;> RD
    SL &#8211;> RD
    BS &#8211;> PG
    BS &#8211;> KF
    PS &#8211;> KF
    KF &#8211;> TS
    KF &#8211;> NS
    TS &#8211;> S3

    classDef client fill:#4A90D9,stroke:#2C5F8A,color:#fff
    classDef gateway fill:#7B68EE,stroke:#5A4DB8,color:#fff
    classDef service fill:#2ECC71,stroke:#1A7A44,color:#fff
    classDef storage fill:#E67E22,stroke:#A85C18,color:#fff
    classDef messaging fill:#E74C3C,stroke:#922B21,color:#fff

    class MA,WA client
    class AG,AL gateway
    class CS,ES,SS,IS,SL,BS,PS,TS,NS service
    class PG,RD,ES2,S3 storage
    class KF messaging
</div>


<p>The interesting thing about this architecture is how the services are split. Movie Catalog and Event Catalog are read-heavy and can tolerate some staleness. Seat Lock Service and Inventory Service need to be strongly consistent. Booking and Payment need transactional guarantees. Notification and Ticket Generation can be asynchronous. Those different consistency and availability needs drive nearly every architectural decision in the system.</p>

<a name="Movie.and.Event.Discovery"></a>
<h2>Movie and Event Discovery</h2>

<p>Discovery is the entry point for every user. When someone opens BookMyShow in Mumbai and searches for movies playing this weekend, a lot has to happen very fast.</p>

<p>The city-based discovery model works by maintaining a geographically-indexed catalog. When you land on the homepage, the app knows your city from your profile or device location. It queries a discovery API that returns movies currently showing in your city, filtered and ranked by show availability, ratings, and personalization signals.</p>

<p>The movie catalog itself is maintained separately from the show schedule. A movie has metadata — title, description, cast, genre, language, certificate rating, poster images — that rarely changes. The show schedule, which theatres are running which film at which times, changes every day and sometimes multiple times a day. Keeping these two concerns separate allows the catalog to be heavily cached while show data stays fresher.</p>

<p>Search is powered by a system like Elasticsearch. Users search not just by movie title but by theatre name, actor names, genre, and location. Elasticsearch handles the fuzzy matching, typo tolerance, and relevance ranking. The search index is updated asynchronously as the catalog changes — new movies are indexed within seconds of being added, and theatre schedule updates propagate within a few minutes.</p>

<p>Personalization layers on top of this through a separate recommendation system. If you frequently book action films on Friday evenings, the platform learns that and surfaces relevant content higher. This runs on pre-computed models that score user-movie pairs and persist the scores in a fast-read store.</p>

<p>Caching is critical here. The top 50 movies showing in a given city on a given day are queried millions of times. These results are cached aggressively at the CDN level and at the application cache level. The cache is invalidated when a new movie is added or when show availability changes significantly.</p>

<a name="Theatre.Integration.Architecture"></a>
<h2>Theatre Integration Architecture</h2>

<p>This is one of the most overlooked parts of the system, and one of the most complex. BookMyShow does not own theatres. It partners with thousands of independent chains — PVR, INOX, Cinepolis, and hundreds of single-screen and regional chains — each of which runs its own internal ticketing and seat management systems.</p>

<p>Integrating with those systems is a significant engineering challenge. The integration architecture looks something like this:</p>

<div class="mermaid">
graph TD
    subgraph TheatrePartners[&#8220;Theatre Partners&#8221;]
        PVR[PVR System]
        INOX[INOX System]
        IND[Independent Theatres]
    end

    subgraph Integration[&#8220;Integration Layer&#8221;]
        TA[Theatre Adapter]
        SC[Schedule Sync]
        IV[Inventory Validator]
    end

    subgraph Platform[&#8220;BookMyShow Platform&#8221;]
        IS[Inventory Service]
        BS[Booking Service]
        RD[(Redis Cache)]
        PG[(PostgreSQL)]
    end

    PVR &#8211;>|API Push or Pull| TA
    INOX &#8211;>|API Push or Pull| TA
    IND &#8211;>|File or API| TA
    TA &#8211;> SC
    SC &#8211;> IV
    IV &#8211;> IS
    IS &#8211;> RD
    IS &#8211;> PG
    BS &#8211;> TA
    TA &#8211;>|Confirm Booking| PVR
    TA &#8211;>|Confirm Booking| INOX

    classDef theatre fill:#8E44AD,stroke:#6C3483,color:#fff
    classDef integration fill:#2980B9,stroke:#1F618D,color:#fff
    classDef platform fill:#27AE60,stroke:#1D8348,color:#fff
    classDef storage fill:#E67E22,stroke:#A85C18,color:#fff

    class PVR,INOX,IND theatre
    class TA,SC,IV integration
    class IS,BS platform
    class RD,PG storage
</div>


<p>Some theatres provide real-time APIs. Some push schedule updates via webhooks. Some older single-screen theatres still deliver CSV files of their show schedules. The Theatre Adapter layer normalizes all of this into a consistent internal representation.</p>

<p>Inventory synchronization is tricky because theatre systems often maintain their own seat state. When a customer books through a theatre&rsquo;s own counter, BookMyShow needs to know that seat is gone. Synchronization happens through a combination of periodic polling, webhook updates, and event-driven callbacks. The polling interval varies by partner sophistication — every 30 seconds for modern API-driven partners, every few minutes for slower systems.</p>

<p>The hard problem is what happens when synchronization fails. If the platform loses connectivity to a theatre&rsquo;s system mid-booking, you need a fallback. The typical approach is to hold a pessimistic view of inventory during outages — mark seats as unavailable if you cannot confirm their state. This hurts conversion but prevents double bookings, which is the right tradeoff.</p>

<p>When BookMyShow itself confirms a booking, it calls back to the theatre&rsquo;s system to mark the seat as taken in their internal state. This confirmation call is critical. If it fails, the theatre might resell that seat through their own counter. The system handles this with retries, exponential backoff, and a reconciliation job that runs periodically to catch divergence between BookMyShow&rsquo;s booking records and the theatre&rsquo;s actual state.</p>

<a name="Seat.Inventory.Management"></a>
<h2>Seat Inventory Management</h2>

<p>Inventory management is the heart of the platform, and it is where most of the interesting distributed systems problems live.</p>

<p>Every seat in every screen in every theatre in every city has a lifecycle. At any given moment, a seat is in one of three states: available, locked, or booked.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>State</th>
        <th>Meaning</th>
        <th>Duration</th>
        <th>Owner</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Available</td>
        <td>No one has expressed interest; seat can be selected</td>
        <td>Until selected or show starts</td>
        <td>None</td>
      </tr>
      <tr>
        <td>Locked</td>
        <td>A user is in the process of booking; temporarily reserved</td>
        <td>Typically 8–10 minutes</td>
        <td>Specific user session</td>
      </tr>
      <tr>
        <td>Booked</td>
        <td>Payment confirmed; seat is permanently sold</td>
        <td>Permanent until show</td>
        <td>Specific booking ID</td>
      </tr>
      <tr>
        <td>Expired Lock</td>
        <td>User abandoned checkout; lock expired and seat released</td>
        <td>Transitions back to Available</td>
        <td>None</td>
      </tr>
    </tbody>
  </table>
</div>


<p>The trickiest state is the Locked state. This is a temporary hold on a seat that gives a user time to complete checkout without another user stealing the seat. Getting this right requires careful engineering.</p>

<p>The most natural implementation is to use a database column — a <code>locked_by</code> field and a <code>lock_expires_at</code> timestamp. When a user selects seats, you run an update that atomically sets these fields if the current state is <code>available</code>. If the update affects zero rows, the seat was already taken. If it affects one row, you have the lock.</p>

<p>This works, but it has limitations at scale. Every seat selection generates database writes, and at peak load, thousands of these writes compete for the same rows. This creates lock contention in the database itself.</p>

<p>A better approach is to move the locking layer out of the primary database and into Redis. Redis is single-threaded for its command execution, which makes it naturally suited for serializing concurrent lock requests. It also has native TTL support, which handles lock expiry automatically.</p>

<a name="Seat.Locking.System.Deep.Dive"></a>
<h2>Seat Locking System Deep Dive</h2>

<p>The seat locking system deserves its own careful examination because it is where so many booking platforms fail.</p>

<p>The locking flow looks like this:</p>

<div class="mermaid">
sequenceDiagram
    participant U as User
    participant BS as Booking Service
    participant SL as Seat Lock Service
    participant RD as Redis
    participant IS as Inventory Service

    U->>BS: Select seats A1 B2
    BS->>IS: Check seat availability
    IS->>RD: GET seat:show123:A1
    RD&#8211;>>IS: null (available)
    IS&#8211;>>BS: Seats available
    BS->>SL: Acquire lock for A1 B2
    SL->>RD: SET seat:show123:A1 userSession123 EX 600 NX
    SL->>RD: SET seat:show123:B2 userSession123 EX 600 NX
    RD&#8211;>>SL: OK
    SL&#8211;>>BS: Lock acquired
    BS&#8211;>>U: Seats locked for 10 min
    U->>BS: Complete payment
    BS->>IS: Mark seats as booked
    IS->>RD: SET seat:show123:A1 BOOKED
    IS->>RD: SET seat:show123:B2 BOOKED
    IS&#8211;>>BS: Booking confirmed
</div>


<p>The <code>SET key value EX ttl NX</code> command in Redis is the workhorse here. <code>NX</code> means &ldquo;only set if not exists.&rdquo; This makes the operation atomic — if two users try to lock the same seat at the exact same millisecond, only one will succeed because Redis executes commands sequentially. The <code>EX 600</code> sets a 600-second expiry, which automatically releases the lock if the user abandons checkout.</p>

<p>When a user selects multiple seats, all locks must be acquired as a unit. You cannot lock seat A1 successfully, fail to lock B2, and leave A1 locked — that creates phantom reservations. The lock service tries to acquire all requested seats atomically using a Redis transaction (MULTI/EXEC). If any lock fails, it rolls back all acquired locks and returns an error to the user.</p>

<p>The TTL is a careful balance. Too short, and users run out of time mid-checkout. Too long, and abandoned sessions tie up inventory. Most ticketing platforms land somewhere between 8 and 15 minutes. BookMyShow shows a countdown timer to the user, which also creates a mild urgency nudge that improves checkout conversion.</p>

<p>Lock failures happen in several scenarios. The most common is two users selecting the same seat simultaneously. The first one gets the lock; the second gets an error and is shown a message that the seat was just taken. The inventory display updates in near real-time to show the seat as unavailable.</p>

<p>A subtler failure mode is the lock acquired but payment failing and the user retrying. In this case, the lock is still valid (the TTL has not expired), so the retry does not need to re-acquire. The booking service checks lock ownership before each payment attempt — if the lock is still held by the same session, it proceeds.</p>

<p>What happens when the system crashes between locking and booking? Redis TTLs handle the cleanup — seats auto-expire. But if a payment was initiated before the crash, you need idempotency keys to ensure the payment is not charged twice on retry.</p>

<a name="Booking.Workflow"></a>
<h2>Booking Workflow</h2>

<p>The complete booking journey is more complex than it appears. Here is the full flow:</p>

<div class="mermaid">
flowchart TD;

%% =========================
%% Movie Discovery
%% =========================

A[Open BookMyShow App];
B[Detect City];
C[Discover Movies];
D[Select Movie];
E[Browse Show Dates];
F[Choose Theatre and Showtime];

%% =========================
%% Seat Selection
%% =========================

G[Load Interactive Seat Map];
H[Select Seats];

I{Seats Available?};

J[Seats No Longer Available];

K[Acquire Seat Lock];

L{Lock Acquired?};

%% =========================
%% Checkout
%% =========================

M[Apply Offers and Coupons];
N[Display Payment Screen];
O[Choose Payment Method];
P[Initiate Payment];

Q{Payment Successful?};

R[Retry Payment or Cancel];

%% =========================
%% Booking Completion
%% =========================

S[Confirm Booking];
T[Mark Seats Booked<br/>and Release Lock];
U[Generate Digital Ticket];
V[Send Email and Push Notification];
W[Display Booking Confirmation];

X[Release Seat Lock];

%% =========================
%% User Journey
%% =========================

A &#8211;>|Launch App| B;
B &#8211;>|Location Resolved| C;
C &#8211;>|Movie Selected| D;
D &#8211;>|View Showtimes| E;
E &#8211;>|Choose Show| F;
F &#8211;>|Load Seats| G;
G &#8211;>|Seat Selection| H;

H &#8211;> I;

I &#8211;>|No| J;

I &#8211;>|Yes| K;

K &#8211;> L;

L &#8211;>|No| J;

L &#8211;>|Yes| M;

M &#8211;> N;

N &#8211;> O;

O &#8211;> P;

P &#8211;> Q;

Q &#8211;>|No| R;

Q &#8211;>|Yes| S;

S &#8211;> T;

T &#8211;> U;

U &#8211;> V;

V &#8211;> W;

R &#8211;>|Retry| P;

R &#8211;>|Cancel| X;

%% =========================
%% Node Styling
%% =========================

style A fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff;
style B fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff;
style C fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff;
style D fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff;
style E fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff;
style F fill:#2563eb,stroke:#1e40af,stroke-width:4px,color:#ffffff;

style G fill:#16a34a,stroke:#166534,stroke-width:4px,color:#ffffff;
style H fill:#16a34a,stroke:#166534,stroke-width:4px,color:#ffffff;
style K fill:#16a34a,stroke:#166534,stroke-width:4px,color:#ffffff;

style I fill:#f59e0b,stroke:#b45309,stroke-width:5px,color:#ffffff;
style L fill:#f59e0b,stroke:#b45309,stroke-width:5px,color:#ffffff;
style Q fill:#f59e0b,stroke:#b45309,stroke-width:5px,color:#ffffff;

style M fill:#06b6d4,stroke:#0e7490,stroke-width:4px,color:#ffffff;
style N fill:#06b6d4,stroke:#0e7490,stroke-width:4px,color:#ffffff;
style O fill:#06b6d4,stroke:#0e7490,stroke-width:4px,color:#ffffff;
style P fill:#06b6d4,stroke:#0e7490,stroke-width:4px,color:#ffffff;

style S fill:#22c55e,stroke:#15803d,stroke-width:5px,color:#ffffff;
style T fill:#22c55e,stroke:#15803d,stroke-width:5px,color:#ffffff;
style U fill:#22c55e,stroke:#15803d,stroke-width:5px,color:#ffffff;
style V fill:#22c55e,stroke:#15803d,stroke-width:5px,color:#ffffff;
style W fill:#22c55e,stroke:#15803d,stroke-width:5px,color:#ffffff;

style J fill:#ef4444,stroke:#b91c1c,stroke-width:4px,color:#ffffff;
style R fill:#ef4444,stroke:#b91c1c,stroke-width:4px,color:#ffffff;
style X fill:#ef4444,stroke:#b91c1c,stroke-width:4px,color:#ffffff;

%% =========================
%% Link Styling
%% =========================

linkStyle 0,1,2,3,4,5,6 stroke:#2563eb,stroke-width:3px;
linkStyle 7,9 stroke:#16a34a,stroke-width:4px;
linkStyle 8,10,18 stroke:#ef4444,stroke-width:4px;
linkStyle 11,12,13,14 stroke:#06b6d4,stroke-width:4px;
linkStyle 15 stroke:#22c55e,stroke-width:5px;
linkStyle 16,17,19,20 stroke:#22c55e,stroke-width:4px;

</div>


<p>Each step in this flow has its own failure modes and engineering considerations. Let us walk through the most important ones.</p>

<p>When the user arrives at the seat map, they are seeing a snapshot of inventory. This snapshot is served from cache and can be a few seconds old. The real source of truth is checked only when they actually try to lock seats. This is intentional — serving the seat map from the live database for every viewer at peak load would be impossibly expensive.</p>

<p>Offer and coupon validation happens after seat selection but before payment. This step queries the offers service to check eligibility. Common checks include minimum order value, applicable payment method, user eligibility (first booking, specific bank card offer), and offer inventory (some offers are limited to a certain number of uses).</p>

<p>The payment initiation step is where the platform hands off to external payment processors. This handoff creates a window of uncertainty — the user&rsquo;s money might leave their account before BookMyShow can confirm the booking. Managing that window is the core of payment engineering.</p>

<a name="Payment.Infrastructure"></a>
<h2>Payment Infrastructure</h2>

<p>Payment is the most failure-sensitive part of the system. Money is involved, so correctness is non-negotiable.</p>

<div class="mermaid">
flowchart TD
    A[User selects payment] &#8211;> B[Payment Orchestrator]
    B &#8211;> C{Select Gateway}
    C &#8211;>|UPI| D[UPI Gateway]
    C &#8211;>|Card| E[Card Processor]
    C &#8211;>|Wallet| F[Wallet Service]
    C &#8211;>|Net Banking| G[Net Banking Gateway]
    D &#8211;> H{Payment Result}
    E &#8211;> H
    F &#8211;> H
    G &#8211;> H
    H &#8211;>|Success| I[Payment Capture]
    H &#8211;>|Failure| J[Retry Logic]
    H &#8211;>|Timeout| K[Async Status Check]
    I &#8211;> L[Booking Confirmation]
    J &#8211;>|Retry OK| I
    J &#8211;>|Max Retries| M[Payment Failed]
    K &#8211;>|Confirmed| I
    K &#8211;>|Declined| M
    M &#8211;> N[Release Seat Locks]
    L &#8211;> O[Ticket Generation]

    classDef orchestrator fill:#8E44AD,stroke:#6C3483,color:#fff
    classDef gateway fill:#2980B9,stroke:#1F618D,color:#fff
    classDef success fill:#27AE60,stroke:#1D8348,color:#fff
    classDef error fill:#E74C3C,stroke:#922B21,color:#fff
    classDef async fill:#F39C12,stroke:#B7770D,color:#fff

    class B orchestrator
    class D,E,F,G gateway
    class I,L,O success
    class M,N error
    class K async
</div>


<p>The payment orchestrator sits between the booking service and multiple payment gateways. Its job is to select the right gateway, handle retries, manage idempotency, and reconcile the final state.</p>

<p>Idempotency is critical. If a payment request is retried due to a network timeout, the gateway must recognize it as a duplicate and not charge the user twice. This is implemented using idempotency keys — a unique identifier generated per payment attempt that is passed to the gateway. Most modern gateways support this natively. The platform also maintains its own idempotency log to catch cases where the gateway does not.</p>

<p>The hardest failure scenarios are the ones where the outcome is ambiguous.</p>

<p>Payment timeout is the worst case. The user submits payment, the request goes to the gateway, and the response never comes back — either due to network failure or gateway timeout. At this point, the platform does not know if the payment went through. It starts an asynchronous status polling job that queries the gateway for the payment status. If the payment went through, it completes the booking. If it did not, it releases the seat locks. If the gateway never responds, the platform eventually sides with the user and releases the seats, then later reconciles if the payment did process.</p>

<p>Payment success but booking failure is another dangerous scenario. The payment gateway confirms success, but the booking service crashes before writing to the database. The user has been charged but has no ticket. The reconciliation system catches this by comparing payment records against booking records and triggering a re-booking or refund automatically.</p>

<p>Duplicate payment detection uses a combination of idempotency keys at the gateway level and deduplication logic in the payment service. Every payment event carries enough information to reconstruct the intent — user, booking ID, amount, method — and duplicate events are detected and discarded before they affect state.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Failure Scenario</th>
        <th>Detection</th>
        <th>Recovery</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Payment timeout (no response)</td>
        <td>Client timeout + async polling</td>
        <td>Poll gateway; confirm or release locks</td>
      </tr>
      <tr>
        <td>Payment success, booking write fails</td>
        <td>Reconciliation job</td>
        <td>Re-attempt booking or auto-refund</td>
      </tr>
      <tr>
        <td>Duplicate payment request</td>
        <td>Idempotency key at gateway</td>
        <td>Return original response, no double charge</td>
      </tr>
      <tr>
        <td>Partial refund failure</td>
        <td>Refund status monitoring</td>
        <td>Retry refund with exponential backoff</td>
      </tr>
      <tr>
        <td>Currency/amount mismatch</td>
        <td>Pre-payment validation</td>
        <td>Reject payment, show error to user</td>
      </tr>
    </tbody>
  </table>
</div>


<a name="Ticket.Generation.System"></a>
<h2>Ticket Generation System</h2>

<p>Once payment is confirmed and the booking is written to the database, ticket generation kicks off asynchronously. This is an event-driven process — the booking service publishes a <code>booking.confirmed</code> event, and the ticket service consumes it.</p>

<p>The ticket service creates a unique ticket record that combines the booking ID, show details, seat details, and user details into a tamper-proof representation. A QR code is generated from a hash of this data — typically including the booking ID and a secret salt — so that a scanner at the theatre can verify authenticity without an internet connection.</p>

<p>The QR code is embedded into a formatted PDF ticket, which is uploaded to object storage (something like S3). The ticket service then updates the booking record with the ticket URL, and the notification service delivers the ticket via email, SMS, and push notification.</p>

<p>Ticket uniqueness is enforced at multiple levels. The booking ID is the primary identifier. The QR code is cryptographically derived from the booking ID plus a server secret, making it impossible to forge. At the theatre, scanners validate QR codes either online (checking against the booking database) or offline (verifying the cryptographic signature). Offline validation is important for venues with unreliable connectivity.</p>

<a name="Notification.Infrastructure"></a>
<h2>Notification Infrastructure</h2>

<p>Notification is the last mile of the booking journey, and it has to be reliable even under high load. When ten thousand people book simultaneously during a flash sale, ten thousand confirmation messages need to go out.</p>

<p>The notification system is entirely asynchronous and event-driven. It consumes events from Kafka and routes them to the appropriate channels based on user preferences and event type.</p>

<div class="mermaid">
flowchart LR
    K[Kafka Events] &#8211;> NC[Notification Consumer]
    NC &#8211;> R{Route by Channel}
    R &#8211;> SMS[SMS Provider]
    R &#8211;> EM[Email Provider]
    R &#8211;> PN[Push Service]
    SMS &#8211;> D1[Delivered]
    EM &#8211;> D2[Delivered]
    PN &#8211;> D3[Delivered]
    D1 &#8211;> L[Delivery Log]
    D2 &#8211;> L
    D3 &#8211;> L

    classDef kafka fill:#E74C3C,stroke:#922B21,color:#fff
    classDef service fill:#2980B9,stroke:#1F618D,color:#fff
    classDef delivery fill:#27AE60,stroke:#1D8348,color:#fff

    class K kafka
    class NC,R service
    class SMS,EM,PN service
    class D1,D2,D3,L delivery
</div>


<p>SMS is the highest priority channel because it is universally accessible — even users without smartphones receive it. The platform integrates with SMS aggregators that have direct connections to telecom operators. Delivery rates and latency are monitored closely.</p>

<p>Email handles richer content — the full ticket PDF, event details, map links. Email is reliable but slower. For time-sensitive notifications (show cancellations, booking failures), email alone is not enough.</p>

<p>Push notifications reach users who have the app installed and have granted notification permission. They are great for reminders — two hours before showtime, for example — but unreliable as the sole delivery mechanism because devices can be offline.</p>

<p>The retry logic for notifications handles transient failures. If an SMS fails to deliver, the system retries with exponential backoff. After a configured number of retries, it falls back to an alternative channel. Delivery receipts are logged and surfaced in monitoring dashboards.</p>

<a name="Event-Driven.Architecture"></a>
<h2>Event-Driven Architecture</h2>

<p>The event-driven backbone is what holds the entire system together and makes it scalable. Nearly every major state transition in the platform generates an event, and those events drive downstream processing asynchronously.</p>

<p>The key insight is that not everything needs to happen synchronously in the booking request path. What the user needs synchronously is the lock confirmation and the booking confirmation. Everything else — ticket generation, notification delivery, analytics recording, reward point crediting — can happen after the booking is confirmed.</p>

<div class="mermaid">
flowchart TD
    BS[Booking Service] &#8211;>|booking.confirmed| K[Kafka]
    PS[Payment Service] &#8211;>|payment.success| K
    IS[Inventory Service] &#8211;>|inventory.updated| K
    K &#8211;>|booking.confirmed| TS[Ticket Service]
    K &#8211;>|booking.confirmed| NS[Notification Service]
    K &#8211;>|booking.confirmed| RS[Rewards Service]
    K &#8211;>|booking.confirmed| AN[Analytics]
    K &#8211;>|payment.success| BS2[Booking Completion]
    K &#8211;>|inventory.updated| SS[Search Index Updater]

    classDef producer fill:#8E44AD,stroke:#6C3483,color:#fff
    classDef kafka fill:#E74C3C,stroke:#922B21,color:#fff
    classDef consumer fill:#27AE60,stroke:#1D8348,color:#fff

    class BS,PS,IS producer
    class K kafka
    class TS,NS,RS,AN,BS2,SS consumer
</div>


<p>Kafka is the standard choice for this kind of event streaming because it provides durable, ordered, replayable event logs. If the notification service goes down, it does not lose events — it picks up from where it left off when it comes back. If a new downstream consumer needs to be added (say, a new loyalty program), it can replay historical events without any changes to the upstream services.</p>

<p>The event schema design is important. Events should be self-contained — enough information to process without additional lookups wherever possible. A <code>booking.confirmed</code> event should carry the booking ID, user ID, show ID, seat list, payment amount, and timestamp. If the notification service has to make additional API calls to get this information, you have created hidden coupling and additional failure points.</p>

<p>Dead letter queues handle poison pill events — events that consistently fail processing. Instead of blocking the entire consumer, failing events are routed to a separate queue for investigation and manual replay.</p>

<a name="Database.Design"></a>
<h2>Database Design</h2>

<p>The data model reflects the domain hierarchy clearly. Here is a simplified version of the core schemas:</p>

<pre><code class="sql">-- Core entities

CREATE TABLE movies (
  id           BIGSERIAL PRIMARY KEY,
  title        VARCHAR(255) NOT NULL,
  language     VARCHAR(50),
  genre        VARCHAR(100),
  duration_min INTEGER,
  release_date DATE,
  rating       VARCHAR(10),
  created_at   TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE theatres (
  id        BIGSERIAL PRIMARY KEY,
  name      VARCHAR(255) NOT NULL,
  city      VARCHAR(100) NOT NULL,
  address   TEXT,
  lat       DECIMAL(9,6),
  lon       DECIMAL(9,6)
);

CREATE TABLE screens (
  id          BIGSERIAL PRIMARY KEY,
  theatre_id  BIGINT REFERENCES theatres(id),
  name        VARCHAR(50),
  total_seats INTEGER
);

CREATE TABLE shows (
  id          BIGSERIAL PRIMARY KEY,
  movie_id    BIGINT REFERENCES movies(id),
  screen_id   BIGINT REFERENCES screens(id),
  start_time  TIMESTAMPTZ NOT NULL,
  language    VARCHAR(50),
  format      VARCHAR(20) -- 2D, 3D, IMAX
);

CREATE TABLE seats (
  id        BIGSERIAL PRIMARY KEY,
  screen_id BIGINT REFERENCES screens(id),
  row_label VARCHAR(5),
  seat_num  INTEGER,
  category  VARCHAR(20) -- REGULAR, PREMIUM, RECLINER
);

CREATE TABLE seat_inventory (
  id         BIGSERIAL PRIMARY KEY,
  show_id    BIGINT REFERENCES shows(id),
  seat_id    BIGINT REFERENCES seats(id),
  status     VARCHAR(20) DEFAULT 'AVAILABLE', -- AVAILABLE, LOCKED, BOOKED
  locked_by  VARCHAR(100),
  lock_exp   TIMESTAMPTZ,
  booking_id BIGINT,
  UNIQUE (show_id, seat_id)
);

CREATE TABLE bookings (
  id           BIGSERIAL PRIMARY KEY,
  user_id      BIGINT NOT NULL,
  show_id      BIGINT REFERENCES shows(id),
  total_amount DECIMAL(10,2),
  status       VARCHAR(20), -- PENDING, CONFIRMED, CANCELLED
  created_at   TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE payments (
  id             BIGSERIAL PRIMARY KEY,
  booking_id     BIGINT REFERENCES bookings(id),
  amount         DECIMAL(10,2),
  method         VARCHAR(50),
  gateway        VARCHAR(50),
  gateway_ref    VARCHAR(255),
  status         VARCHAR(20), -- INITIATED, SUCCESS, FAILED, REFUNDED
  idempotency_key VARCHAR(255) UNIQUE,
  created_at     TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE tickets (
  id         BIGSERIAL PRIMARY KEY,
  booking_id BIGINT REFERENCES bookings(id),
  qr_hash    VARCHAR(255) UNIQUE,
  ticket_url TEXT,
  issued_at  TIMESTAMPTZ DEFAULT now()
);
</code></pre>

<p>Indexing is critical for performance. The most commonly queried paths are: find shows for a movie in a city, find available seats for a show, find bookings by user. Each of these needs appropriate composite indexes.</p>

<pre><code class="sql">CREATE INDEX idx_shows_movie_city ON shows(movie_id) 
  INCLUDE (screen_id, start_time);

CREATE INDEX idx_seat_inventory_show ON seat_inventory(show_id, status)
  WHERE status = 'AVAILABLE';

CREATE INDEX idx_bookings_user ON bookings(user_id, created_at DESC);
</code></pre>

<p>The <code>seat_inventory</code> table is particularly hot during peak loads. It is read thousands of times per second and written frequently. Partitioning it by <code>show_id</code> or by date reduces the working set and improves cache hit rates.</p>

<p>For very high-traffic shows, it is worth considering moving inventory management for that specific show to a dedicated shard — essentially giving a blockbuster show its own database slice for the booking period.</p>

<a name="Caching.Systems"></a>
<h2>Caching Systems</h2>

<p>Caching is not an optimization in a system like this. It is a fundamental architectural requirement. Without aggressive caching, the platform would fall over on any moderately popular movie release.</p>

<p>The caching hierarchy has multiple layers.</p>

<p>The CDN layer handles static assets and semi-static content — movie posters, theatre logos, and even JSON API responses for movie listings that change infrequently. CDN caching dramatically reduces the load that reaches your origin servers.</p>

<p>The application cache (Redis) handles more dynamic data. The movie catalog for a given city is cached for several minutes. Theatre schedules are cached for a few minutes. Show availability (the aggregate count of available seats for a show) is cached for 30–60 seconds.</p>

<p>The most important caching insight is around seat map data. The seat map for a popular show is requested thousands of times per second when booking opens. If every request hits the database, you are in trouble. The solution is to cache the seat map aggressively and use event-driven invalidation — when a seat state changes, you publish an invalidation event, and the cache is refreshed from the database.</p>

<p>But you do not want perfect freshness in the seat map cache. A few seconds of staleness is acceptable — the real enforcement happens at lock acquisition time. The seat map shows users an approximate view; the lock is what actually serializes access.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Data Type</th>
        <th>Cache Layer</th>
        <th>TTL</th>
        <th>Invalidation Strategy</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Movie metadata</td>
        <td>CDN + Redis</td>
        <td>1–24 hours</td>
        <td>On catalog update</td>
      </tr>
      <tr>
        <td>Theatre schedules</td>
        <td>Redis</td>
        <td>5–15 minutes</td>
        <td>On schedule sync</td>
      </tr>
      <tr>
        <td>Show availability count</td>
        <td>Redis</td>
        <td>30–60 seconds</td>
        <td>On seat state change</td>
      </tr>
      <tr>
        <td>Seat map snapshot</td>
        <td>Redis</td>
        <td>10–30 seconds</td>
        <td>On any seat lock or booking</td>
      </tr>
      <tr>
        <td>User session and lock</td>
        <td>Redis</td>
        <td>10 minutes (TTL)</td>
        <td>Auto-expire</td>
      </tr>
    </tbody>
  </table>
</div>


<p>Hotspot management is a specific problem during blockbuster releases. When a single show is being booked by a hundred thousand users simultaneously, even a cached seat map can become a thundering herd — every request refreshes the cache at the same time when it expires. The solution is staggered TTLs (each cache entry gets a random TTL jitter) and cache stampede prevention using techniques like probabilistic early expiration, where a single request is allowed to refresh the cache slightly before it expires.</p>

<a name="Scalability.Deep.Dive"></a>
<h2>Scalability Deep Dive</h2>

<p>The platform must handle orders-of-magnitude traffic spikes with minimal preparation time. IPL finals tickets have gone on sale and been fully booked within 30 seconds. Major movie releases like RRR or Jawan created booking surges that stress-tested every component in the system.</p>

<p>Horizontal scaling handles most of the compute-side problem. All the stateless services — movie catalog, search, booking — can be deployed on Kubernetes and scaled out by adding more pods. Kubernetes Horizontal Pod Autoscaler watches CPU and memory metrics and spins up pods within minutes.</p>

<p>But scaling the stateful parts is harder. The database cannot be scaled out trivially. The seat lock service in Redis cannot easily be distributed without coordination overhead.</p>

<p>For database scaling, the typical approach is read replicas for read-heavy queries (movie catalog, search) and connection pooling (PgBouncer) to prevent connection exhaustion. For write-heavy tables (seat_inventory, bookings during peak), you might use application-level sharding by show_id or partition by date.</p>

<p>For the seat lock service, Redis Cluster provides horizontal scaling. Seat locks for different shows are naturally distributed across different Redis nodes, because the key space (show-specific seat keys) is not shared. This makes horizontal scaling of the lock service relatively straightforward.</p>

<p>The real scaling challenge during IPL or blockbuster events is the booking service itself — the service that takes payment and writes the confirmed booking. This is a write-heavy, strongly-consistent workload. The approach is to pre-provision extra capacity ahead of known high-traffic events, use a queue-based buffer in front of the booking writer (so you can absorb bursts without dropping requests), and implement graceful degradation (if the booking service is near capacity, introduce a virtual queue UX so users wait their turn rather than seeing errors).</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Component</th>
        <th>Scaling Technique</th>
        <th>Bottleneck Risk</th>
        <th>Mitigation</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Movie Catalog</td>
        <td>CDN + horizontal pod scale</td>
        <td>Low</td>
        <td>Aggressive caching</td>
      </tr>
      <tr>
        <td>Search</td>
        <td>Elasticsearch cluster scale-out</td>
        <td>Medium</td>
        <td>Read replicas, result caching</td>
      </tr>
      <tr>
        <td>Seat Lock (Redis)</td>
        <td>Redis Cluster by key space</td>
        <td>Medium at extreme peaks</td>
        <td>Shard by show_id</td>
      </tr>
      <tr>
        <td>Booking Writer</td>
        <td>Queue buffering + pre-provisioning</td>
        <td>High during flash sales</td>
        <td>Virtual queue, DB sharding</td>
      </tr>
      <tr>
        <td>Payment Orchestrator</td>
        <td>Horizontal pod scale</td>
        <td>Medium (gateway limits)</td>
        <td>Multiple gateways, circuit breakers</td>
      </tr>
      <tr>
        <td>Notification</td>
        <td>Kafka consumer scale-out</td>
        <td>Low</td>
        <td>Partition parallelism</td>
      </tr>
    </tbody>
  </table>
</div>


<p>Virtual queuing during peak events deserves more discussion because it is an important UX and engineering decision. Instead of letting all users simultaneously attempt booking (which overwhelms the system and leads to errors), you funnel them through a virtual queue — assign each user a position, let them through in batches, and give them a reliable estimate of their wait time. This converts a frustrating &ldquo;server error&rdquo; experience into a managed wait, and it lets the backend operate within its designed throughput.</p>

<a name="Reliability.and.Availability"></a>
<h2>Reliability and Availability</h2>

<p>Reliability is about what happens when things break. And in a distributed system at this scale, things break regularly.</p>

<p>The payment service is the most critical single point of failure. A payment service outage means no new bookings can complete. The mitigation is multiple payment gateway integrations — if one gateway is down, the orchestrator automatically routes to a fallback. Circuit breakers prevent hammering a failing gateway and give it time to recover.</p>

<p>The database has primary-replica failover with automatic promotion. If the primary fails, a replica is promoted within seconds. This causes a brief write outage but preserves the integrity of all committed data.</p>

<p>Redis failures are handled by Redis Sentinel or Cluster, which provides automatic failover. But a Redis failure during a booking creates a specific problem — all seat locks are gone. The recovery strategy here is to accept that the lock state is lost and let the locks expire naturally from the database side (you maintain a database-level lock record as a secondary source of truth, updated less frequently). During the recovery window, you run the system in a more conservative mode — tighter lock durations, no new lock acquisition until Redis is recovered.</p>

<p>Monitoring and alerting are not afterthoughts. The platform runs dashboards for booking throughput, seat lock acquisition success rate, payment success rate, notification delivery rate, and database replication lag. Anomaly detection fires alerts when any of these metrics deviate significantly from baseline.</p>

<a name="Security.Considerations"></a>
<h2>Security Considerations</h2>

<p>Ticket scalping through automated bots is a persistent problem. The platform uses several layers of defense. Rate limiting at the API Gateway level caps the number of booking requests per IP and per user account per minute. CAPTCHA challenges are triggered when behavioral signals suggest automation — unusually fast seat selection, repeated identical requests, IP address patterns matching known bot farms.</p>

<p>Payment security follows PCI-DSS compliance requirements. Card data is tokenized — the platform never stores raw card numbers. All payment communication uses TLS. Fraud detection runs real-time checks on payment attempts, flagging unusual patterns like a new account with no history making a high-value booking, or the same card being used across many different accounts in a short window.</p>

<p>Authentication uses short-lived JWTs with refresh token rotation. Sensitive operations like booking confirmation and refund initiation require re-authentication or OTP verification.</p>

<a name="Engineering.Tradeoffs"></a>
<h2>Engineering Tradeoffs</h2>

<p>Every architectural decision in this system involves a tradeoff, and being explicit about those tradeoffs is what separates good engineers from great ones.</p>

<p>Seat lock duration is a tradeoff between user experience and inventory efficiency. A longer lock gives users more time to complete checkout, reducing frustration. But it also ties up inventory longer, reducing availability for other users. The optimal value depends on your checkout conversion funnel — if 60% of users abandon after locking seats, shorter locks make sense. If most users who lock do complete, longer locks are fine.</p>

<p>Caching versus freshness is a constant tension. A stale seat map might show a seat as available that is actually locked or booked. This leads to failed lock acquisitions — the user sees a seat, selects it, and then gets an error. Too aggressive caching creates a poor experience. Too little caching means the database cannot handle the load. The right answer is layers of caching with different TTLs and relying on the lock layer for correctness rather than the display layer.</p>

<p>Consistency versus availability in the seat inventory is perhaps the most fundamental tradeoff in the whole system. During a network partition or a database failover, you have to choose: do you keep the system available for bookings (risking double-booking) or do you stop accepting bookings until consistency is restored (risking revenue loss)? Most production ticketing platforms choose consistency — they would rather lose bookings than create double-booking incidents, which require manual resolution and damage trust far more.</p>

<a name="Technology.Stack"></a>
<h2>Technology Stack</h2>

<p>The technology choices for a platform like this are driven by specific performance and operational requirements.</p>

<p>Java or Go for core services is a common choice. Java with Spring Boot offers a mature ecosystem for building distributed services, excellent database drivers, and strong observability tooling. Go offers lower memory overhead and faster startup times, which matters for services that scale in and out frequently.</p>

<p>PostgreSQL is the primary relational store for bookings, inventory, and payments. It offers strong transactional guarantees, excellent JSON support for semi-structured data, and mature tooling for replication and failover.</p>

<p>Redis serves as the seat locking layer and primary application cache. Its single-threaded execution model makes it safe for concurrent lock operations, and its native TTL support handles lock expiry cleanly.</p>

<p>Kafka is the messaging backbone. Its durable log model means events are never lost, consumers can replay events, and multiple consumers can independently process the same event stream without coordination.</p>

<p>Elasticsearch powers search and discovery. It handles fuzzy matching, geospatial queries (find theatres near me), and relevance ranking out of the box.</p>

<p>Kubernetes provides the container orchestration layer — health checks, auto-scaling, rolling deployments, and resource isolation between services.</p>

<a name="System.Design.Interview.Perspective"></a>
<h2>System Design Interview Perspective</h2>

<p>BookMyShow is a classic system design interview question, and interviewers use it specifically because it surfaces knowledge of distributed systems, inventory management, and concurrency control.</p>

<p>The most common mistake candidates make is jumping straight to a generic CRUD architecture without discussing the hard parts. Any entry-level engineer can describe a movies table and a bookings table. What separates a strong candidate is the discussion of seat locking, concurrent booking prevention, and payment consistency.</p>

<p>Strong candidates ask clarifying questions first. What is the expected scale? Millions of daily users or thousands? What is the peak traffic scenario? Are we designing for a single city or the full platform? These questions signal that you understand the tradeoffs are scale-dependent.</p>

<p>The seat locking discussion is where candidates reveal their depth. The progression from &ldquo;mark the seat as taken in the database&rdquo; to &ldquo;use SELECT FOR UPDATE with a transaction&rdquo; to &ldquo;move the lock to Redis for throughput&rdquo; to &ldquo;use Redis SET NX EX for atomic lock with auto-expiry&rdquo; demonstrates increasing sophistication at each step. Interviewers know this path and reward candidates who can articulate why each step improves on the last.</p>

<p>Payment consistency is another differentiating topic. Explaining the payment-success-booking-failure scenario and proposing idempotency keys, event sourcing, and reconciliation jobs signals real production experience.</p>

<p>Candidates should also be ready to discuss how they would handle a sudden 100x traffic spike. The answer should touch on auto-scaling compute, read replicas for the database, Redis Cluster for the lock layer, Kafka-based queue buffering for the booking writer, and CDN caching for static content. Mentioning virtual queuing as a UX mitigation for extreme spikes shows architectural thinking beyond pure backend concerns.</p>

<p>Common weak answers include: &ldquo;We can cache everything,&rdquo; without explaining what happens to consistency. &ldquo;We can scale horizontally,&rdquo; without specifying which components and how. &ldquo;We use a distributed database,&rdquo; without discussing the consistency tradeoffs of that choice.</p>

<p>The strongest answers treat the system as a collection of sub-problems, each with its own requirements, and demonstrate awareness that the solutions to those sub-problems interact — that your caching decisions affect your consistency guarantees, that your lock timeout affects your inventory turnover rate, that your payment retry logic must be idempotent or you risk charging users twice.</p>

<a name="Closing.Thoughts"></a>
<h2>Closing Thoughts</h2>

<p>Building a production-grade ticketing platform is a masterclass in distributed systems engineering. Every component that looks simple on the surface hides significant complexity underneath.</p>

<p>The seat locking problem is really a distributed mutual exclusion problem. The payment consistency problem is really a distributed transaction problem. The inventory synchronization problem with theatre partners is really a distributed state reconciliation problem. The flash-sale scaling problem is really a resource contention problem at extreme concurrency.</p>

<p>What makes systems like BookMyShow interesting is not any single clever algorithm or architecture pattern. It is the way multiple hard problems interact with each other and must be solved coherently. A solution that handles seat locking correctly but breaks payment idempotency is not a solution. A system that scales brilliantly but allows double bookings has failed at its core purpose.</p>

<p>The best engineers working on these systems develop a habit of asking &ldquo;what happens when this fails?&rdquo; at every step. They design for the failure mode, not just the happy path. And they understand that the right answer to almost every architectural question is &ldquo;it depends&rdquo; — on your scale, your consistency requirements, your user experience priorities, and the specific failure modes you are most afraid of.</p>

<p>If you are preparing for a system design interview, do not memorize the architecture. Understand the problems it solves and why each design decision addresses a specific constraint. That understanding is what interviewers are probing for, and it is also what makes you a better engineer in practice.</p>
]]></content>
    </entry>
    
    <entry>
        <title type="html"><![CDATA[How Indigo Booking Works?]]></title>
        <link href="https://rishijeet.github.io/blog/how-indigo-booking-works/"/>
        <updated>2026-05-31T12:02:55+05:30</updated>
        <id>https://rishijeet.github.io/blog/how-indigo-booking-works</id>
        <content type="html"><![CDATA[<blockquote><p><em>IndiGo is India&rsquo;s largest airline by market share, operating hundreds of flights daily across domestic and international routes. If you&rsquo;ve ever booked a flight on goindigo.in or the IndiGo app, you interacted with one of the most sophisticated transactional systems in the country — a system that has to handle millions of searches, coordinate real-time inventory across thousands of flights, process payments securely, and issue legally valid tickets, all within a few seconds.</em></p></blockquote>

<p><img src="/images/system_design/indigo-banner.jpg" height="300" width="900" alt="Alt text" /></p>

<p>Airline reservation systems are not just booking systems. They are distributed systems problems at scale, with real money, real people, and legally binding contracts involved. A failed payment that results in a confirmed seat is a liability. A double-booked seat is a disaster. A slow search experience sends users to a competitor. The stakes are incredibly high.</p>

<p>What makes airline booking particularly hard compared to, say, an e-commerce system? In e-commerce, you can slightly oversell and manage the backorder. In airline booking, there is no backorder. If seat 14A on flight 6E-205 departing Mumbai at 8:30 AM is sold, it is sold. You cannot manufacture another 14A. Inventory is finite, time-bounded, and irreplaceable.</p>

<!--more-->


<p>Add to this the following complexity: prices change every few minutes based on demand, remaining seats, time to departure, competitor pricing, and internal revenue management algorithms. Two users searching the same route at the same time may see different prices. A seat that showed available when you started the booking flow may be gone by the time you hit confirm. And the system must handle all of this consistently, at scale, with zero tolerance for double bookings.</p>

<p>This is why airline reservation systems sit alongside banking and healthcare as the most critical transactional systems in the software world.</p>

<a name="Core.Components.of.an.Airline.Booking.Platform"></a>
<h2>Core Components of an Airline Booking Platform</h2>

<p>Before diving into each component, it helps to understand what the full system looks like from a bird&rsquo;s eye view.</p>

<p>The <strong>Flight Search Service</strong> handles all user queries about available flights between origin and destination pairs across specific dates. It is read-heavy and latency-sensitive.</p>

<p>The <strong>Fare Engine</strong> calculates the price a specific user should see for a specific flight at a specific point in time. This is not a simple lookup. It involves revenue management rules, demand signals, fare class availability, taxes, surcharges, and promotional logic.</p>

<p>The <strong>Seat Inventory Service</strong> tracks exactly how many seats are available, held, reserved, or sold across every flight in the network. This is the most write-contended component in the system.</p>

<p>The <strong>Reservation Service</strong> orchestrates the process of temporarily locking a seat, coordinating payment, and confirming the booking. It manages the lifecycle from &ldquo;user clicked book&rdquo; to &ldquo;ticket issued.&rdquo;</p>

<p>The <strong>Payment Service</strong> handles the actual money movement — authorization, capture, refund, and reconciliation — across UPI, credit cards, debit cards, net banking, and wallets.</p>

<p>The <strong>Ticketing Service</strong> generates the official e-ticket after payment is confirmed. The ticket is a legally recognized travel document.</p>

<p>The <strong>PNR Generation System</strong> creates the unique booking reference that ties together the passenger, flight, seat, and payment into a single retrievable record.</p>

<p>The <strong>Check-In System</strong> manages the online and airport check-in process, including boarding pass generation and seat finalization.</p>

<p>The <strong>Notification Service</strong> delivers booking confirmations, reminders, boarding alerts, and delay notifications via SMS, email, and push notifications.</p>

<p>The <strong>Loyalty and Ancillary Systems</strong> handle frequent flyer points, baggage add-ons, meal preferences, and other revenue-generating services.</p>

<a name="High-Level.Booking.Architecture"></a>
<h2>High-Level Booking Architecture</h2>

<p>When you open the IndiGo app and search for a flight, your request travels through multiple services before you see results. Here is a simplified view of the overall architecture:</p>

<div class="mermaid">
graph TD
  A[Mobile App] &#8211;> B[API Gateway]
  C[Web App] &#8211;> B
  B &#8211;> D[Flight Search Service]
  B &#8211;> E[Pricing Engine]
  B &#8211;> F[Inventory Service]
  B &#8211;> G[Reservation Service]
  B &#8211;> H[Payment Service]
  B &#8211;> I[Ticketing Service]
  B &#8211;> J[Notification Service]
  D &#8211;> K[Search Index]
  D &#8211;> F
  E &#8211;> L[Fare Rules DB]
  E &#8211;> F
  F &#8211;> M[Inventory DB]
  G &#8211;> F
  G &#8211;> H
  H &#8211;> N[Payment Gateway]
  I &#8211;> O[Ticket Store]
  G &#8211;> I
  I &#8211;> J

  classDef client fill:#1a73e8,color:#fff,stroke:none
  classDef gateway fill:#0d47a1,color:#fff,stroke:none
  classDef service fill:#1565c0,color:#fff,stroke:none
  classDef store fill:#0a3d62,color:#fff,stroke:none

  class A,C client
  class B gateway
  class D,E,F,G,H,I,J service
  class K,L,M,N,O store
</div>


<p>The API Gateway is the entry point for all client traffic. It handles authentication, rate limiting, SSL termination, and request routing. Behind it, each service is independently deployable and independently scalable. This separation is intentional — the search service handles dramatically more traffic than the payment service, so they need to scale differently.</p>

<a name="Flight.Search.System.Deep.Dive"></a>
<h2>Flight Search System Deep Dive</h2>

<p>Search is the highest volume operation in the entire booking platform. For every booking that completes, there are hundreds of searches. Users search, compare, abandon, search again, and eventually book. This means the search system must be extremely fast, extremely available, and must not create write load on the inventory system.</p>

<p>When you search for a flight from Delhi to Mumbai on a specific date, here is what actually happens:</p>

<p>The search service first checks a distributed cache (typically Redis or Memcached) for a pre-computed result set for that origin-destination-date combination. Popular routes like DEL-BOM are searched thousands of times per hour. Pre-computing and caching these results means the database is not hit for every search request.</p>

<p>If the cache misses, the system queries an Elasticsearch or Solr-based search index that contains flight schedules, availability buckets, and price ranges. This index is periodically refreshed from the inventory system but is not a real-time view. The goal of search is to give the user a fast, accurate-enough result, not a millisecond-precise inventory snapshot.</p>

<p>Why not query live inventory for every search? Because inventory is stored in a transactional database optimized for writes and consistency. Hitting that database with millions of search queries simultaneously would destroy its performance and create lock contention that slows down actual bookings.</p>

<p>The search index is updated asynchronously. When inventory changes — a seat is booked, a hold expires, a fare bucket closes — an event is published to a message queue. Search index consumers read from this queue and update the index with a short lag, typically a few seconds. This means search results might not be perfectly real-time, but they are fast, scalable, and close enough for a user browsing flights.</p>

<p>Search results are returned with available seat counts and price ranges. The exact price is calculated only when a user selects a specific flight, because fare calculation is more expensive than a search index lookup.</p>

<a name="Seat.Inventory.Management"></a>
<h2>Seat Inventory Management</h2>

<p>This is arguably the most complex component in the entire airline system. Understanding seat inventory requires understanding how airlines think about their product.</p>

<p>An airline does not just have seats. It has fare classes. Fare classes are subdivisions of the cabin that allow the airline to sell the same physical seat at different prices to different customers. On a typical IndiGo flight, you might have fare classes like S, T, V, Q, L, K, M, B, H, Y — each representing a different price point and set of rules.</p>

<p>The inventory system tracks how many seats are available within each fare class on each flight. This is called a fare bucket. When you buy a cheap ticket, you are buying from a low-priced fare bucket. When that bucket empties, the cheapest available price jumps to the next bucket. This is why flight prices increase as you approach the travel date and as more seats are sold.</p>

<p>Here is a simplified view of what an inventory record looks like:</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Fare Class</th>
        <th>Total Seats</th>
        <th>Available</th>
        <th>Held</th>
        <th>Sold</th>
        <th>Price Range</th>
      </tr>
    </thead>
    <tbody>
      <tr><td>S</td><td>4</td><td>0</td><td>0</td><td>4</td><td>Lowest</td></tr>
      <tr><td>T</td><td>6</td><td>0</td><td>0</td><td>6</td><td>Low</td></tr>
      <tr><td>V</td><td>8</td><td>2</td><td>1</td><td>5</td><td>Medium-Low</td></tr>
      <tr><td>Q</td><td>10</td><td>7</td><td>0</td><td>3</td><td>Medium</td></tr>
      <tr><td>Y</td><td>20</td><td>18</td><td>0</td><td>2</td><td>Highest</td></tr>
    </tbody>
  </table>
</div>


<p>The hardest problem in inventory management is race conditions. Imagine two users simultaneously trying to book the last seat in fare class V on the same flight. Both query inventory, both see one seat available, both initiate the booking flow. Without proper concurrency control, both bookings could complete, resulting in an oversold seat.</p>

<p>Airlines solve this with a technique called seat locking or inventory holding. When a user selects a flight and starts the booking flow, the system places a soft hold on inventory: it decrements the available count and increments the held count. This hold is temporary — typically 10 to 15 minutes. If payment is not completed within that window, the hold expires and inventory is restored.</p>

<p>Implementing this correctly in a distributed system is non-trivial. The inventory service cannot simply do a read-then-write in two separate database operations, because another process could read and modify the same row between those two operations. The update must be atomic.</p>

<p>A typical implementation uses optimistic locking with database-level compare-and-swap operations. The inventory update includes a version check: &ldquo;Update inventory where available_seats = 1 and version = 42, set available_seats = 0, held_seats = 1, version = 43.&rdquo; If another transaction has already modified this row, the version check fails, and the system retries or rejects the request.</p>

<p>For high-throughput scenarios, some systems use pessimistic locking with SELECT FOR UPDATE, which acquires a row-level lock before reading. This prevents concurrent modifications but reduces write throughput on heavily contended flights.</p>

<p>Another approach is to move inventory management to an in-memory system like Redis and use Redis&rsquo;s atomic operations (DECR, WATCH/MULTI/EXEC) to manage seat counts. This provides much higher throughput than a relational database for hot inventory, with the relational database serving as the durable source of truth that Redis is synchronized with.</p>

<a name="Dynamic.Fare.Calculation.Engine"></a>
<h2>Dynamic Fare Calculation Engine</h2>

<p>The fare engine is where airline economics becomes visible in the system. Understanding why it works the way it does requires understanding the concept of revenue management.</p>

<p>Airlines have a fixed inventory that expires at a specific time. An empty seat on a departed flight generates zero revenue. The goal of revenue management is to maximize total revenue across all seats by selling the right seat to the right customer at the right price at the right time.</p>

<p>This is why prices are dynamic. Early in the booking window, when a flight has many empty seats, the airline offers low prices to stimulate demand and fill the plane. As the flight fills up and the departure date approaches, prices rise because the remaining seats are scarce and late-booking travelers tend to be less price-sensitive business travelers who value flexibility.</p>

<p>The fare calculation engine takes multiple inputs:</p>

<p>The base fare for the route, which reflects operating costs including fuel, crew, airport fees, and overhead. The fare class the user is eligible for, which depends on inventory availability at query time. Taxes and surcharges, which include government-mandated passenger service fees, fuel surcharges, and airport development fees. These are not discretionary — they must be applied accurately.</p>

<p>On top of the base fare and taxes, the engine applies modifiers. A promotional code might apply a discount. Booking far in advance might unlock an early-bird fare. Booking for a group might apply group pricing rules. A user with IndiGo&rsquo;s frequent flyer status might see a different fare structure.</p>

<p>The reason two users see different prices is often because they query at slightly different times, with different inventory states. User A queries and sees 3 seats available in fare class V. User B queries 30 seconds later, after those 3 seats have been placed on hold by other users, and now sees only Q class available, which is more expensive.</p>

<p>The fare engine itself is typically stateless — it takes inputs (flight, date, fare class, passenger type, applied promotions) and returns a price. State is managed by the inventory system. This makes the fare engine horizontally scalable. You can run dozens of instances behind a load balancer.</p>

<a name="Reservation.System.Deep.Dive"></a>
<h2>Reservation System Deep Dive</h2>

<p>The reservation system orchestrates the most critical workflow in the platform: converting a user&rsquo;s intent to buy into a confirmed booking.</p>

<p>The reservation lifecycle has several distinct states:</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>State</th>
        <th>Description</th>
        <th>Duration</th>
        <th>Trigger</th>
      </tr>
    </thead>
    <tbody>
      <tr><td>INITIATED</td><td>User begins booking flow</td><td>Seconds</td><td>User selects flight</td></tr>
      <tr><td>HELD</td><td>Inventory soft-locked</td><td>10-15 min</td><td>Passenger details submitted</td></tr>
      <tr><td>PENDING_PAYMENT</td><td>Awaiting payment confirmation</td><td>5-10 min</td><td>Payment initiated</td></tr>
      <tr><td>PAYMENT_CONFIRMED</td><td>Payment authorized</td><td>Seconds</td><td>Payment gateway callback</td></tr>
      <tr><td>CONFIRMED</td><td>Booking complete, PNR issued</td><td>Permanent</td><td>Ticket generated</td></tr>
      <tr><td>CANCELLED</td><td>Booking voided</td><td>Permanent</td><td>User or system action</td></tr>
      <tr><td>EXPIRED</td><td>Hold timed out</td><td>Permanent</td><td>TTL expiry</td></tr>
    </tbody>
  </table>
</div>


<p>The transition from HELD to CONFIRMED involves a distributed transaction across the inventory service, payment service, and ticketing service. This is where things get architecturally challenging.</p>

<p>In a traditional database world, you would wrap these operations in a single ACID transaction. But in a microservices architecture, each service has its own database. You cannot span a database transaction across service boundaries.</p>

<p>Airlines solve this with the Saga pattern. A saga is a sequence of local transactions, each with a compensating transaction that can undo it if a later step fails. The booking saga looks like this:</p>

<ul>
<li>Step 1: Lock inventory (compensating action: release lock).</li>
<li>Step 2: Charge payment (compensating action: refund).</li>
<li>Step 3: Generate ticket (compensating action: void ticket).</li>
<li>Step 4: Issue PNR (compensating action: cancel PNR).</li>
</ul>


<p>If payment succeeds but ticket generation fails, the saga triggers the compensating actions in reverse order: the payment is refunded, the inventory lock is released. This ensures the system reaches a consistent state even when individual steps fail.</p>

<p>The saga can be implemented in two ways. Choreography-based sagas use events: each service publishes an event after completing its step, and the next service listens for that event. Orchestration-based sagas use a central orchestrator service that explicitly calls each step and handles failures. For complex booking workflows, orchestration tends to be easier to reason about and debug.</p>

<a name="Booking.Workflow"></a>
<h2>Booking Workflow</h2>

<p>Let us walk through the complete end-to-end flow, including what happens at each step and what can go wrong.</p>

<div class="mermaid">
graph TD
  A[User Searches Flight] &#8211;> B[Search Service returns results]
  B &#8211;> C[User selects fare]
  C &#8211;> D[Fare Engine calculates price]
  D &#8211;> E[User enters passenger details]
  E &#8211;> F[Inventory Service places hold]
  F &#8211;> G{Hold successful?}
  G &#8211;>|No| H[Show seat unavailable]
  G &#8211;>|Yes| I[User initiates payment]
  I &#8211;> J[Payment Service calls gateway]
  J &#8211;> K{Payment authorized?}
  K &#8211;>|No| L[Release hold and show failure]
  K &#8211;>|Yes| M[Ticketing Service generates e-ticket]
  M &#8211;> N[PNR Generation Service creates PNR]
  N &#8211;> O[Notification Service sends confirmation]
  O &#8211;> P[Booking Complete]

  classDef step fill:#1565c0,color:#fff,stroke:none
  classDef decision fill:#0d47a1,color:#fff,stroke:none
  classDef terminal fill:#0a3d62,color:#fff,stroke:none
  classDef failure fill:#b71c1c,color:#fff,stroke:none

  class A,B,C,D,E,F,I,J,M,N,O step
  class G,K decision
  class P terminal
  class H,L failure
</div>


<p>When the user submits passenger details, the reservation service immediately tries to place a soft hold on inventory. This is the first critical moment. If the hold fails — because another user got there first — the system surfaces a friendly &ldquo;seat no longer available&rdquo; message and sends the user back to search results.</p>

<p>If the hold succeeds, the user is given a time window to complete payment. A countdown timer in the UI reflects the hold expiry. On the backend, a scheduled job monitors held reservations and releases those that expire without payment.</p>

<p>After payment, the system receives a callback from the payment gateway. This callback must be processed idempotently — it could arrive more than once if the network is unreliable. The payment service records the payment authorization ID from the gateway and checks whether it has already processed a booking for this authorization ID before taking action.</p>

<p>Once payment is confirmed, the reservation transitions to PAYMENT_CONFIRMED and the ticketing workflow begins.</p>

<a name="Payment.Infrastructure"></a>
<h2>Payment Infrastructure</h2>

<p>Payment is where real money moves, and where the consequences of failures are most visible to users. IndiGo&rsquo;s payment infrastructure must handle a wide variety of payment methods with varying reliability characteristics.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Payment Method</th>
        <th>Latency</th>
        <th>Reliability</th>
        <th>Failure Mode</th>
      </tr>
    </thead>
    <tbody>
      <tr><td>UPI</td><td>1-3 seconds</td><td>High</td><td>Bank timeout, user rejection</td></tr>
      <tr><td>Credit Card</td><td>2-5 seconds</td><td>High</td><td>Decline, 3DS failure</td></tr>
      <tr><td>Debit Card</td><td>2-5 seconds</td><td>Medium</td><td>Insufficient funds, bank down</td></tr>
      <tr><td>Net Banking</td><td>5-30 seconds</td><td>Medium</td><td>Bank redirect failure</td></tr>
      <tr><td>Wallet</td><td>1-2 seconds</td><td>High</td><td>Insufficient balance</td></tr>
    </tbody>
  </table>
</div>


<p>The payment service does not directly integrate with every bank and wallet. It integrates with a payment aggregator (like Razorpay, PayU, or Cashfree in the Indian context) that handles the downstream integrations. The aggregator provides a unified API and handles the complexity of bank-specific protocols.</p>

<p>The trickiest scenario in payment processing is what engineers call the &ldquo;dual write&rdquo; problem: payment succeeds at the gateway, but the callback to the booking system fails or times out. Now the user&rsquo;s money has been deducted, but the booking system does not know about it.</p>

<p>Airlines handle this with a reconciliation job. The payment service periodically queries the gateway for the status of all pending transactions. When it finds a transaction that the gateway shows as successful but the booking system has not confirmed, it triggers the booking confirmation flow retroactively.</p>

<p>This is why sometimes your bank shows a deduction before you receive the booking confirmation email. The payment captured, but the confirmation flow was delayed.</p>

<p>Idempotency is critical throughout. Every payment request is assigned a unique idempotency key — typically a combination of the reservation ID and an attempt number. If the same payment request is submitted twice (due to a retry), the payment service checks whether it has already processed a request with this idempotency key and returns the cached result instead of creating a duplicate charge.</p>

<a name="PNR.Generation.System"></a>
<h2>PNR Generation System</h2>

<p>PNR stands for Passenger Name Record. It is the six-character alphanumeric code that uniquely identifies your booking. When you call the airline, this is the first thing they ask for.</p>

<p>PNRs are not just user-facing identifiers. They are the primary key of the global booking record in airline systems. They are recognized by Global Distribution Systems (GDS) like Amadeus and Sabre, which airlines use to coordinate with travel agents and codeshare partners.</p>

<p>Generating a PNR has specific requirements. It must be unique across all bookings, not just current bookings but all historical bookings. It must be short enough to be communicated verbally. It must not contain characters that look similar (0 and O, 1 and I are typically excluded). It must be generated in a distributed system where multiple instances need to produce non-colliding IDs simultaneously.</p>

<p>The simplest approach is to generate a random 6-character alphanumeric string from a safe character set (A-Z, 2-9, excluding confusable characters) and check for collisions before committing. With roughly 34<sup>6</sup> possible combinations (about 1.5 billion), the collision probability is very low in practice.</p>

<p>A more sophisticated approach uses a distributed ID generation service — similar to Twitter&rsquo;s Snowflake or Flickr&rsquo;s ticket server — that guarantees uniqueness without requiring a collision check. The service encodes a timestamp, a machine ID, and a sequence number into a compact representation that maps to the PNR character space.</p>

<p>The PNR lookup system must be extremely fast. Airport agents, customer service representatives, and self-service kiosks all query PNRs constantly. The PNR record and its associated booking details are cached aggressively in a fast key-value store like Redis, with the relational database serving as the authoritative source.</p>

<a name="Ticketing.System"></a>
<h2>Ticketing System</h2>

<p>The e-ticket is a legally valid travel document. It proves that you have a confirmed reservation on a specific flight. Its generation is not optional — without a ticket, the booking is not complete.</p>

<p>E-ticket generation involves assembling a record that includes the passenger name exactly as it appears on their ID document, the flight details (number, date, departure, arrival, times), the seat assignment, the fare paid, applicable taxes, the ticket number (distinct from the PNR), the booking class, the ticket validity dates, and any ancillary services purchased.</p>

<p>Ticket numbers follow the IATA standard format: a three-digit airline code followed by a ten-digit number. IndiGo&rsquo;s airline code is 6E, which corresponds to a specific numeric prefix in the IATA ticketing system. These numbers must be unique, sequentially issued, and globally unambiguous.</p>

<p>After generation, the ticket is stored in the ticketing database and the PDF or structured data is made available for download and email delivery. The ticket data is also transmitted to the Departure Control System (DCS), which airport staff use to manage boarding.</p>

<p>Ticket modification — a name correction, a date change — triggers a re-issuance workflow. The original ticket is voided and a new ticket is generated with a new ticket number. The fare difference, if any, is collected or refunded. This re-issuance must maintain the integrity of the audit trail.</p>

<a name="Check-In.Architecture"></a>
<h2>Check-In Architecture</h2>

<p>Online check-in typically opens 48 hours before departure and closes a few hours before boarding. The check-in system is architecturally simpler than the reservation system, but it has strict operational constraints.</p>

<p>When a passenger checks in online, the system validates that the reservation is in CONFIRMED state, that the check-in window is open, that the passenger&rsquo;s travel documents are valid (for international flights), and that no operational holds exist on the booking (such as an unpaid fee).</p>

<p>If validation passes, the system upgrades the booking to CHECKED_IN state and generates a boarding pass. The boarding pass contains the passenger name, flight number, seat number, boarding time, gate number, and a barcode or QR code that encodes the BCBP (Bar Coded Boarding Pass) standard used globally.</p>

<p>The boarding pass barcode must be scannable at airport security and at the gate. This requires the check-in system to be synchronized with the Departure Control System in real time. The DCS is the system of record during airport operations — it knows which passengers have checked in, which bags have been tagged, which passengers have boarded, and whether the flight is ready to close.</p>

<p>In the event of a flight delay or gate change, the DCS triggers notifications that flow through the airline&rsquo;s notification infrastructure to reach passengers via SMS and app push notifications.</p>

<a name="Notification.Infrastructure"></a>
<h2>Notification Infrastructure</h2>

<p>Notifications in an airline system are not a nice-to-have. A missed gate change notification can mean a passenger misses their flight. A delayed refund notification affects trust. A timely check-in reminder reduces no-shows.</p>

<p>The notification system consumes events from the main booking and operations event stream. When a booking is confirmed, a BOOKING_CONFIRMED event is published. The notification service listens for this event and dispatches confirmations via email, SMS, and push notification.</p>

<p>Different notification channels have different reliability characteristics. Email is reliable but slow. SMS is fast but can fail in poor network areas. Push notifications require the user to have the app installed with notifications enabled.</p>

<p>For critical notifications like booking confirmations, the system sends via multiple channels simultaneously and tracks delivery receipts where possible. For reminders, the system schedules notifications in a job queue with retry logic — if an SMS fails to deliver, the job retries with exponential backoff.</p>

<p>At scale, notification delivery is managed through dedicated providers: AWS SES or SendGrid for email, Twilio or Route Mobile for SMS, Firebase Cloud Messaging for push. The booking system itself does not directly call these providers — it publishes events, and the notification service handles the downstream dispatch. This decoupling means a transient SMS provider outage does not affect the booking flow.</p>

<a name="Cancellation.and.Refund.Systems"></a>
<h2>Cancellation and Refund Systems</h2>

<p>Cancellation is the unhappy path, and it involves undoing a completed distributed transaction — which is hard.</p>

<p>When a user cancels a booking, the system must: update the reservation state to CANCELLED, calculate the refund amount based on the fare rules and cancellation timing, release the inventory back to the flight, initiate the refund through the payment service, generate a cancellation ticket, and notify the passenger.</p>

<p>Refund calculation is non-trivial. IndiGo (like most airlines) has tiered cancellation penalties based on how many hours before departure the cancellation occurs. The fare rules engine must apply these penalties correctly, accounting for non-refundable components like certain taxes and surcharges, seat selection fees, and baggage fees.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Cancellation Window</th>
        <th>Refund Percentage</th>
        <th>Processing Time</th>
      </tr>
    </thead>
    <tbody>
      <tr><td>More than 7 days before departure</td><td>High (minus penalty)</td><td>5-7 business days</td></tr>
      <tr><td>2-7 days before departure</td><td>Medium (minus penalty)</td><td>5-7 business days</td></tr>
      <tr><td>Less than 48 hours</td><td>Low or nil depending on fare type</td><td>5-7 business days</td></tr>
      <tr><td>Non-refundable fares</td><td>Taxes only</td><td>5-7 business days</td></tr>
    </tbody>
  </table>
</div>


<p>Payment reversal is initiated through the payment gateway&rsquo;s refund API. The refund takes days to reflect in the customer&rsquo;s account because it must travel through the payment network back to the customer&rsquo;s bank or card, which operates on T+2 or T+3 settlement cycles.</p>

<p>Reconciliation is a significant operational challenge. The airline&rsquo;s internal accounting system must track every booking, payment, refund, and partial refund and ensure that the amounts match what the payment gateway and bank settlement reports show. Discrepancies must be investigated and resolved, which requires detailed audit logs at every step of the payment and refund flow.</p>

<a name="Event-Driven.Architecture"></a>
<h2>Event-Driven Architecture</h2>

<p>Modern airline platforms are deeply event-driven. Almost every significant action in the booking lifecycle produces an event, and those events drive subsequent processing asynchronously.</p>

<div class="mermaid">
graph TD
  A[Reservation Service] &#8211;>|BookingConfirmed| B[Event Bus]
  C[Payment Service] &#8211;>|PaymentCaptured| B
  D[Inventory Service] &#8211;>|InventoryUpdated| B
  E[Ticketing Service] &#8211;>|TicketIssued| B
  F[Check-in Service] &#8211;>|PassengerCheckedIn| B
  B &#8211;> G[Notification Service]
  B &#8211;> H[Analytics Platform]
  B &#8211;> I[Loyalty Service]
  B &#8211;> J[Search Index Updater]
  B &#8211;> K[Audit Log Service]
  B &#8211;> L[Reconciliation Service]

  classDef producer fill:#1565c0,color:#fff,stroke:none
  classDef bus fill:#0d47a1,color:#fff,stroke:none
  classDef consumer fill:#0a3d62,color:#fff,stroke:none

  class A,C,D,E,F producer
  class B bus
  class G,H,I,J,K,L consumer
</div>


<p>The event bus (typically Kafka in production systems of this scale) acts as the central nervous system. Services publish events without knowing who will consume them. New consumers — a new analytics pipeline, a new loyalty integration — can be added without modifying the producers.</p>

<p>Kafka&rsquo;s durability guarantees mean that events are not lost even if a consumer is temporarily down. When the notification service restarts after an outage, it resumes consuming from where it left off and delivers any confirmations that were queued during downtime.</p>

<p>Kafka partitioning ensures ordering. Events for the same booking are routed to the same partition, guaranteeing that a TicketIssued event for booking X is processed after the BookingConfirmed event for booking X by the same consumer.</p>

<a name="Database.Design"></a>
<h2>Database Design</h2>

<p>The core entities in an airline booking system and their rough schemas:</p>

<p>Flights table: flight_id, flight_number, origin, destination, departure_time, arrival_time, aircraft_type, status. Indexed on (origin, destination, departure_date) for search queries.</p>

<p>Inventory table: flight_id, fare_class, total_seats, available_seats, held_seats, sold_seats, version. The version column enables optimistic locking. This table sees extremely high write contention and may benefit from row-level sharding or in-memory replication.</p>

<p>Bookings table: booking_id, pnr, status, created_at, updated_at, user_id, total_fare, currency. Partitioned by created_at for query performance. Indexed on pnr for fast lookups.</p>

<p>Passengers table: passenger_id, booking_id, first_name, last_name, dob, passport_number, nationality, seat_number, meal_preference. Linked to bookings via booking_id.</p>

<p>Payments table: payment_id, booking_id, amount, currency, status, gateway_transaction_id, payment_method, created_at, updated_at. The gateway_transaction_id must have a unique index to prevent duplicate payment recording.</p>

<p>Tickets table: ticket_id, ticket_number, booking_id, passenger_id, flight_id, issued_at, voided_at, status. Ticket numbers are globally unique and indexed for fast retrieval.</p>

<p>Refunds table: refund_id, booking_id, payment_id, amount, reason, status, initiated_at, completed_at. Linked to both the booking and the original payment for reconciliation.</p>

<p>Partitioning strategy: bookings and payments tables are partitioned by date range. Older partitions are moved to cold storage or archived. Active partitions for recent months remain on fast SSD-backed storage. This keeps query performance high as the dataset grows over years.</p>

<a name="Caching.Systems"></a>
<h2>Caching Systems</h2>

<p>Caching is what allows an airline platform to serve millions of requests per day without melting its databases.</p>

<p>Flight schedule data — routes, flight numbers, departure times — changes infrequently. This is aggressively cached at the CDN level and in application-level Redis caches with TTLs of several hours.</p>

<p>Fare data changes frequently but not per-millisecond. Fare cache entries for a specific route-date-class combination have TTLs of one to five minutes. This means a user might see a price that is slightly stale, but the actual price is confirmed at checkout. If the price has changed, the user is informed and asked to confirm.</p>

<p>Inventory data is the most volatile. The available seat count for a hot route can change tens of times per minute. The inventory service uses a write-through cache: every inventory update writes to both Redis and the database. The cache is the primary read path for the booking flow (checking if seats are available), while the database is the authoritative write path (actually decrementing counts with optimistic locking).</p>

<p>Search results for popular routes like DEL-BOM or BOM-GOI are precomputed and cached at the CDN edge. When a user in Chennai searches for a popular domestic route, the response can be served from a CDN node in Chennai without any backend processing. This reduces both latency and backend load dramatically.</p>

<p>Cache invalidation for inventory is event-driven. When an inventory update event is published to Kafka, a cache invalidation consumer reads the event and invalidates or updates the relevant cache entries. This keeps the cache reasonably fresh without requiring the write path to manage cache coherence synchronously.</p>

<a name="Scalability.Deep.Dive"></a>
<h2>Scalability Deep Dive</h2>

<p>IndiGo sees enormous traffic spikes during flash sales, long weekends, holiday announcements, and flight schedule releases. The system must handle 10x or 20x normal traffic during these events.</p>

<p>The search service is the easiest to scale horizontally. Since it is stateless and primarily read-oriented, adding more search service instances behind the load balancer immediately increases capacity. The search index (Elasticsearch) can be horizontally scaled by adding data nodes and increasing replica count.</p>

<p>The inventory service is harder to scale because it is write-heavy and requires strong consistency. Horizontal scaling of the inventory service requires careful partitioning — all inventory operations for a given flight must go to the same database shard, to prevent cross-shard distributed transactions. Flights can be partitioned by flight_id modulo the number of shards.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Component</th>
        <th>Scaling Strategy</th>
        <th>Bottleneck</th>
        <th>Mitigation</th>
      </tr>
    </thead>
    <tbody>
      <tr><td>Search Service</td><td>Horizontal, stateless</td><td>Index refresh lag</td><td>Increase replicas, tune refresh interval</td></tr>
      <tr><td>Fare Engine</td><td>Horizontal, stateless</td><td>Fare rules DB reads</td><td>Cache fare rules aggressively</td></tr>
      <tr><td>Inventory Service</td><td>Sharded by flight</td><td>Write contention on hot flights</td><td>Redis atomic ops, retry with backoff</td></tr>
      <tr><td>Reservation Service</td><td>Horizontal with saga orchestration</td><td>Payment gateway latency</td><td>Async payment confirmation</td></tr>
      <tr><td>Payment Service</td><td>Horizontal, idempotent</td><td>Gateway rate limits</td><td>Multiple gateway integrations, circuit breaker</td></tr>
      <tr><td>Notification Service</td><td>Horizontal consumers from Kafka</td><td>Provider rate limits</td><td>Queue and batch, multiple providers</td></tr>
    </tbody>
  </table>
</div>


<p>During flash sales, one additional technique is queue-based load leveling. Instead of allowing all users to hit the booking service simultaneously, the system places booking requests into a queue and processes them at a controlled rate. Users see a virtual waiting room UI while their request queues. This protects backend systems from being overwhelmed while ensuring all users are served in order.</p>

<a name="Reliability.and.Availability"></a>
<h2>Reliability and Availability</h2>

<p>An airline booking platform must be extremely available. Downtime directly costs revenue and damages brand trust. The target availability for the core booking path is typically 99.95% or higher.</p>

<p>Reliability is achieved through redundancy at every layer. Multiple availability zones for all services. Active-active database replication for reads. Automated failover for primary database failures. Circuit breakers on all external dependencies — if the payment gateway starts responding slowly, the circuit breaker opens and surfaces a friendly error instead of allowing requests to pile up and cause cascading failures.</p>

<p>Health checks and automated instance replacement ensure that unhealthy service instances are removed from the load balancer pool and replaced by the container orchestration platform (Kubernetes) automatically.</p>

<p>Chaos engineering — deliberately injecting failures into the system in controlled ways — is practiced by mature engineering teams to identify weaknesses before they manifest as production incidents.</p>

<a name="Security.Considerations"></a>
<h2>Security Considerations</h2>

<p>Payment security is governed by PCI-DSS compliance requirements. IndiGo&rsquo;s payment infrastructure does not store raw card numbers — these are tokenized by the payment gateway and replaced with non-sensitive tokens that can be stored and used for refunds without exposing card data.</p>

<p>All communication between services is encrypted in transit using TLS. Authentication between services uses mutual TLS or JWT tokens issued by a central identity service.</p>

<p>Fraud detection runs asynchronously on booking data. Signals like unusual booking velocity from a single IP, multiple bookings for the same passenger on overlapping flights, and card BIN country mismatch with billing address trigger fraud scoring. High-score bookings may be flagged for manual review or automatically blocked.</p>

<p>Bot attacks and fare scraping are countered with rate limiting at the API Gateway level, CAPTCHA challenges for suspicious traffic patterns, and behavioral analysis that distinguishes human users from automated scrapers by examining mouse movement patterns, request timing, and session behavior.</p>

<a name="Engineering.Tradeoffs"></a>
<h2>Engineering Tradeoffs</h2>

<p>Every architectural decision in a booking system involves tradeoffs. Understanding these tradeoffs is what separates a good system designer from a great one.</p>

<p>Consistency vs availability in inventory: A strictly consistent inventory system ensures zero double bookings but may reject valid booking requests under high contention (for example, when optimistic locking retries are exhausted). A more available system might allow a tiny number of oversells and resolve them operationally. Most airlines choose strict consistency and accept the small percentage of failed booking attempts, because overselling a seat creates a far worse experience than a booking failure.</p>

<p>Reservation hold duration vs user experience: A longer hold gives users more time to complete payment, reducing checkout abandonment. But longer holds mean inventory is locked for longer, which can make flights appear more full than they are, discouraging other potential buyers. Airlines tune hold durations carefully — 10 to 15 minutes is a common sweet spot.</p>

<p>Fare caching vs freshness: Caching fares for five minutes means users occasionally see slightly stale prices. The alternative — computing fares in real time for every display — is far more expensive and slower. The solution is to cache the displayed fare but revalidate the price at checkout, which is the industry standard approach.</p>

<p>Dynamic pricing vs predictability: Revenue management systems that reprice frequently maximize airline revenue but create an unpredictable experience for users. This is a deliberate business decision, not a technical limitation. Some airlines offer price-lock features (often for a small fee) that let users lock a fare while they decide, which is itself a product built on top of the reservation hold mechanism.</p>

<a name="Real-World.Technology.Stack"></a>
<h2>Real-World Technology Stack</h2>

<p>Large-scale airline booking platforms typically use a combination of the following technologies, each chosen for specific characteristics:</p>

<p>Java and Spring Boot form the backbone of most reservation and payment services. Java&rsquo;s maturity, strong concurrency support, and vast ecosystem make it a reliable choice for financial-grade transaction processing.</p>

<p>Go is increasingly used for high-throughput, low-latency services like the search API and the notification dispatcher. Go&rsquo;s lightweight goroutines and fast startup time make it excellent for services that handle massive concurrent request volumes.</p>

<p>Apache Kafka is the event streaming backbone. It provides durable, ordered, partitioned event streams with replay capability. For an airline platform, the ability to replay events is invaluable for debugging incidents and reprocessing failed operations.</p>

<p>Redis serves multiple roles: distributed cache, session store, inventory counter with atomic operations, and rate limiter. Its single-threaded event loop and in-memory speed make it ideal for high-throughput, low-latency operations.</p>

<p>PostgreSQL is the primary relational database for bookings, payments, and passengers. Its strong ACID guarantees, row-level locking, and mature replication features make it well-suited for financial transaction records.</p>

<p>Elasticsearch powers the flight search index. Its full-text search and aggregation capabilities, combined with horizontal scalability, make it the right tool for search workloads.</p>

<p>Kubernetes orchestrates all services. It provides automatic scaling, health management, rolling deployments, and resource isolation.</p>

<p>CDN systems (Akamai, Cloudflare) cache static assets and popular search results at edge nodes globally, reducing latency for end users and offloading traffic from origin servers.</p>

<a name="System.Design.Interview.Perspective"></a>
<h2>System Design Interview Perspective</h2>

<p>When an interviewer asks you to design an airline booking system like IndiGo, they are primarily testing your understanding of distributed systems, concurrency, and tradeoffs — not your knowledge of airlines specifically.</p>

<p>The most common opening question is: &ldquo;Design a flight search and booking system.&rdquo; Strong candidates immediately clarify requirements: read vs write ratio, expected scale, consistency requirements, and failure tolerance.</p>

<p>The areas that differentiate strong from weak answers are exactly the areas covered in this blog. Weak candidates design a simple CRUD application with a single database. They propose a direct SELECT from the inventory table for every search query and ignore concurrency issues in the booking flow.</p>

<p>Strong candidates identify the read-write asymmetry immediately: search is orders of magnitude more frequent than booking, so they deserve separate optimization strategies. They introduce caching for search, a dedicated inventory service with optimistic locking for booking, and an event-driven architecture for post-booking processes.</p>

<p>The inventory management section is the most important thing to get right in an interview. Explain fare buckets, soft holds, optimistic locking, and expiry TTLs. This shows you understand the real complexity of the problem.</p>

<p>The payment section is where candidates demonstrate understanding of distributed failure scenarios. Discuss idempotency keys, the dual-write problem, and the reconciliation job. Interviewers at companies like Flipkart, Razorpay, MakeMyTrip, and OYO ask exactly these kinds of questions.</p>

<p>On scalability, go beyond &ldquo;add more servers.&rdquo; Discuss partitioning strategies for the inventory service, CDN caching for search, and Kafka-based decoupling for async processing. Explain the tradeoffs of each decision.</p>

<p>Common mistakes to avoid: not handling the inventory race condition, not discussing payment failure scenarios, ignoring the hold expiry mechanism, and not distinguishing between the search data model and the booking data model.</p>

<p>The best answers treat the booking system as what it really is: a distributed systems problem with financial consequences, where correctness and reliability matter more than raw throughput.</p>

<a name="Closing.Thoughts"></a>
<h2>Closing Thoughts</h2>

<p>Building a system like IndiGo&rsquo;s booking platform is one of the genuinely hard engineering challenges in the software industry. It combines real-time inventory management, financial transaction correctness, distributed system coordination, and high availability into a single product that millions of people depend on daily.</p>

<p>The engineering principles that make this system work — event sourcing, idempotency, optimistic locking, saga-based distributed transactions, aggressive caching with event-driven invalidation, and graceful degradation — are the same principles that apply across any system that must be fast, reliable, and correct at scale.</p>

<p>If you understand how a flight booking platform is built, you understand distributed systems. And that understanding translates directly into better system design across every domain you work in.</p>
]]></content>
    </entry>
    
    <entry>
        <title type="html"><![CDATA[How YouTube Scaled to Billions of Users Using MySQL?]]></title>
        <link href="https://rishijeet.github.io/blog/how-youtube-scaled-to-billions-of-users-using-mysql/"/>
        <updated>2026-05-30T13:26:58+05:30</updated>
        <id>https://rishijeet.github.io/blog/how-youtube-scaled-to-billions-of-users-using-mysql</id>
        <content type="html"><![CDATA[<blockquote><p><em>There is a common belief in software engineering circles that relational databases cannot scale. The moment you say &ldquo;MySQL&rdquo; in a room full of distributed systems engineers, someone will inevitably raise an eyebrow and say &ldquo;that won&rsquo;t hold up at scale.&rdquo; YouTube&rsquo;s story is one of the most powerful counterarguments to that assumption. At the time of its acquisition by Google in 2006, YouTube was already one of the fastest-growing websites on the internet. And it was running on MySQL.</em></p></blockquote>

<p><img src="/images/system_design/youtube-mysql.jpg" height="300" width="900" alt="Alt text" /></p>

<p>Understanding how YouTube built and scaled its database infrastructure is not just a historical curiosity. It is a practical masterclass in how thoughtful engineers take ordinary tools and stretch them far beyond what anyone thought possible. The lessons here apply directly to the systems you are building today.</p>

<a name="The.Scale.of.the.Problem"></a>
<h2>The Scale of the Problem</h2>

<p>Let&rsquo;s ground this with numbers. YouTube launched in 2005. Within six months it was serving more than two million video views per day. By 2006, that number had climbed to over 100 million views per day. Google acquired it for $1.65 billion in October of that year. By 2012, more than 72 hours of video were being uploaded every minute. Today that number exceeds 500 hours per minute.</p>

<!--more-->


<p>Every single one of those uploads, views, likes, comments, subscriptions, and playlist additions generates database traffic. Most of it lands on metadata — who uploaded what, who watched what, who subscribed to whom. And for years, the engine handling that metadata was MySQL.</p>

<p>The engineers at YouTube did not achieve this by luck or brute force. They achieved it through a series of deliberate architectural decisions, each one designed to address a specific bottleneck at a specific moment in time. That is the story worth understanding.</p>

<a name="The.Early.Architecture"></a>
<h2>The Early Architecture</h2>

<p>In the beginning, YouTube was a classic LAMP stack application. Linux, Apache, MySQL, and Python. The earliest engineers chose this combination because it was fast to build with, well understood, and cost-effective. There was no budget for exotic distributed systems. There was no team large enough to operate them even if the budget existed.</p>

<p>The data model was straightforward. You had users, videos, comments, and subscriptions. A user had a profile. A video had an owner, a title, a description, tags, and a view count. Comments belonged to videos and users. Subscriptions connected one user to another. None of this was conceptually complicated. It mapped cleanly to relational tables.</p>

<p>A simplified version of the early schema looked something like this:</p>

<pre><code class="sql">CREATE TABLE users (
    user_id      BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    username     VARCHAR(50) NOT NULL,
    email        VARCHAR(255) NOT NULL,
    password_hash VARCHAR(64) NOT NULL,
    created_at   DATETIME NOT NULL,
    PRIMARY KEY (user_id),
    UNIQUE KEY idx_username (username),
    UNIQUE KEY idx_email (email)
);

CREATE TABLE videos (
    video_id     BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    uploader_id  BIGINT UNSIGNED NOT NULL,
    title        VARCHAR(100) NOT NULL,
    description  TEXT,
    status       ENUM('processing', 'active', 'deleted') NOT NULL DEFAULT 'processing',
    view_count   BIGINT UNSIGNED NOT NULL DEFAULT 0,
    duration_sec INT UNSIGNED,
    uploaded_at  DATETIME NOT NULL,
    PRIMARY KEY (video_id),
    KEY idx_uploader (uploader_id),
    KEY idx_uploaded_at (uploaded_at)
);

CREATE TABLE subscriptions (
    subscriber_id  BIGINT UNSIGNED NOT NULL,
    channel_id     BIGINT UNSIGNED NOT NULL,
    subscribed_at  DATETIME NOT NULL,
    PRIMARY KEY (subscriber_id, channel_id),
    KEY idx_channel (channel_id)
);

CREATE TABLE comments (
    comment_id   BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    video_id     BIGINT UNSIGNED NOT NULL,
    user_id      BIGINT UNSIGNED NOT NULL,
    body         TEXT NOT NULL,
    posted_at    DATETIME NOT NULL,
    PRIMARY KEY (comment_id),
    KEY idx_video (video_id),
    KEY idx_user (user_id)
);

CREATE TABLE playlists (
    playlist_id  BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    owner_id     BIGINT UNSIGNED NOT NULL,
    title        VARCHAR(150) NOT NULL,
    created_at   DATETIME NOT NULL,
    PRIMARY KEY (playlist_id),
    KEY idx_owner (owner_id)
);

CREATE TABLE playlist_items (
    playlist_id  BIGINT UNSIGNED NOT NULL,
    video_id     BIGINT UNSIGNED NOT NULL,
    position     INT UNSIGNED NOT NULL,
    added_at     DATETIME NOT NULL,
    PRIMARY KEY (playlist_id, position),
    KEY idx_video (video_id)
);
</code></pre>

<p>This schema is clean, normalized, and easy to reason about. It was the right choice for a startup. The mistake would have been to over-engineer it from day one.</p>

<a name="Why.MySQL."></a>
<h2>Why MySQL?</h2>

<p>The decision to use MySQL was not a random one. MySQL was the dominant open-source relational database in 2005. It had excellent Python and PHP client libraries. Its documentation was thorough. The engineering community knew how to operate it. Hosting providers supported it out of the box.</p>

<p>The alternatives at the time were limited. PostgreSQL existed but had a smaller deployment community. Oracle and DB2 were expensive and designed for enterprise procurement cycles, not startup velocity. The NoSQL movement was not yet a coherent thing — Cassandra was not released until 2008, MongoDB until 2009. The Bigtable paper had been published by Google in 2006, but it was not software you could download and run.</p>

<p>More importantly, MySQL&rsquo;s transactional semantics gave the early YouTube engineers something they needed: confidence. When a user uploaded a video and the metadata write succeeded, it succeeded completely. When a comment was posted, it was either there or it was not. Relational databases handle these kinds of guarantees naturally, and in the early days, correctness mattered more than raw throughput.</p>

<p>There is also the team dynamics angle. The early YouTube team was small. Hiring engineers who already knew MySQL was straightforward. Training new engineers on MySQL was fast. That is not a technical reason to choose a database, but it is a real-world reason that affects how fast you can build and how reliably you can operate.</p>

<a name="What.MySQL.Actually.Stored"></a>
<h2>What MySQL Actually Stored</h2>

<p>This is the section that confuses many engineers when they first learn about YouTube&rsquo;s architecture. MySQL stored metadata. Not video files. Understanding this distinction is essential to understanding why MySQL could scale at all.</p>

<p>Video files are enormous. A single one-minute video at 1080p is several hundred megabytes. At the time of YouTube&rsquo;s peak growth, hundreds of hours of video were being uploaded every minute. Storing those binary blobs in MySQL would have been catastrophic for several reasons.</p>

<p>The replication overhead alone would have been unbearable. MySQL replication works by shipping the binary log from primary to replicas. Every write to the primary gets replicated to every replica. If a video upload writes 500MB to the database, that 500MB gets shipped to every replica in your topology. With multiple replicas, your network bandwidth explodes. Your disk I/O on every replica server maxes out. Your replication lag balloons to the point where replicas become useless for serving reads.</p>

<p>Backup complexity becomes a nightmare. Taking a logical dump of a database that contains hundreds of terabytes of binary blobs is not a reasonable operation. The restore time alone would make recovery objectives impossible to meet.</p>

<p>MySQL&rsquo;s InnoDB storage engine is optimized for row-level operations on structured data. It is not optimized for streaming large blobs to clients. The buffer pool, which is InnoDB&rsquo;s primary caching mechanism, would fill entirely with video data, leaving no room to cache the index and row data that actually needs to be fast.</p>

<p>So YouTube stored video files in a purpose-built distributed object storage system. Google&rsquo;s infrastructure, after the acquisition, ultimately moved these to systems closely related to GFS (Google File System). Before the acquisition, YouTube used their own storage infrastructure. The key design principle was the same in both cases: the database stores a reference to the video file, not the file itself.</p>

<pre><code class="sql">CREATE TABLE video_storage (
    video_id      BIGINT UNSIGNED NOT NULL,
    quality       ENUM('240p', '360p', '480p', '720p', '1080p') NOT NULL,
    storage_key   VARCHAR(255) NOT NULL,   -- path or key in the object store
    file_size     BIGINT UNSIGNED NOT NULL,
    codec         VARCHAR(50),
    PRIMARY KEY (video_id, quality)
);
</code></pre>

<p>The <code>storage_key</code> is just a string. MySQL stores it in microseconds. The actual bytes live somewhere else. This pattern — storing metadata in a relational database and binary content in an object store — is one of the most important architectural lessons from YouTube&rsquo;s history, and it remains the correct approach for virtually every modern system that handles media.</p>

<a name="The.Scaling.Problem.Arrives"></a>
<h2>The Scaling Problem Arrives</h2>

<p>Growth that looks like a feature is actually a time bomb if your infrastructure is not ready for it. YouTube&rsquo;s user base did not grow linearly — it grew exponentially. And each new user did not just watch videos. They uploaded videos, left comments, subscribed to channels, created playlists, and clicked like buttons. Every one of those actions generated writes to MySQL.</p>

<p>The first problem to emerge was hot tables. The <code>videos</code> table was the center of gravity for almost every operation in the system. Watch a video — read from <code>videos</code>. Upload a video — write to <code>videos</code>. Get recommendations — read from <code>videos</code>. The table became a bottleneck not because MySQL was slow, but because the table was genuinely too hot. Too many concurrent reads and writes hitting the same data structures, the same indexes, the same pages in the buffer pool.</p>

<p>The <code>view_count</code> column in the <code>videos</code> table is worth discussing specifically. Every video view incremented a counter. At peak traffic, popular videos were receiving hundreds of thousands of views per minute. That means hundreds of thousands of <code>UPDATE videos SET view_count = view_count + 1 WHERE video_id = X</code> statements per minute, all hitting the same row. InnoDB&rsquo;s row-level locking handles concurrent updates well, but there is a physical limit to how fast you can serialize updates to a single row. This is called write hotspotting, and it is a fundamental distributed systems problem that no amount of hardware can fully solve.</p>

<p>The second problem was index growth. As the number of videos, users, and comments grew into the hundreds of millions, the indexes on the hot tables grew proportionally. An index on <code>video_id</code> in the <code>comments</code> table that once fit entirely in memory no longer fit. Every query that accessed comments for a video now required disk I/O. Disk I/O is orders of magnitude slower than memory access. Query latency climbed.</p>

<p>The third problem was backup windows. MySQL backups take the system offline or put significant load on it. As the dataset grew, the backup window grew with it. What once took minutes started taking hours. Backup complexity became an operational nightmare.</p>

<p>Vertical scaling — adding more RAM, more CPU, faster disks — bought time. But it could not buy enough time. Eventually the team had to confront the reality that a single MySQL server, no matter how powerful, had a ceiling. The ceiling was lower than the load.</p>

<a name="Read.Replicas:.The.First.Scaling.Move"></a>
<h2>Read Replicas: The First Scaling Move</h2>

<p>The first architectural response to the scaling problem was read replicas. This is a technique that is well understood today, but worth explaining precisely because the tradeoffs matter.</p>

<p>MySQL&rsquo;s primary-replica replication works like this: every write to the primary database is recorded in the binary log. Replicas connect to the primary, pull those log entries, and apply them to their own data. The replication is asynchronous — the primary does not wait for replicas to confirm before acknowledging a write to the application.</p>

<p>The practical effect is that you can direct all write traffic to the primary and distribute read traffic across multiple replicas. For YouTube, where the ratio of reads to writes was extremely high (far more people watch videos than upload them), this was an immediate win.</p>

<div class="mermaid">
graph TD
    A[Application Servers] &#8211;> B[Primary MySQL]
    A &#8211;> C[Replica 1]
    A &#8211;> D[Replica 2]
    A &#8211;> E[Replica 3]
    B &#8211;> C
    B &#8211;> D
    B &#8211;> E
    style B fill:#c0392b,color:#fff
    style C fill:#2980b9,color:#fff
    style D fill:#2980b9,color:#fff
    style E fill:#2980b9,color:#fff
    style A fill:#27ae60,color:#fff
</div>


<p>Adding a replica is operationally straightforward. You provision a new server, let it sync from the primary, and add it to the pool of servers that handle read queries. YouTube added many replicas to handle their growing read load.</p>

<p>But replication has a critical tradeoff: replication lag. Because replication is asynchronous, replicas can fall behind the primary. If a user uploads a video and immediately navigates to their video library, the read query might hit a replica that has not yet received the write. The video appears to be missing. This inconsistency is called eventual consistency — the replica will eventually catch up, but for a brief window, it shows stale data.</p>

<p>YouTube handled this with careful routing logic. Writes that needed to be immediately visible (like a user&rsquo;s own video upload) were read back from the primary, not a replica. Background reads and less latency-sensitive queries were spread across replicas. This kind of read-after-write consistency management is a real operational burden. It means your application code has to be aware of the replication topology, which adds complexity.</p>

<p>Another challenge: replicas help with reads, but they do nothing for write scalability. The primary still handles every write in the system. As write traffic grew, the primary became the bottleneck all over again.</p>

<a name="Sharding:.Horizontal.Scaling.for.Real"></a>
<h2>Sharding: Horizontal Scaling for Real</h2>

<p>Read replicas extend the read capacity of a single dataset. Sharding extends the write capacity by splitting the dataset itself across multiple independent database servers. It is the point at which &ldquo;scaling MySQL&rdquo; stops being a configuration exercise and becomes an architectural commitment.</p>

<p>The concept is simple: instead of one database that holds all the data, you have many databases, each holding a partition of the data. If you have ten shards and you route each user&rsquo;s data to a specific shard based on the user&rsquo;s ID, each shard holds roughly one-tenth of the total data and handles roughly one-tenth of the total traffic.</p>

<p>YouTube sharded their MySQL infrastructure based primarily on users. A user and all of their associated data — their videos, their comments, their subscriptions, their playlists — was co-located on the same shard. This choice was deliberate. By keeping related data together, you minimize the number of cross-shard joins and the number of shards you need to query to assemble a user&rsquo;s complete profile.</p>

<div class="mermaid">
graph TD
    A[Router Layer] &#8211;> B[Shard 0 users 0 to 9M]
    A &#8211;> C[Shard 1 users 9M to 18M]
    A &#8211;> D[Shard 2 users 18M to 27M]
    A &#8211;> E[Shard N users NxM to end]
    B &#8211;> F[Primary + Replicas]
    C &#8211;> G[Primary + Replicas]
    D &#8211;> H[Primary + Replicas]
    E &#8211;> I[Primary + Replicas]
    style A fill:#8e44ad,color:#fff
    style B fill:#c0392b,color:#fff
    style C fill:#c0392b,color:#fff
    style D fill:#c0392b,color:#fff
    style E fill:#c0392b,color:#fff
</div>


<p>The shard routing logic typically works through a lookup table or a deterministic hash. YouTube used a lookup table approach for some data, where a lightweight central mapping service told the application which shard to query for a given user ID. The lookup service itself had to be extremely fast and highly available — it was in the critical path of every database operation.</p>

<p>Hash-based sharding, where <code>shard_id = hash(user_id) % num_shards</code>, is simpler but has a brutal problem: resharding. If you start with 16 shards and grow to 32, a naive modulo hash sends roughly half of your users to different shards. You have to move enormous amounts of data while keeping the system live. YouTube invested significant engineering effort in building resharding tools that could migrate data incrementally, with careful coordination between the old shard assignment and the new one.</p>

<p>Consistent hashing is an alternative that reduces the resharding problem. By mapping both user IDs and shards to positions on a virtual ring, adding a new shard only requires moving data from one adjacent shard rather than rehashing all data. Virtual nodes (where each physical shard occupies multiple positions on the ring) further smooths the load distribution.</p>

<p>Sharding introduces cross-shard query problems. If you want to find all comments on a given video, and comments are sharded by the commenter&rsquo;s user ID, you may need to query multiple shards and merge the results. YouTube handled this by denormalizing certain data — storing copies of information in places where it was frequently accessed, rather than always going back to the canonical source. Denormalization trades storage efficiency and write complexity for read performance, and at internet scale, that tradeoff is almost always worth it.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Sharding Strategy</th>
        <th>Description</th>
        <th>Strengths</th>
        <th>Weaknesses</th>
        <th>Used For</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Range-based</td>
        <td>User IDs 0–9M on shard 0, 9M–18M on shard 1, etc.</td>
        <td>Simple routing, easy range scans</td>
        <td>Hotspots on new user shard, uneven load</td>
        <td>Early YouTube</td>
      </tr>
      <tr>
        <td>Hash-based (modulo)</td>
        <td>shard = hash(user_id) % N</td>
        <td>Even distribution, deterministic</td>
        <td>Expensive resharding when N changes</td>
        <td>Many early sharded systems</td>
      </tr>
      <tr>
        <td>Consistent hashing</td>
        <td>Virtual ring; adding shards moves minimal data</td>
        <td>Minimal data movement on resize</td>
        <td>More complex routing logic</td>
        <td>Later scaling phases</td>
      </tr>
      <tr>
        <td>Directory-based</td>
        <td>Central lookup table for user-to-shard mapping</td>
        <td>Flexible, supports irregular assignments</td>
        <td>Lookup service is a SPOF; extra round trip</td>
        <td>YouTube shard routing</td>
      </tr>
    </tbody>
  </table>
</div>


<a name="MySQL.Replication.Architecture.in.Depth"></a>
<h2>MySQL Replication Architecture in Depth</h2>

<p>Each shard in YouTube&rsquo;s MySQL topology was not a single server. Each shard was itself a small cluster: one primary and multiple replicas. The primary handled all writes for that shard. The replicas handled reads.</p>

<p>The replication topology evolved over time. In the early phases, each shard had a simple star topology — one primary, several replicas all reading directly from it. This worked well for a moderate number of replicas but eventually hit limits. The primary could only sustain so many replication connections and so much log shipping bandwidth before it became a bottleneck.</p>

<p>Later, YouTube implemented hierarchical replication: a relay replica would sit between the primary and a group of downstream replicas. The primary shipped logs to the relay, which then redistributed to the downstream replicas. This reduced the load on the primary and allowed each shard to scale to more replicas without the primary suffering.</p>

<div class="mermaid">
graph TD
    A[Primary] &#8211;> B[Relay Replica 1]
    A &#8211;> C[Relay Replica 2]
    B &#8211;> D[Read Replica]
    B &#8211;> E[Read Replica]
    B &#8211;> F[Analytics Replica]
    C &#8211;> G[Read Replica]
    C &#8211;> H[Read Replica]
    C &#8211;> I[Backup Replica]
    style A fill:#c0392b,color:#fff
    style B fill:#e67e22,color:#fff
    style C fill:#e67e22,color:#fff
    style D fill:#2980b9,color:#fff
    style E fill:#2980b9,color:#fff
    style F fill:#16a085,color:#fff
    style G fill:#2980b9,color:#fff
    style H fill:#2980b9,color:#fff
    style I fill:#7f8c8d,color:#fff
</div>


<p>Failover was a constant operational concern. If a shard&rsquo;s primary died, one of the replicas had to be promoted to primary. The challenge was ensuring that the promoted replica had fully caught up with the old primary before taking writes. Semi-synchronous replication, which requires at least one replica to acknowledge a write before the primary commits, was used to reduce the risk of data loss during failover. The tradeoff was slightly higher write latency — the primary had to wait for one acknowledgment before returning to the application.</p>

<p>The failover automation — detecting that a primary was down, selecting the most up-to-date replica, updating the routing table, and notifying application servers of the new primary — was its own significant engineering problem. YouTube built tooling to automate this process, but it required careful testing and regular drills to ensure it worked correctly when it was actually needed.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Replication Model</th>
        <th>How It Works</th>
        <th>Data Safety</th>
        <th>Write Latency Impact</th>
        <th>Use Case</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Asynchronous</td>
        <td>Primary commits, then ships log to replicas</td>
        <td>Risk of data loss on failover</td>
        <td>None</td>
        <td>Default MySQL replication; used for read scaling</td>
      </tr>
      <tr>
        <td>Semi-synchronous</td>
        <td>Primary waits for at least one replica ACK before commit</td>
        <td>At most one transaction lost on failover</td>
        <td>Adds one network RTT</td>
        <td>YouTube shard primaries</td>
      </tr>
      <tr>
        <td>Synchronous (Group Replication)</td>
        <td>All replicas must commit before primary acknowledges</td>
        <td>No data loss</td>
        <td>Significant; proportional to replica count</td>
        <td>Not typical at YouTube scale</td>
      </tr>
    </tbody>
  </table>
</div>


<a name="Caching.Saves.the.Day"></a>
<h2>Caching Saves the Day</h2>

<p>If you take one operational insight away from YouTube&rsquo;s database architecture, make it this: caching did more for their scalability than almost any database optimization. At peak traffic, the vast majority of data being served to users was coming from cache, not from MySQL at all.</p>

<p>YouTube used Memcached extensively. Memcached is a distributed, in-memory key-value store. It is extremely simple: you store a value under a key, and you retrieve it by key. It does not support queries, indexes, or transactions. It is not durable — if a Memcached server restarts, the cache is gone. None of that matters for its intended purpose.</p>

<p>The pattern is straightforward:</p>

<pre><code class="python">def get_video_metadata(video_id):
    cache_key = f"video:{video_id}"
    result = memcached.get(cache_key)
    if result is not None:
        return result
    # Cache miss: go to the database
    result = db.query(
        "SELECT * FROM videos WHERE video_id = %s", video_id
    )
    memcached.set(cache_key, result, ttl=300)  # Cache for 5 minutes
    return result
</code></pre>

<p>For a popular video, this means the database query executes very rarely. The first request after a cache miss populates the cache. All subsequent requests within the TTL window hit Memcached, which responds in under a millisecond. The database is protected from the thundering herd of requests that a viral video would otherwise generate.</p>

<p>YouTube cached aggressively: video metadata, user profiles, subscription lists, comment counts, recommendation data. The cache layer became as important to the architecture as the database layer. Engineers had to reason carefully about cache invalidation — when a user updated their profile, the cached version had to be purged or updated. When a video was deleted, its cache entry had to be removed.</p>

<p>Cache invalidation bugs are subtle and nasty. A stale cache entry for a deleted video meant users would see that video in recommendations or search results even after the owner deleted it. YouTube built explicit invalidation pipelines that would sweep through the cache and remove entries when the underlying data changed.</p>

<p>The other major caching challenge was cold starts. When a Memcached server was restarted or a new server was added to the pool, its cache was empty. All the requests that had been hitting that cache suddenly fell through to the database. If this happened for a large enough portion of the cache simultaneously — after a datacenter event, for example — the sudden flood of database queries could overwhelm the database and cause a cascading failure. YouTube engineers called this a &ldquo;thundering herd&rdquo; and built delays and jitter into cache population logic to smooth the reconstruction.</p>

<div class="mermaid">
graph LR
    A[User Request] &#8211;> B[App Server]
    B &#8211;> C{Memcached Hit?}
    C &#8211;>|Yes| D[Return Cached Data]
    C &#8211;>|No| E[Query MySQL Shard]
    E &#8211;> F[Populate Cache]
    F &#8211;> D
    style A fill:#27ae60,color:#fff
    style B fill:#2980b9,color:#fff
    style C fill:#8e44ad,color:#fff
    style D fill:#16a085,color:#fff
    style E fill:#c0392b,color:#fff
    style F fill:#e67e22,color:#fff
</div>


<a name="The.Video.Upload.Pipeline"></a>
<h2>The Video Upload Pipeline</h2>

<p>Understanding how a video upload actually works helps illustrate how YouTube kept the database out of the critical path for expensive operations.</p>

<p>When a user initiates an upload, the process looks roughly like this:</p>

<p>A request hits the application tier. A metadata record is inserted into MySQL immediately, with status set to <code>processing</code>. The video ID generated from this insert is returned to the client. The client uploads the raw video bytes directly to a storage endpoint — not to the application server, and certainly not to MySQL. The storage system acknowledges receipt and enqueues a transcoding job.</p>

<div class="mermaid">
graph TD
    A[User Upload Request] &#8211;> B[App Server Creates Metadata Row]
    B &#8211;> C[MySQL Shard Write - Status processing]
    B &#8211;> D[Return video_id to Client]
    D &#8211;> E[Client Uploads Video Bytes to Object Store]
    E &#8211;> F[Object Store Enqueues Transcoding Job]
    F &#8211;> G[Transcoding Workers Process Video]
    G &#8211;> H[Update MySQL - Status active plus storage_keys]
    G &#8211;> I[Generate Thumbnail]
    I &#8211;> H
    style A fill:#27ae60,color:#fff
    style C fill:#c0392b,color:#fff
    style F fill:#8e44ad,color:#fff
    style G fill:#e67e22,color:#fff
    style H fill:#c0392b,color:#fff
</div>


<p>The transcoding workers take the raw video and produce multiple quality versions: 240p, 360p, 480p, 720p, 1080p, and eventually 4K. Each version is stored in the object storage system. Once all versions are complete, the worker updates the MySQL record to change the status to <code>active</code> and records the storage keys for each quality level.</p>

<p>This asynchronous, pipeline-based approach means that MySQL is involved at the beginning of the upload (metadata creation) and at the end (status update), but not during the heavy lifting. The actual video bytes never touch MySQL. The transcoding, which is the most computationally expensive part of the process, happens in a separate system that MySQL knows nothing about.</p>

<p>This design also made the system resilient. If a transcoding worker crashed halfway through processing a video, the metadata record was still in MySQL with status <code>processing</code>. The job could be retried. Nothing was lost.</p>

<a name="The.Video.Watch.Pipeline"></a>
<h2>The Video Watch Pipeline</h2>

<p>Serving a video to a viewer is equally revealing. Consider what happens when you click play on a YouTube video:</p>

<p>The browser makes a request to YouTube&rsquo;s servers. The application needs to do several things: verify the video exists and is public, retrieve the metadata for display (title, description, channel name, view count, like count), fetch recommendations for the sidebar, retrieve top comments, and determine the best CDN endpoint from which to serve the video file.</p>

<div class="mermaid">
graph TD
    A[Viewer Clicks Play] &#8211;> B[Load Balancer]
    B &#8211;> C[App Server]
    C &#8211;> D{Metadata in Cache?}
    D &#8211;>|Yes| E[Return from Memcached]
    D &#8211;>|No| F[Query MySQL Shard]
    F &#8211;> G[Populate Memcached]
    G &#8211;> E
    E &#8211;> H[Fetch Recommendations]
    E &#8211;> I[Fetch Comments from Shard]
    E &#8211;> J[Determine CDN Node]
    J &#8211;> K[Stream Video from CDN]
    style A fill:#27ae60,color:#fff
    style C fill:#2980b9,color:#fff
    style D fill:#8e44ad,color:#fff
    style F fill:#c0392b,color:#fff
    style K fill:#16a085,color:#fff
</div>


<p>The video metadata and the channel information come from cache wherever possible. The recommendation system, which had its own separate data store and processing infrastructure, returns a list of video IDs. Those video IDs are then resolved to full metadata records, again from cache. The comments are fetched from the appropriate MySQL shard.</p>

<p>The video file itself is served from a CDN — a Content Delivery Network. YouTube built and operated an enormous CDN infrastructure that cached popular video files at edge nodes close to viewers around the world. For a popular video, the file was likely cached at a CDN node a few hundred miles from the viewer, not streaming all the way from a central datacenter. MySQL is completely uninvolved in the actual video delivery. It knows the storage key that points to the file. The CDN knows how to serve the file. These two systems talk to each other only during cache misses at the CDN layer.</p>

<a name="Database.Bottlenecks.YouTube.Encountered"></a>
<h2>Database Bottlenecks YouTube Encountered</h2>

<p>No architecture discussion is complete without an honest accounting of the failures and pain points. YouTube&rsquo;s MySQL infrastructure encountered several categories of problems repeatedly.</p>

<p>Index maintenance was a persistent challenge. Large tables with many indexes meant that every write operation had to update multiple data structures. An insert into the <code>comments</code> table might update three or four indexes in addition to writing the row itself. As indexes grew into the hundreds of millions of entries, the I/O cost of maintaining them became a major fraction of write latency. Teams had to be very deliberate about which indexes existed, periodically auditing and removing indexes that were no longer providing sufficient read benefit to justify their write overhead.</p>

<p>Replication lag was a recurring nightmare. At peak upload and engagement times, the binary logs from the primary generated more data than the replicas could apply at full speed. Replication lag would build up over hours, reaching minutes or even tens of minutes. A replica that is ten minutes behind cannot serve reads for anything that happened in the last ten minutes. Engineers would get paged in the middle of the night because replication lag on a critical shard had exceeded their threshold. Resolving it required either reducing write load, speeding up replicas, or both.</p>

<p>Long-running transactions caused lock contention. A query that ran for even a few seconds could hold locks that blocked hundreds of subsequent queries. Schema migrations on hot tables were particularly dangerous — adding a column to the <code>videos</code> table on a live system without causing an outage required careful use of tools like <code>pt-online-schema-change</code>, which performs the migration in the background by creating a shadow table and swapping atomically.</p>

<p><code>AUTO_INCREMENT</code> exhaustion was a real problem for tables that had existed since the early days. A <code>BIGINT UNSIGNED</code> column can hold values up to about 18.4 quintillion, which is comfortably unlimited for any foreseeable use case. But some early columns used <code>INT UNSIGNED</code>, which maxes out at about 4.3 billion. As the platform grew toward billions of rows in some tables, the prospect of wrapping around an integer ID was not hypothetical. Schema migrations to widen ID columns required careful coordination.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Bottleneck</th>
        <th>Root Cause</th>
        <th>Symptom</th>
        <th>Mitigation</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Write hotspot on view_count</td>
        <td>Millions of concurrent increments to a single row</td>
        <td>High lock wait time; slow writes</td>
        <td>Counter sharding; async batch updates; approximate counts</td>
      </tr>
      <tr>
        <td>Index bloat</td>
        <td>Many indexes on large tables</td>
        <td>Slow inserts and updates; high disk I/O</td>
        <td>Index audits; deferred indexing; covering indexes</td>
      </tr>
      <tr>
        <td>Replication lag</td>
        <td>Writes outpace replica apply speed</td>
        <td>Stale reads; replica unusable; pages at night</td>
        <td>More replicas; multi-threaded replication; reduce write amplification</td>
      </tr>
      <tr>
        <td>Schema migrations</td>
        <td>ALTER TABLE locks the table</td>
        <td>Seconds-long outages; write queue backup</td>
        <td>pt-online-schema-change; blue-green deploys</td>
      </tr>
      <tr>
        <td>Cross-shard joins</td>
        <td>Related data on different shards</td>
        <td>Complex application code; multi-shard queries</td>
        <td>Denormalization; application-layer joins; data locality design</td>
      </tr>
      <tr>
        <td>INT ID overflow</td>
        <td>4-byte ID approaching max value</td>
        <td>Silent corruption risk; insert failures</td>
        <td>Migrate to BIGINT; monitor high watermarks</td>
      </tr>
    </tbody>
  </table>
</div>


<a name="Event-Driven.Architecture.Evolution"></a>
<h2>Event-Driven Architecture Evolution</h2>

<p>As YouTube matured, the team recognized that not every write needed to be synchronous. When a user subscribed to a channel, the subscription row needed to be written immediately — but the notification to the channel owner, the feed update, the recommendation model update, and the analytics counter increment did not. These could be processed asynchronously.</p>

<p>This insight led YouTube toward an event-driven architecture. When a significant action occurred — a video upload, a subscription, a like — the application would publish an event to a message queue. Background workers would consume those events and carry out the downstream effects. This reduced the amount of work that each user-facing request had to do synchronously, which reduced latency. It also reduced the write amplification on MySQL — instead of updating five tables in a single transaction, you wrote one event to a queue and let workers handle the rest.</p>

<p>The message queue acted as a buffer. If the downstream workers fell behind, events queued up harmlessly. When the workers caught up, the queue drained. The user experience was slightly eventually consistent — your subscription notification might arrive a few seconds late — but this was a perfectly acceptable tradeoff.</p>

<p>This pattern eventually evolved into systems that closely resembled what is now commonly done with Apache Kafka: durable, distributed, append-only logs that allowed multiple consumers to read the same stream of events independently. Recommendation systems, analytics pipelines, notification systems, and feed generators could all consume the same event stream at their own pace.</p>

<p>The evolution toward event-driven architecture also reduced the coupling between systems. Before, updating the recommendations required a direct write to the recommendation database as part of the same transaction as the subscription write. After, the recommendation system just consumed subscription events from the queue and updated its own data store independently. This made each system easier to change, easier to operate, and easier to scale independently.</p>

<a name="Database.Schema.Design.Lessons"></a>
<h2>Database Schema Design Lessons</h2>

<p>The practical lessons from YouTube&rsquo;s schema evolution are worth cataloguing explicitly, because they recur in every system that grows from startup to scale.</p>

<p>Denormalization is not a dirty word. Normalized schemas are beautiful in textbooks because every piece of information lives in exactly one place. At scale, this means every read that needs multiple pieces of information requires joins, and joins at scale are expensive. YouTube gradually denormalized hot data — storing the uploader&rsquo;s username in the <code>videos</code> table alongside the <code>uploader_id</code>, for example, so that the video list page did not need to join against the <code>users</code> table for every row in the results. The tradeoff: when a user changed their username, two tables needed to be updated. At the scale of video views versus username changes, this was the right tradeoff.</p>

<p>Partitioning extends table capacity. MySQL&rsquo;s table partitioning feature allows a single logical table to be physically split into multiple files based on a column value. A <code>comments</code> table partitioned by <code>posted_at</code> month means that old comments live in separate physical partitions that can be archived, compressed, or dropped independently of recent comments. Queries that only need recent comments can be scoped to the relevant partitions, dramatically reducing the amount of data scanned.</p>

<p>Covering indexes eliminate table row fetches. An index that includes all the columns needed by a query allows MySQL to satisfy the query entirely from the index without touching the table data. For queries that read many rows but only need a few columns — like fetching the <code>title</code> and <code>uploaded_at</code> for all videos by a user — a covering index on <code>(uploader_id, uploaded_at, title)</code> can be dramatically faster than a non-covering index that forces a row lookup for each result.</p>

<p>Approximate counts are good enough. Updating <code>view_count</code> on every view is expensive. But users do not actually need the view count to be exact to the second. YouTube moved to approximate counting — updating counters in batches, using probabilistic data structures like HyperLogLog for unique visitor counts, and displaying rounded numbers (&ldquo;2.3 million views&rdquo;) rather than exact figures. This reduced write amplification enormously while preserving the user-facing feature.</p>

<a name="Scalability.Deep.Dive"></a>
<h2>Scalability Deep Dive</h2>

<p>It is worth mapping the scaling journey explicitly against the techniques deployed, because this sequence — vertical scaling, read replicas, caching, sharding — is the canonical path for any database that needs to grow from small to internet-scale.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>Phase</th>
        <th>Approximate Scale</th>
        <th>Primary Technique</th>
        <th>Key Constraint It Solved</th>
        <th>New Problems Introduced</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Launch</td>
        <td>Single-digit thousands of users</td>
        <td>Single MySQL server</td>
        <td>Simplicity and speed to market</td>
        <td>None yet</td>
      </tr>
      <tr>
        <td>Early growth</td>
        <td>Millions of users</td>
        <td>Vertical scaling (bigger server)</td>
        <td>Outgrown initial hardware</td>
        <td>Single point of failure; hardware ceiling approaching</td>
      </tr>
      <tr>
        <td>Hyper-growth</td>
        <td>Tens of millions of users</td>
        <td>Read replicas + Memcached</td>
        <td>Read saturation of single server</td>
        <td>Replication lag; cache invalidation complexity</td>
      </tr>
      <tr>
        <td>Post-acquisition scale-out</td>
        <td>Hundreds of millions of users</td>
        <td>User-based sharding</td>
        <td>Write saturation of single primary</td>
        <td>Cross-shard queries; resharding complexity</td>
      </tr>
      <tr>
        <td>Internet scale</td>
        <td>Billions of users</td>
        <td>Deeper sharding + event-driven + specialized stores</td>
        <td>Global scale; diverse workload types</td>
        <td>Operational complexity; polyglot persistence</td>
      </tr>
    </tbody>
  </table>
</div>


<p>One important nuance: these phases were not cleanly sequential. YouTube was deploying read replicas while still running on a single shard. They were deploying sharding while still investing in caching improvements. Real systems evolve in parallel, with multiple scaling efforts running simultaneously, each addressing a different bottleneck.</p>

<p>The vertical scaling phase deserves particular attention because it is often dismissed too quickly. Upgrading your database server from 32GB RAM to 256GB RAM, or from spinning disks to fast NVMe SSDs, can buy you months or years of additional runway without any code changes. That runway is valuable. It buys you time to implement the more complex solutions correctly, rather than under emergency pressure.</p>

<p>The danger of vertical scaling is that it creates a false sense of security. It works, and it works well — right up until it stops working. At that point, if you have not been investing in horizontal scaling capabilities, you face a crisis. The best-run teams treat vertical scaling as one tool among several, not as the final answer.</p>

<a name="Reliability.and.Disaster.Recovery"></a>
<h2>Reliability and Disaster Recovery</h2>

<p>Scaling is about handling more traffic. Reliability is about not losing data or availability when things go wrong. These are related but distinct engineering concerns, and YouTube had to address both.</p>

<p>Backups at MySQL scale are genuinely hard. Logical backups (<code>mysqldump</code>) produce a consistent snapshot by locking tables or using InnoDB&rsquo;s MVCC — but at large scale, they are slow to produce and slow to restore. Physical backups (copying the underlying InnoDB data files, using tools like Percona XtraBackup) are faster for both creation and restore, but require careful coordination to ensure consistency.</p>

<p>YouTube maintained backups at multiple levels: recent backups for rapid recovery, older backups for recovery from silent corruption, and backups replicated to geographically separate locations for disaster recovery. The operational discipline of regularly testing restores — not just taking backups, but actually restoring from them and verifying the data — was essential. A backup you have never restored is a backup you do not actually have.</p>

<p>Multi-region architecture required solving the replication problem at geographic scale. Replicating data across continents introduces hundreds of milliseconds of round-trip latency, which means even semi-synchronous replication becomes impractical as a primary consistency mechanism. YouTube&rsquo;s approach was to replicate asynchronously to remote regions and accept that the remote region&rsquo;s data was slightly behind. In a disaster recovery scenario, this meant some recent data might be lost. The amount of acceptable data loss, measured in time, is called the Recovery Point Objective (RPO). YouTube had to be explicit about its RPO and build infrastructure that met it.</p>

<a name="MySQL.vs.NoSQL:.An.Honest.Comparison"></a>
<h2>MySQL vs NoSQL: An Honest Comparison</h2>

<p>As YouTube scaled, the question inevitably arose: should they replace MySQL with something better suited to internet-scale workloads? The NoSQL movement was gaining momentum. Cassandra, MongoDB, HBase, and other systems promised horizontal scalability without the complexity of manual sharding.</p>

<p>The answer YouTube&rsquo;s engineers arrived at was nuanced, and it is worth unpacking.</p>

<div class="scrollable-table-container">
  <table class="scrollable-table">
    <thead>
      <tr>
        <th>System</th>
        <th>Data Model</th>
        <th>Horizontal Scale</th>
        <th>Consistency</th>
        <th>Query Flexibility</th>
        <th>Operational Maturity (circa 2008)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>MySQL (sharded)</td>
        <td>Relational; tables, rows, joins</td>
        <td>Manual sharding; significant engineering</td>
        <td>Strong within shard; eventual across shards</td>
        <td>High; full SQL</td>
        <td>Excellent; huge community</td>
      </tr>
      <tr>
        <td>Cassandra</td>
        <td>Wide-column; partition key plus clustering</td>
        <td>Native; designed for horizontal scale</td>
        <td>Tunable; typically eventual</td>
        <td>Limited; partition-key lookups optimized</td>
        <td>Poor; very new in 2008</td>
      </tr>
      <tr>
        <td>Bigtable / HBase</td>
        <td>Wide-column; row key plus column families</td>
        <td>Native; scales to petabytes</td>
        <td>Row-level strong consistency</td>
        <td>Limited; no secondary indexes natively</td>
        <td>Google-internal only until HBase emerged</td>
      </tr>
      <tr>
        <td>Dynamo-style (DynamoDB, Riak)</td>
        <td>Key-value; simple get/put by key</td>
        <td>Native; masterless, highly available</td>
        <td>Eventual; last-write-wins</td>
        <td>Very limited; key-only lookups</td>
        <td>Amazon&#8217;s Dynamo paper published 2007; not public</td>
      </tr>
      <tr>
        <td>MongoDB</td>
        <td>Document; JSON-like BSON documents</td>
        <td>Sharding available but limited early on</td>
        <td>Document-level strong consistency</td>
        <td>Moderate; rich query operators</td>
        <td>First release 2009; very immature initially</td>
      </tr>
    </tbody>
  </table>
</div>


<p>The key insight is that YouTube&rsquo;s engineers already had MySQL running in production, serving billions of requests per day, with a team that deeply understood its behavior. Migrating to an unproven system at that scale, under that traffic, carried enormous risk. A NoSQL system that could not be operated reliably in production was worse than a SQL system that required careful engineering to scale.</p>

<p>Over time, YouTube did introduce specialized stores for specific use cases. For time-series data (analytics, viewing history), systems closer to Bigtable or columnar databases were better fits. For the recommendation graph, graph databases or custom graph processing systems were more appropriate. For globally distributed, low-latency key-value access, Memcached&rsquo;s successor systems and eventually Spanner-like distributed databases became the right choice.</p>

<p>Could YouTube be built on MySQL today? Yes, with the same sharding and caching strategies they used. But a greenfield YouTube built today would likely use MySQL for its core metadata, Cassandra or DynamoDB for high-write, high-scale supplementary data (view events, engagement metrics), and Spanner or CockroachDB for globally consistent metadata. The architecture would be polyglot from the start, using the right tool for each data access pattern, rather than stretching a single system to cover all use cases.</p>

<a name="Engineering.Tradeoffs"></a>
<h2>Engineering Tradeoffs</h2>

<p>Every architectural decision in YouTube&rsquo;s history involved giving something up to get something else. Being explicit about these tradeoffs is what separates senior engineering thinking from junior thinking.</p>

<p>Consistency versus availability: sharded MySQL with asynchronous replication chose availability over strong consistency. When a replica fell behind, reads from that replica returned stale data rather than blocking until the replica caught up. Users occasionally saw inconsistent state. This was acceptable because the alternative — blocking user requests until replication caught up — would have made the product unusably slow during any replication lag event.</p>

<p>Normalization versus performance: normalized schemas are correct and compact. Denormalized schemas are faster for reads at the cost of write complexity and storage. YouTube moved steadily toward denormalization as read performance became the dominant concern. Every denormalization decision was a bet that the read traffic savings would outweigh the write complexity and the risk of inconsistency between the canonical record and its denormalized copies.</p>

<p>Engineering simplicity versus operational efficiency: a single MySQL instance is simple to operate and simple to reason about. A sharded MySQL topology with hundreds of shards, each with a primary and multiple replicas, is a complex operational environment. You gain write scalability at the cost of operational complexity. The team that operates this system needs more expertise, better tooling, and more rigorous processes. This cost is real, and it should not be underestimated when making architectural decisions.</p>

<p>Caching versus freshness: every cached entry represents a promise that you will tolerate serving potentially stale data. The longer the TTL, the lower the database load, but the more out-of-date the served data might be. YouTube tuned cache TTLs differently for different types of data. View counts, which change constantly and are tolerant of some staleness, could have short TTLs or be served from approximate in-memory counters. User profile data, which changes rarely but where staleness is more visible to users, warranted more careful invalidation.</p>

<a name="What.Modern.Engineers.Can.Learn"></a>
<h2>What Modern Engineers Can Learn</h2>

<p>The YouTube MySQL story is not just history. The patterns and antipatterns it illustrates are directly applicable to the systems being built today.</p>

<p>Start simple and add complexity when you hit actual bottlenecks. YouTube used a single MySQL server at launch because that was the right tool for the scale. Engineers who design for billions of users before they have thousands are wasting time and creating operational complexity that slows them down. The art is in knowing when the current architecture is genuinely at its limit, not when it looks inelegant.</p>

<p>Separate your data by access pattern. Video files and video metadata have fundamentally different access characteristics. Binary content belongs in object storage. Metadata belongs in a relational database. Time-series events belong in a columnar or time-series store. When you force all data types into one system, you end up with a system that handles all of them poorly.</p>

<p>Caching is a first-class architectural citizen, not an optimization. The YouTube architecture without Memcached would have required many times more database servers to serve the same traffic. Plan your caching layer from early in your architecture, not as an afterthought when the database starts struggling.</p>

<p>Understand your write patterns before they hurt you. The <code>view_count</code> hotspot was a predictable problem — any counter that gets incremented by millions of concurrent users will encounter write contention. Identifying these patterns early and designing around them (batch updates, approximate counts, counter sharding) is far less painful than fixing them in production.</p>

<p>Build operational tooling as a core competency. Failover automation, replication monitoring, schema migration tools, resharding scripts — these are not nice-to-haves. They are what makes the difference between an architecture that works in theory and one that works in production at three in the morning when something goes wrong.</p>

<a name="System.Design.Interview.Perspective"></a>
<h2>System Design Interview Perspective</h2>

<p>YouTube&rsquo;s database architecture is a favorite subject for system design interviews, and for good reason: it covers the full stack of database scaling techniques in a context that most candidates are familiar with.</p>

<p>When an interviewer asks you to design YouTube, the weak answer focuses immediately on which database to use. The strong answer starts by understanding the access patterns, data types, and scale requirements before making any technology choices.</p>

<p>Start by clarifying the data: what kinds of data does the system need to store, how large is each entity, how frequently is it read versus written, and what consistency guarantees are actually required? Establish a rough sense of scale: how many users, how many daily active users, how many video uploads per minute, how many view events per second.</p>

<p>Then work through the tiers of the architecture. Start with what goes in a relational database (user metadata, video metadata, subscription records, comment records) versus what goes elsewhere (the video files themselves in object storage, view event streams in a time-series or stream processing system, recommendation data in a specialized graph or ML store).</p>

<p>Discuss read replicas as the first scaling move. Explain why they help (read distribution) and where they fall short (no help for writes). Discuss caching as a complementary mechanism that dramatically reduces database load. Explain cache invalidation and the tradeoffs around cache TTLs.</p>

<p>When the interviewer asks how you would handle ten times or a hundred times more traffic, introduce sharding. Be specific about your sharding key (user ID is typically the right choice for YouTube-like data because it keeps related data co-located). Discuss the tradeoffs: cross-shard queries become hard, resharding is expensive, the routing layer adds complexity.</p>

<p>Common mistakes candidates make: jumping to Cassandra or DynamoDB without justifying why MySQL is insufficient, ignoring the separation between metadata and media storage, forgetting about the CDN for actual video delivery, and treating caching as an optional optimization rather than a core architectural component.</p>

<p>The strongest candidates discuss not just the happy path but the failure modes: what happens when a shard&rsquo;s primary fails, how you handle replication lag, how you monitor and alert on database health, and how you perform schema migrations without downtime. These operational considerations signal that you understand production systems, not just theoretical ones.</p>

<a name="Conclusion"></a>
<h2>Conclusion</h2>

<p>YouTube&rsquo;s story is ultimately a story about pragmatism. The engineers who built YouTube did not have a crystal ball that told them they were building one of the largest platforms in internet history. They had a LAMP stack, a growing user base, and a set of very smart people who understood their tools deeply enough to push them far beyond their apparent limits.</p>

<p>MySQL was not the wrong choice. It was the right choice for the right reasons, scaled through the right techniques. Read replicas, sharding, aggressive caching, careful schema design, event-driven architecture — none of these are exotic techniques. They are the discipline of understanding a system deeply enough to know when and how to extend it.</p>

<p>The lesson is not &ldquo;use MySQL for everything.&rdquo; The lesson is &ldquo;understand your data, understand your access patterns, scale the bottleneck in front of you, and do not add complexity before you need it.&rdquo; That discipline applies regardless of whether your database is MySQL, Postgres, Cassandra, or anything else.</p>

<p>The engineers who built YouTube&rsquo;s infrastructure were not doing magic. They were doing engineering. And that is exactly what makes their work worth studying.</p>
]]></content>
    </entry>
    
</feed>
